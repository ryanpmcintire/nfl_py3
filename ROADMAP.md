# NFL ATS research roadmap

Completed rows and dated wave logs live in [docs/roadmap_archive.md](docs/roadmap_archive.md).


This is the living backlog for the revived project. It deliberately includes
boring infrastructure, serious research ideas, and moonshots. A feature is not
"done" because it can be computed: it is done when its availability time is
documented, leakage tests pass, and its incremental value is measured in
walk-forward evaluation.

**Repository cut (2026-09-10).** Every `scripts/*.py` named by a row below
that no longer exists on disk was a one-off research script reachable from
nothing (not the capture scheduler, not the CLI, not `src/`, not any ops
file). They were removed in the 2026-09-10 hygiene commit and stay readable
at `git show b7ed31d^:scripts/<name>.py`; the rows keep naming them as the
evidence trail. The scripts that remain are the ones something runs.

## North star

Build an auditable system that estimates NFL game and cover probabilities,
measures uncertainty honestly, and supports paper decisions for ATS pools and
simulated bankrolls. Success means better calibrated out-of-time forecasts than
strong baselines—not an isolated profitable backtest.

**Primary goal (clarified 2026-08-17): beat the OPENING line the user's
football pool (Splash Sports) grades against.** Splash's pool engine posts
lines Tuesday, revises once Wednesday, then freezes them for the week — so
the Tuesday-opener grade, not the closing-line grade, is the headline
metric. First predeclared measurement (`docs/opener_evaluation.md`), scored
against the then-frozen incumbent `player` model only (`player` was not
promoted yet): **52.50% against openers** on 1,537 paired 2020–2025 games,
vs **51.09% against closes on the same games** — paired delta **+1.35
points**, ~99.9% probability positive, season-blocked interval vs the coin
flip excludes 50%. **These three numbers (52.50 / 51.09 / +1.35) belong to
`player`, not to the model now active.** The active model is `weak_stack`
(promoted MOD-07, commit `68b4dc0`), separately re-measured on the same
1,537-game protocol
(`artifacts/opener_evaluation/20260818T013115Z/metadata.json`): **52.83%
against openers**, season-blocked **[50.98%, 54.83%]** — a further **+0.33
points over `player`'s 52.50%** opener accuracy. `weak_stack`'s own close
accuracy on these games is **51.56%**, not `player`'s 51.09%; the two
models' numbers are not interchangeable. **Instrument note, 2026-08-19:**
the opener evaluation historically graded with the sign rule
(`residual > 0`), but production plays the probability rule
(`home_cover_probability >= 0.5`, `pool.py`); re-graded under the rule
production actually plays, the same model on the same 1,537 games scores
**53.36% against openers** — measured twice independently this session
(error-analysis reproduction and a fresh `opener-evaluation` run,
`docs/opener_evaluation.md` 2026-08-19 addendum). The 52.83% remains the
predeclared protocol figure; the evaluator now reports both rules, and by
owner decision (2026-08-19) the public site's headline leads with the
production-rule grade (53.4%, protocol figure retained as provenance;
all six seasons above the coin flip under that rule, season-blocked
[51.97%, 54.56%]). Closing lines, CLV, and vig are secondary.
The ceiling analysis, gap accounting (52.5% → ~57% theoretical; 54–55%
practical), and the prioritized build queue live in
`docs/pool_edge_plan.md` — start there when resuming this work.

## Evidence gates

Every research addition must clear these gates:

1. **Prediction integrity:** independently recompute and certify every card's
   schema, probability bounds, market math, decision policy, and training cutoff.
2. **Point-in-time validity:** prove when the value was available.
3. **Data sufficiency:** count independent games/weeks and source regimes, not
   merely player rows or plays; high-dimensional work needs enough repeated
   player/opponent observations to survive an outer-season test.
4. **Baseline improvement:** compare against market-only, Elo, and current full
   models using identical test weeks.
5. **Stability:** report season-by-season effects and uncertainty, not one
   pooled number.
6. **Prospective confirmation:** freeze the method before evaluating new games.

### 2026-09-03 This Week lineup and impact view

**In progress:** the This Week game deep dive now has a source-aware projected
lineup panel. The static renderer reads an ignored `lineups.json` artifact built
from the current depth-chart snapshot; it exposes starter identity, source time,
injury-feed status, and QB-family model impact without changing the forecast
artifact. The panel is organized by unit — offense, defense, and special teams
in labeled sections with per-reader visibility toggles — and covers the full
latest snapshot including backups, not just starters. A depth-chart/model QB
mismatch fails closed: the renderer refuses to publish the panel beside a
forecast whose projected QB is absent from that snapshot, and the lineup-model
sync guard skips only prediction rows with no lineup entry (historical rows).
Current injury status and non-QB play probabilities remain unavailable until a
current injury source is attached; the UI does not infer them.

**Daily lineup-synced regeneration:** the forecast now regenerates from fresh
lineup information at least once a day. Seven scheduled `lineups_<day>` jobs run
Monday through Sunday at noon Eastern via `scripts/refresh_lineup_forecast.py`,
which snapshots the current depth chart, rebuilds the player features and weekly
forecast from that snapshot (`--refresh-player-data`, reusing snapshots already
on disk), then rebuilds the lineup artifact linked to the new forecast and
republishes the board. The player-feature builders consume the latest local
depth-chart snapshot by default (`--depth-root` override) and record its ID in
feature-table provenance; only depth rows observed before each game's decision
timestamp can move that game's projected QB. The Tuesday lock
(`weekly-run --record-decisions` on 2026-09-08) remains the binding played card;
daily refreshes write no prospective ledger rows.

## Status legend

- ✅ Done and verified
- 🚧 In progress in the current development slice
- ⬜ Ready or planned
- 🔬 Research question; build only after prerequisites
- 🌙 Moonshot

### Backlog accounting

Run `.\.tools\uv.exe run --no-sync python scripts\roadmap_inventory.py` from
PowerShell for the live count. The inventory deliberately reports two numbers:
`remaining` includes every non-done row, while `active/planned` includes only
🚧 and ⬜ rows. Research questions, moonshots, and declined/blocked work remain
visible but are not misrepresented as the immediately executable queue. Use
`--json` for machine-readable totals and per-phase counts.

## Phase 13 — general engineering improvements

These are cross-cutting improvements identified during a project review on
2026-09-04. They are planning items, not completed evidence or model findings.
Prioritize operational integrity and evidence usability before broadening the
feature search.

| ID | Status | Item | Definition of done |
|---|---|---|---|


ENG-22 through ENG-35 were added 2026-09-04 from gaps the ENG-01..ENG-17 work
measured while landing; ENG-35 is time-bound to the Week 1 lock, and ENG-22/23/24
close the lineage follow-ups so that the safety check's lineage rows name real
snapshots. The recommended sequence is ENG-02 and ENG-01 first, followed by ENG-03,
ENG-14, ENG-15, ENG-16, ENG-04, and ENG-06. ENG-13 and ENG-17 should then
protect replay and baseline comparability; ENG-18 through ENG-21 are follow-on
operational polish. This ordering is an engineering judgment; it does not
rank or reject any research signal based on an interval crossing zero.

### 2026-09-04 Phase 13 execution wave

Twenty Phase 13 rows moved to ✅ and one to 🚧 in a single session run by
sonnet/opus subagents (about three at a time) with the coordinator re-running
every item's own test file before flipping its status. All counts below are
**measured** by the coordinator unless labelled *reported* (agent claim, not
independently re-run). Nothing was committed by the agents; the real Week 1
lock remains 2026-09-08 and no genuine ledger row was created.

- **ENG-01** `weekly-run --record-decisions` now writes an immutable package
  under `artifacts/lockday_packages/<season>_wk<week>_<stamp>/` linking git
  revision, model identity, feature-table and source-manifest digests, the
  forecast and card, every recorder result, ledger before/after counts, and
  `lockday_verify` output; `--no-package` opts out; the write is fail-safe at
  three levels so a lock already appended can never roll back.
  `scripts/lockday_package_verify.py` recomputes every digest. 23 tests;
  static rehearsal audit 0 errors. The `--full-replay` path is verified
  statically only (see ENG-35).
- **ENG-02** `nfl-ats preflight [--json] [--strict]` categorises checks into
  environment / configuration / research_data and exits non-zero only on the
  first two; 36 tests; live run on this machine 24 of 24 ok. Its CLI
  registration was lost to a concurrent `git stash` and restored from
  `stash@{0}` by the coordinator.
- **ENG-03** heartbeat file, per-job health, per-source freshness
  (`src/nfl_ats/capture_freshness.py`) and `capture_scheduler.py --health
  [--json]`, plus `lockday_verify.py --with-capture-health`; 36 new tests.
  The live daemon predated the heartbeat code and was restarted by the
  coordinator at the end of the wave.
- **ENG-04** (🚧) four lineup intents answered from `lineups.json` with
  source-time anchors and the fail-closed consistency refusal; 20 tests;
  live query works against the production artifact. The inline-JS chat port
  does not yet carry the intents (ENG-25).
- **ENG-05** 97-question golden corpus, `assistant_eval.py`, 108 tests at
  100% per category against the synthetic fixture; one accessibility gap
  fixed (`<noscript>` now says JavaScript is required). Surfaced ENG-36.
- **ENG-06** settled-week scorecard for the active model and every
  challenger; every row's classification is `unresolved_below_power` and the
  report never emits a terminal verdict; 12 tests. *Reported*: the 2025 run
  shows zero rows for all 35 entrants because no production ledger exists yet.
- **ENG-07** read-only registry explorer with five views; 8 tests; registry
  files byte-identical before and after. *Reported* findings: the next-shots
  list is dominated by reliability and oracle-control rows; roughly 29
  rotation families exist against 350+ weak-signal families (ENG-27).
- **ENG-08** five refresh-trigger detectors validated against
  min(kickoff, Sunday 16:00 ET); idempotent JSONL evidence log; scheduler
  job `refresh_trigger_log_sun`; 19 tests. *Reported*: the live scan
  reconstructed 32 deadline-valid injury-report triggers for 2026 Week 1.
- **ENG-09** `artifact_contracts.py` stamps feature tables, forecasts and
  cards with schema/builder versions and refuses a version mismatch before
  fitting or publishing; legacy unstamped artifacts warn only; 29 tests;
  live check on today's artifacts is compatible with two legacy warnings.
- **ENG-10** `cli.py` shrank from 6,556 lines to 87; commands live in twelve
  domain modules under `src/nfl_ats/cli_commands/` behind an ordered
  `REGISTRARS` tuple, shared helpers in `cli_common.py`, and the four public
  workflows are split into parse, orchestrate and write steps. A normalised
  argparse snapshot taken before any edit is identical after (66 commands),
  root and workflow `--help` output is byte-identical, and
  `tests/test_cli_contract.py` (20 tests) pins the tracked
  `tests/fixtures/cli_contract.json` against future drift.
  `scripts/lockday_contract.py` needed a 12-line update because it had
  hard-coded `cli.py` as the file holding the publish result map.
- **ENG-11** `scripts/verify_fast.py` / `verify_full.py`; `full` marker on
  31 tests; *reported* full suite 85 s wall, fast tier 78 s, so the saving is
  modest and the four AGENTS.md gates stay the release gate.
- **ENG-12** `card_explanation.py` builds a fixed-shape per-pick explanation
  (market line, this-game model probability, fired overlays, per-source
  freshness, Tuesday-to-refresh status) with a language contract that rejects
  causal or profitability phrasing; `publishing.py` always writes
  `explanations.json` beside the forecast and can add a card line (default
  off); 32 tests. *Reported*: live Week 1 explanations render for both a
  no-overlay pick and the coach-fade-flipped BAL at IND pick.
- **ENG-13** `run_replay.py` verifies digests, environment drift and git
  revision, then recomputes a forecast into a temp root; 12 tests.
  *Reported*: the live Week 1 forecast reproduced all 264 prediction columns
  exactly, while `uv.lock` had drifted since the artifact was written.
- **ENG-14** ten sources with budgets derived from `SCHEDULE`; card and
  metadata carry complete / degraded / blocked per source; only sources that
  already fail closed can block; 59 tests.
- **ENG-15** ledger reconciler with six classifications and a report-only
  recovery plan; 26 tests; idempotency pinned by test.
- **ENG-16** `lineage.json` beside every forecast; four fail-closed lineage
  checks added to the safety contract; 20 tests. *Reported*: 9 of 15
  decision-bearing fields on the live card declare an unrecorded source
  (ENG-22, ENG-23, ENG-24).
- **ENG-17** frozen 60-game parity fixture; all four comparison paths grade
  identical populations, cutoffs and pushes; 20 tests. *Reported*: real-data
  check 1,537 opener games versus 1,552 market-only, symmetric difference
  131, explained by the snapshot-pair requirement (ENG-28).
- **ENG-18** Tuesday-lock versus refresh diff with explicit
  changed / unchanged / no_data cells; 10 tests. *Reported*: the live Week 1
  run finds 18 forecast-artifact passes that are probably development reruns.
- **ENG-19** retention classes (`evidence`, `point_in_time_capture`,
  `scratch`, `reproducible`) and a dry-run `--budget-check`; point-in-time
  captures are now hard-excluded from any plan; 63 tests; no delete path.
- **ENG-20** `registry/research_queue.json` and generated
  `docs/research_queue.md` with `--check`; 35 tests; 64 rows. *Reported*:
  both grade pools show zero globally unspent default blocks, so every fresh
  Phase 12 lead resolves to a reuse-with-discount action.
- **ENG-21** `environment_report()` wired into the shared provenance writer
  and the lock-day manifest; secrets redacted to presence booleans; 28 tests.

**Second wave, same day (ENG-22 onward).**

- **ENG-22** `feature_manifest.py` propagates the base nflverse snapshot id
  transitively through every enrichment manifest and `lineage.py` prefers it
  over the table digest; 11 tests. Takes effect on the next feature rebuild
  (sequence in `docs/feature_lineage.md`); simulated in-process, all nine
  digest-fallback fields on the live card resolve to snapshot
  `20260824T115346Z`.
- **ENG-23** `market_observation.attach_market_observed_at` joins the Tuesday
  opener capture's `observed_at_utc` into the forecast frame, the player
  enrichment fills `*_injury_observed_at` from the snapshot capture instant
  when no team revision is visible, and lineage prefers both; 12 new tests,
  411 in the related selection. Dormant until the next feature rebuild and
  `margin-predict`; an in-memory rebuild with identical raw snapshots shows
  both fields populated (reported).
- **ENG-28** the parity suite gained `active_model_production` and
  `overlay_production` paths that call the real opener evaluation and the
  four-overlay composition against a commit-safe miniature opener store;
  35 parity tests. Real data: market-only 1,552 games, production 1,479,
  symmetric difference 73 entirely on the market-only side, explained by
  the opener-plus-close snapshot-pair requirement (reported).
- **ENG-24** `publish_active_predictions` now writes the played card's own
  `lineage.json` beside the card, extending the forecast lineage with one
  record per fired overlay and up to three tiebreaker records, validated
  fail-closed before the card is written; 9 new tests, 99 in the related
  selection. Live Week 1 read-only: coach fade fired on BAL at IND and DAL at
  NYG, spread-gap-zone fade on CLE at JAX, tiebreaker DEN at KC resolved from
  data alone; decision-bearing fields 15 to 20 (reported). The root-level
  generated file is now gitignored.
- **ENG-25** the four lineup intents are ported to the inline-JS chat panel
  and an in-repo Node parity harness (`tests/parity/assistant_parity.mjs`,
  `tests/test_assistant_js_parity.py`) compares Python and JS answers on 98
  questions with 0 mismatches (Node 22 present; the test skips, never fails,
  without Node). 130 assistant tests pass (measured). ENG-04 therefore moves
  to ✅.
- **ENG-26** heartbeat carries the scheduler's code and schedule digests,
  `--health` flags a stale daemon, the start script refuses a double start,
  and `--acknowledge-missed` records a reason without deleting the row; 81
  scheduler tests. The daemon was restarted on the new code and `--health`
  exits 0 (measured).
- **ENG-27** `rotation.validate_registry` (four checks) is wired as
  `nfl-ats rotation validate` and as a warning on every registry save;
  `nfl-ats rotation declare-coverage --apply` grew the rotation registry from
  30 to 395 families (365 `declared_for_coverage` stubs with no windows, 15
  `no_rotation_needed` records for controls and oracles). Measured by the
  coordinator: all 30 pre-existing families, `season_usage`, `notes` and
  `version` are structurally identical to HEAD and `weak_signals.json` is
  untouched; 75 rotation tests pass. The one live validator error is
  `pbp_drive_bundle [2013, 2017]`; the ticket's named example
  `fluview_elevated_on_production` is a stratified two-leg window by design
  and does not trigger it (read). Owner decisions are queued as ENG-37.
- **ENG-29** `script_contracts.scan_script` (AST only, never imports) verifies
  an in-file `READ_ONLY_SCRIPT = True` declaration against every write site;
  36 scripts now carry the declaration, two artifact writers use the new
  `provenance.write_stamped_artifact` (stamps provenance without a registry
  row), and the old 88-entry allowlist is gone. 50 genuine writers remain in
  a reasoned `_NON_EXPERIMENT_WRITER_SCRIPTS` list (ENG-38); 22 tests pass
  (measured).
- **ENG-30** a `private_raw_root` conftest fixture rooted at the OS temp dir
  replaces `tmp_path` in ten test cases so the private-raw-data guard and the
  git-provenance tests no longer depend on where `--basetemp` points; the
  guard itself is unchanged and a new test pins its in-repo refusal. The
  four affected files pass under an in-repo basetemp (measured). Observed
  but out of scope: pytest's default `pytest-of-<user>` directory can throw
  `PermissionError` under concurrent runs; prefer the PID-keyed basetemp in
  the verify scripts (reported).
- **ENG-31** `docs/concurrent_sessions.md` states the working-tree rules, and
  a machine-local `PreToolUse` hook (`.claude/hooks/guard_git.py`, registered
  in the gitignored `.claude/settings.json`) denies stash, checkout of paths,
  restore, reset --hard, clean -f and forced worktree removal issued through
  the Bash or PowerShell tools; 17 pattern cases and the stdin deny/allow paths
  are measured, and the hook fired for real on its first day (on a heredoc
  that merely mentioned the commands, which led to anchoring the pattern to
  command position). `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` is now 3 in the
  local settings, down from 40.
- **ENG-32** the PFR transactions capture was stale by construction: the
  ingest script resumed the newest snapshot directory and skipped every
  cached year, so the capture instant (the directory name) could never
  advance. A `--fresh-snapshot` flag now writes a new dated snapshot each
  run, copying past years forward with no network cost and refetching only
  the current year; the scheduler jobs pass it. One sanctioned run landed
  `20260904T215655Z` (+319 URLs for 2026) and `--health` reports the source
  fresh (measured). Policy: `pfr_transactions` is yellow with acquisition
  allowed; the yearly sitemap chunk makes the refetch the complete backfill
  (reported).
- **ENG-33** each scorecard row now carries an advisory
  `closing_ground_candidate` (wrong sign resolved, no split-half
  reliability, or none, with evidence) and a `next_admissible_action` from
  the ENG-20 vocabulary, while `classification` stays
  `unresolved_below_power` unconditionally; 82 tests pass and both registry
  files are byte-identical across a live run (measured).
- **ENG-36** the assistant's glossary matcher now scans normalised n-grams
  longest-first in both the Python router and the inline-JS mirror, so
  multi-word terms (and the previously broken "against the spread" alias)
  route; the golden corpus grew to 103 rows and every glossary entry is now
  required to be reachable. 168 assistant tests pass with 0 Python/JS
  mismatches (measured).
- **ENG-34** a SOURCES panel on the This Week page shows each source's
  complete / degraded / blocked state, a worst-wins card state and a legend,
  with a matching assistant intent in Python and JS. The agent found that the
  block was never persisted, so `publish-predictions` now writes
  `source_policy.json` in the forecast directory and beside the card, the
  content model reads the file first, and the forecast's `metadata.json` is
  never rewritten; 115 tests in the related selection pass (measured). Until
  the next real publish the live panel honestly reads NOT RECORDED.
- **ENG-35** the first `--full-replay` FAILED (measured): the package
  writer's fail-safe caught a pandas `Timestamp` JSON error, so the real
  lock would have appended ledger rows and written no package, and the
  rehearsal report then crashed on the same value. Fixed with
  `nfl_ats.io.json_default` on the shared atomic writer and the report dump,
  with a test. The second replay passed: 26 recorders recorded, 0 missing,
  rehearsal package written read-only and verified by
  `lockday_package_verify.py` (measured).

Incident worth recording: one agent ran `git stash` / `git stash pop` in the
shared working tree, which reverted other agents' edits to `cli.py`,
`publishing.py`, `weekly.py`, `provenance.py`, `pyproject.toml` and 24 test
files. Everything was re-applied and re-verified; `stash@{0}` was left in
place as a superseded snapshot. ENG-31 exists because of this.

### 2026-09-04/05 overnight backlog run (autonomous, sonnet lanes, ~3 concurrent)

Nineteen lanes ran overnight under a Fable coordinator while the owner slept;
every experimental verdict flowed through `nfl-ats weak-signals record` /
`nfl-ats rotation record` (registry 693 -> 746 signals), and every number
below is **measured** by the coordinator re-running the lane's own tests and
reading the registry rows unless labelled *reported*. GitHub Actions were
disabled repo-wide on the owner's instruction (both workflow files are now
`workflow_dispatch` only); the Pages site therefore did not redeploy. Batches
landed as `8e0a5c1`, `ffa78d3`, and the wrap-up commit. Full lane reports live
in the session scratchpad, not in the repo.

- **On-production, opener-graded looks (all on the rotation-assigned
  [2020, 2021] block, 456 paired games / 35 weeks, shared positive control
  +44.298 pts P+ 1.000):** post-OT fatigue -0.219 P+ 0.384; MNF-road short
  week 0.000 P+ 0.349; home-Thursday -0.439 P+ 0.182; new stadium 0.000
  P+ 0.348; dome shootout 0.000 P+ 0.438; low-total division home dog +0.439
  P+ 0.690; September heat -0.877 P+ 0.084; opener-softness fade -0.877
  P+ 0.182; ML-spread divergence -0.658 P+ 0.278; road-favourite-7+ fade
  **resolved against** [-2.863, -0.219] P+ 0.008 (`closed_negative`,
  `wrong_sign_resolved`); division dog / Week-1 dog / ATS-streak unresolved;
  opening-drive EPA +0.219 P+ 0.539; Q3 point differential -0.219 P+ 0.249;
  fourth-down aggression x favourite -1.535 P+ 0.212; rookie-QB debut fade
  0.000 P+ 0.396; **QB revenge +0.658 [-0.228, +1.948] P+ 0.834**; holdout
  slow start 0.000 (4 flagged games league-wide, none in window); **trade-
  deadline integration drag +0.658 [-0.220, +1.595] P+ 0.883** (70 flagged
  games, 25 acquisitions); suspension-return rust +0.219 P+ 0.607;
  rookie-crew underdog -1.097 [-2.691, +0.222] P+ 0.046 **on the 2020-2021
  window only, for the SHIPPED feed-only build (2015-2025 officials feed,
  season floor 2016), against production** — superseded as the family's
  headline, see the rookie-crew row below; crew second-meeting favourite
  -0.658 P+ 0.325. All `unresolved_below_power` except the one admissible
  closure named.
- **Rookie-crew underdog, reconciled (2026-09-09,
  `docs/rookie_crew_reconciliation.md`, family
  `rookie_crew_reconciliation`).** Two reads of this rule disagreed in sign
  and the disagreement was mechanical, not empirical: the -1.097 above is the
  shipped feed-only flag vs production on 456 games, while the officials
  battery's **+0.73 P+ 0.97** is an archive-vs-feed DELTA against the rule's
  own previous build, not a vs-production effect. Re-measured through the
  served opener harness on `opener_evaluation/20260909T183120Z` (replay gate
  passed, 1,537 games, gap 3.8e-15, 0 pick disagreements), 2020-2025, 1,503
  non-push, week-blocked 20,000 draws seed 20260821: the reconciled rule
  (archive-extended referee table, season floor 2010, close-proxy line before
  2020) is **+0.200 [-0.198, +0.602] P+ 0.843 standalone** and **+0.133
  [-0.198, +0.467] P+ 0.790 as a marginal on the played three-member card**;
  the same shipped feed-only flag under the FIXED probability wiring is
  -0.599 P+ 0.049 (2020-2025) and -1.316 P+ 0.040 (2020-2021), so the
  2026-09-08 ECDF defect is NOT what moved the sign — the rule change is.
  Era magnitudes on the card: 2020-2021 **+0.439 P+ 0.936**, 2022-2023 0.000
  P+ 0.503, 2024-2025 0.000 P+ 0.492. Crew trait season-to-season split-half
  reliability **+0.0756 [-0.0201, +0.1453] P+ 0.951** (255 referee-season
  pairs), so `no_split_half_reliability` is not available to close this
  family. All twelve cells `unresolved_below_power`. EV-positive on the
  played card and NOT yet served: crew assignments publish Wednesday, so the
  vehicle is the late-week refresh path (`nfl_ats.crew_tilt_refresh_overlay`),
  not the Tuesday lock.
- **CFB free screens on the XLG-03 benchmark:** post-bye home pooled -0.022
  P+ 0.428, era 2012-2019 +0.262 P+ 0.915, era 2021-2025 -0.446
  [-0.763, -0.166] `refuted_mechanism` for that era slice; rivalry home dog
  +0.011 P+ 0.555; altitude-cold home -0.090 P+ 0.230; sandwich fade blocked
  (no local AP poll).
- **Close-graded full-population screens:** twelve public-handicapper claims
  (LEAD-57; strongest leans road-favourite fade P+ 0.974, Week-1 dog 0.922,
  ATS-streak regress 0.919, division dog 0.912, all of which then went to
  production above); movement day-parts on the 2023-2025 hourly archive
  (Wed follow -0.13 P+ 0.41, Wed->Sat +1.50 P+ 0.93, Sat->Sun-AM +1.09
  P+ 0.91, Sunday-vs-Wednesday per-point +0.094 P+ 0.928).
- **Reliability and quality infrastructure:** snap-weighted career-age curves
  on the `years_exp` axis (LEAD-58, no birth dates exist locally); the
  first/second-half spread archive from the cached VegasInsider movement
  pages (LEAD-60: 1,485 usable 1H spreads, 805 2H, spreads only); PBP
  coaching-trait reliabilities (Spearman-Brown 0.30-0.48, all P+ 1.0);
  rookie-wall stage 1 (WR late-season delta leans the wall direction P+ 0.85;
  dependence metric within-season r 0.988, season-to-season 0.129);
  officials home-bias trait reliability P+ 0.325 (no stage 2 run);
  confidence-point allocation simulation (calibrated-edge ordering resolved
  WORSE than flat on P(first) at fields of 100 and 500; flat kept);
  injury-report hygiene (concussion DNPs sit 99.1% at every position;
  personal-matter DNPs play 53.3% vs 7.9% for injuries -> keep with a
  separate flag).
- **Production defect (ENG-39):** nflverse's 2025 injuries release carries no
  `date_modified`, so the production feature table's injury block is exactly
  zero for every 2025 and 2026 row and the live Week 1 card's injury
  component is a silent zero. An opt-in leakage-safe fallback and an
  always-on `injury_feature_presence` safety check landed; the rebuild and
  re-activation before the 2026-09-08 lock is the owner's decision.
- **Public site:** the findings page's "watching" ranking now excludes
  reliability (correlation-unit) and control rows, which had displaced every
  ATS lead.
- **Incident:** the Claude Code process died at ~02:00 ET when a lane's
  full-suite `pytest -n auto` exhausted the Windows paging file (exit 137,
  measured by that lane); three lanes were resumed from their transcripts
  with no lost work. Fleet pytest runs should cap workers (`-n 2`).

### 2026-09-02 inactives Section 5 result

**Measured** by `scripts/inactives_channel_historical_screen.py` and recorded
through both registries: the opener-graded `[2020, 2021]` historical proxy
screen covered 429 paired games across 33 weeks and measured −1.3986 accuracy
points versus the Tuesday card, week-blocked 95% [−3.1963, +0.2427],
`probability_positive=0.0418`. The realised-margin oracle control measured
+45.9207 points, week-blocked 95% [+40.0911, +51.9139], P+ 1.000. The result
is `unresolved_below_power` (weak-signal registry) / `unresolved` (rotation),
not a closure or promotion; the primary interval and control do not establish
an admissible terminal verdict. Artifact:
`artifacts/inactives_channel_historical_proxy/20260902T155950Z/`. The live
2026 prospective inactives challenger remains active and the played card is
unchanged.

### 2026-09-02 four on-production opener confirmations

**Measured** in the four named `artifacts/*_opener_confirmation/20260902T*/results.json` files: every family was predeclared, assigned by the rotation CLI to 2020-2021, evaluated on 456 paired non-push games across 35 weeks, and retained prediction-level pairs. The primary probability-rule opener deltas versus production were Reddit home comment ratio **+0.439 accuracy points, `probability_positive=0.665`**; pace mismatch **-0.219, P+=0.286**; away active illness **-0.439, P+=0.312**; and red-zone third-down fade **-0.658, P+=0.111**. Each frozen-pick null used 200 within-week permutations and each realized-margin positive control measured **+43.860 points, P+ 1.000**. All four are recorded in both registries as `unresolved_below_power` / `unresolved`; no terminal conclusion, production-card change, or publication follows from this one assigned block.

## Phase 0 — trustworthy foundation

| ID | Status | Item | Definition of done |
|---|---|---|---|

## Phase 1 — research workbench

| ID | Status | Item | Definition of done |
|---|---|---|---|

## Phase 2 — point-in-time market data

| ID | Status | Item | Definition of done |
|---|---|---|---|
| MKT-08 | 🚧 | Timing policy | Compare fixed weekly timestamps and news-triggered updates. **2026-09-02 audit foundation** (`docs/timing_policy_audit.md`; its `scripts/timing_policy_audit.py` was removed in the 2026-09-10 repository cut): 11 clock-dispatched decision jobs, zero prospective refresh-ledger rows, no comparison to score. **2026-09-03 trigger schema landed:** the pick-revision ledger carries `trigger_type`, `trigger_source` and `trigger_observed_at_utc`. **2026-09-11 scored on the archive** (`docs/refresh_timing_policy.md`, `scripts/refresh_timing_policy.py`, `artifacts/refresh_timing_policy/20260911T000000Z/`): the served late-week leader-median follow rule held fixed, only the refresh instants varied, opener-graded forced picks on 1,503 scored games 2020-2025, week-blocked bootstrap 20,000 samples seed 20260911. Capture cadence measured per season: 2020-2022 is 7 NFL snapshots a week at fixed labels with **no Wednesday and no Friday capture**, 2023-2025 adds the hourly backfill (136/week, Tue 00:00 ET to Sun ~10:55 ET) — in every season exactly **one** capture per week falls between 11:00 and 16:00 ET on Sunday (12:25). **The one change that should be played: a per-game last-call refresh at each game's own deadline beats the twelve scheduled passes by +0.1996 accuracy points, week-blocked 95% [+0.0000, +0.4620], `probability_positive` 0.9756 pooled** (0.5000 / 0.9381 / 0.8175 per window; 3 picks differ in six seasons, all three in its favour) at **3.6 refreshes a week instead of 12**. Scheduler holes it needs closed, measured from the gap between a game's last scheduled pass and its deadline (median 1.08 h overall): Wednesday-afternoon games have **no pass at all** before kickoff (both Wednesday jobs fire at 18:15/19:15 ET), Friday games 19.75-25.0 h, Sunday 09:30 ET London games 14.25 h, Saturday 13:00 games 2.5 h — 42 of 1,537 games. Against the Tuesday card the whole refresh channel is era-split: `fixed_passes` reads **-3.7281 [-6.9264, -0.6508] P+ 0.0101** on 2020-2021, **-1.3619 [-4.1584, +1.5595] P+ 0.1757** on 2022-2023, **+2.2514 [-0.5758, +5.2142] P+ 0.9336** on 2024-2025 and -0.7984 [-2.5555, +0.9927] P+ 0.1917 pooled; on the picks it changes the refreshed side goes 36.9% / 45.2% / 58.8% by window. News-triggered refreshing is **not** adopted: 0.5-point trigger -1.3133 [-2.8143, +0.0000] P+ 0.0357 against the fixed schedule on 2024-2025 and 9.9 runs a week there; the 1.0-point trigger is 4.8 runs a week and P+ 0.6381 pooled, kept only as a cheap fallback. Decay curve (same rule at H hours before each deadline) is **not monotone and not flat — its sign is era-dependent**: 2024-2025 climbs +0.38 (24 h) → +2.06 (12 h) → +2.44 (deadline) with P+ 0.61 → 0.92 → 0.94 and no plateau inside the last 12 hours, while 2020-2021 runs the other way to -3.73. Removing the rule's internal Sunday blackout reads +0.4657 P+ 0.7218 pooled and +1.5564 P+ 0.9243 on 2022-2023 — tracked as a challenger, rule unchanged. Split-half reliability of the leader-book net move (bovada vs williamhill_us+mybookieag) is 0.885 raw / **0.939 Spearman-Brown** on 1,434 games, so `no_split_half_reliability` is inadmissible; no positive control was run, so `bounded_by_control` is inadmissible; the sign differs across eras so no wrong sign is resolved for the family. **Nothing closed**: 57 cells recorded `unresolved_below_power` under weak-signal family `refresh_timing_policy` (category `market`, units `accuracy_points`), and rotation family `refresh_timing_policy` spent [2020,2021], [2022,2023] and [2024,2025] with verdict `unresolved` and a null closing ground on all three. The served schedule was **not** changed by this lane. Still open: the model-recompute half of the refresh (point-in-time mid-week feature tables do not exist for 2020-2025) and a real news-triggered arm from prospective ledger rows. **Same night, scheduler:** the four holes were closed with `refresh_last_call_{wed_1215,fri_1330,sat_1215,sun_0830}` (ordinary `refresh-picks --record-decisions` passes ahead of Wednesday-afternoon, Friday, Saturday 13:00 and London 09:30 kickoffs), each exercised once with `--run-job --dry` (MANUAL-DRY-RUN OK ×4) and the daemon restarted on 172 jobs. **2026-09-11, the Sunday-blackout challenger wired (not served):** `late_week_follow_no_sunday_blackout` (`src/nfl_ats/late_week_follow_no_sunday_blackout_overlay.py`) shares the served late-week follow's own live intraday odds read at zero extra capture-window cost, recomputes the leader-median net move over the same three leading books at the same `leader_follow_threshold` gate, only without excluding Sunday-morning quotes, and records both arms on every `refresh-picks` pass in `prospective/late_week_follow_no_sunday_blackout_decisions.parquet`. Registered `ACTIVE_PROSPECTIVE` in `artifacts/prospective/challengers.json` with the doc's own evidence (+3.00 pts P+ 0.9789 at H=3 on 2024-2025, +0.4657 pooled P+ 0.7218 2020-2025, reliability 0.885-0.939); display name "Follow the line on Sunday too" added to `CHALLENGER_DISPLAY_NAMES`. Confirmed on the live 2026 Week 1 card (`refresh-picks --note lane_rehearsal`, no recording flags, Thursday 2026-09-10, before any Sunday quote exists): 14 eligible games, the Sunday-inclusive follow fires on the same 2 games the served rule already follows (ATL_PIT, WAS_PHI, net move +1.0 in both), and the challenger's pick differs from the served card on 1 of 14 games (DAL_NYG) — that one difference is model-recompute drift versus Tuesday's pick on a game the follow rule never touches, not a Sunday-movement effect, since no Sunday data exists yet this week. The served rule and card are unchanged. |

> **2026-08-20 later update to MKT-11:** the resumable Sagarin Era B
> cache advanced from 37 to 242 nonempty pages across eight season keys
> (1998-2005). All 242 cached pages parse, with zero parser exceptions,
> unmapped teams, or zero-byte files. The bounded run stopped before final
> consolidation, so the manifest and Parquet views remain at their prior Era A
> checkpoint and no network-failure count is inferred; rerunning the same
> documented command resumes from these cached pages.

> **2026-08-20 final Era B checkpoint:** the same snapshot is now consolidated
> across all 1998-2011 target keys: 585 durable pages parse cleanly into 18,473
> rating rows and 9,848 Tuesday as-of rows, with zero parser exceptions or
> unmapped teams. A final bounded retry recovered two transient gaps; seven
> Wayback fetches remain documented failures. This completes the usable
> historical source checkpoint. The earlier ATS screen was deliberately not
> rerun, so these additional rows change source coverage, not the current
> 53.4% opener-grade production decision.

> **2026-08-21 complete-source MKT-11 replication:** the frozen seven-cell
> Sagarin battery was rerun against all 585 parsed pages, with hashes for every
> consolidated input added to result provenance and a physical-column-projection
> leakage test. Usable close-grade coverage rose 2,684 -> 2,966 games (2010 and
> 2011 each now 240/256); the 2020-2025 opener population remained 1,053. The
> strongest close-only cell remains the top divergence decile (+3.535 points,
> `probability_positive=0.8908`, n=297), but its opener counterpart remains
> -0.943 points, P+=0.3928 (n=106), and the broad opener divergence cell remains
> -1.478 points, P+=0.2542 (n=406). All seven identities were replaced in place
> as `unresolved_below_power`. No production/prospective change: the complete
> source adds historical close evidence, not a better pool-grade decision.

## Cross-league evidence and transfer — highest research priority

College football supplies many more games and player transitions, but its rows
must not be appended to NFL rows as though both leagues were exchangeable. Its
value is to estimate shared football mechanisms, independently replicate
hypotheses, and construct priors that are subsequently judged on NFL-only outer
weeks.

| ID | Status | Item | Definition of done |
|---|---|---|---|

## Phase 3 — better football state

| ID | Status | Item | Definition of done |
|---|---|---|---|

## Phase 4 — players, coaches, injuries, and offseason priors

| ID | Status | Item | Definition of done |
|---|---|---|---|
| PER-03 | 🚧 | Injury report history | 76,784 canonical 2009–2024 rows are ingested with a 24-hour cutoff; weekly files behave as final observations, not revision streams. PFR/PFT news-wire evidence remains documented in `docs/pfr_transactions_sourcing.md`. **Measured 2026-09-02:** PFR precise-date scopes are fully cached at 4,361/4,361 rows for 2022–2025 and 9,080/9,080 for 2014–2021 (`scripts/pfr_bulk_date_fetch.py --max-fetches 0` for each season set); the old 831-row resumable note was stale. **Live replacement implemented but not exercised** (`docs/sportradar_injury_capture.md`): a policy-registered Sportradar v7 Weekly Injuries capture writes immutable raw/canonical files and SHA-256 provenance, requires current-week/slate coverage, rejects stale/incomplete/wrong-week responses, and exposes a hash-verified capture-time loader whose leakage regression excludes later revisions. Four Wed–Sat scheduler jobs are credential-gated; the RED NFL.com jobs remain paused. No `SPORTRADAR_API_KEY` is configured, so no real current response has passed the contract. **2026-09-03 double-pull (owner directive "pull both"):** the PFR transaction wire is now a live path — `pfr_transactions` posture registered (`config/source_policies.json`, polite 1s crawl, contact UA, private-only) with Wed/Sat 07:00 ET scheduler jobs running the resume-only ingest (never re-fetches cached slugs; pinned by a scheduler test). NFL.com stays paused: tracked RED policy forbids new fetches without express written consent, so "both" in practice means PFR-live plus Sportradar-if-keyed. Exact closure blocker: a Sportradar key (or written NFL.com consent) plus one current `status: complete` manifest; fixtures alone do not close it. |
| PER-07 | 🚧 | Coaching/coordinator changes | **Investigated 2026-08-17; the row's original framing was the wrong shape and is superseded.** Coach identity is priced -- split-half reliability of a coach's mean ATS residual is +0.063 (Spearman-Brown +0.119, 85 coaches), and coaches with an above-50% prior cover rate subsequently cover 49.5-51.0% at every sample threshold. Tenure buys nothing beyond year one (years 2-3 50.74%, 4-6 49.63%, 7+ 51.96% -- no monotonicity) and there is no variance story either (year-1 early-season residual sd 13.53 vs 13.22 for veterans). 4th-down aggressiveness is genuinely reliable and is **NOT closed** -- an earlier draft of this row called it "fully priced" and that was wrong, an underpowered interval read as a null (the RWB-16 error this project exists to avoid). Re-measured independently: expressed relative to each season's league norm (which strips the well-documented league-wide drift toward going for it, and halves the naive figure) year-over-year reliability is **+0.320** on 512 team-season pairs, go-rate-over-expected sd 0.0381. The market test regresses prior-season aggressiveness against the ATS residual on 4,175 games and returns, for a one-sd matchup, **-0.038 points, season-blocked 95% [-0.423, +0.417]** -- an interval 0.84 points wide. The hypothesis it was meant to reject, that the value is *completely* unpriced, is worth about **+0.174 points** and sits comfortably INSIDE that interval. The test therefore cannot distinguish priced from unpriced at this sample size. ~~Resolving 0.174 points at 95% would need roughly 24,000 games -- about 90 NFL seasons (CFB's 12,500 does not close that either).~~ **That framing is retracted**: this project is model/feature-limited, not data-limited (`docs/scaling_and_transfer.md`), and "needs N more games/seasons" is never the conclusion to draw from an underpowered interval. The decision this implies is the one AGENTS.md already sanctions for a category-3 result: pool it. So this can never be confirmed as a standalone candidate and must not be spent on a window; it is a **stacker/pooling input** (RWB-18, `registry/weak_signals.json`), not a dead end. Two honest discounts: the sd 0.123 EPA-points-per-game magnitude is a naive bin-EPA figure that selection almost certainly inflates, since coaches go for it when they expect to convert; and MOD-07 has already returned unresolved once. Interim changes are too thin (29 events; the 65.5% first-game figure is n=29 noise) and are **not provably pregame** -- historical snapshots are backfilled with whoever worked the game, so an interim feature needs an announcement-date source before it could pass a leakage test. **What survives is one binary flag:** a year-1 head coach fades in weeks 1-8, covering 46.72% on 762 games / 104 team-season clusters (cluster 95% [43.46, 50.06]), -1.30 points raw and -1.45 with a cubic quality control, both blocked intervals excluding zero. It is not a quality confound (equal-quality teams that KEPT their coach cover 50.70%) and not a QB proxy (new-QB coefficient is null in the 2x2). The active model does not capture it: it sides with the year-1 team 51.6% of the time and those teams covered 47.6%. Independently replicated on free CFB data at 3.5x the sample (year-1 all weeks 48.73% on 483 clusters; weeks 1-4 47.14%), with the same sign, magnitude and era boundary. **The catch that governs the decision:** the effect is null in 2009-2017 (49.00%, P=0.321) and lives in 2018-2025 (44.79%, P=0.011), i.e. inside the mined era. Worth ~+0.57 pp of full-slate pool accuracy. Do NOT ship on this measurement. **Correction 2026-08-18: the row previously recommended declaring `hc_year_one_fade` with `acknowledges_mined_2018_2025` and buying the answer with one opener window -- that is wrong and has been struck.** The registry entry's own notes say the opposite (`registry/weak_signals.json`): "Do NOT spend an opener window confirming this inside 2018-2025 -- that is its own discovery data. The only non-circular tests are prospective 2026+ and the CFB replication." The circularity is structural, not a style preference: the effect is null in 2009-2017 and lives entirely inside 2018-2025 (the mined era), and the opener grade's entire season pool is `GRADE_POOLS["opener"] = (2020, 2025)` (`src/nfl_ats/rotation.py`) -- every opener-graded block available to spend already sits inside 2018-2025, so no opener window can test this without re-confirming the effect on its own discovery data. **Owner decision 2026-08-18: hold the opener blocks; the honest test is prospective 2026 weeks 1-8, which begins 2026-09-08 at no window cost.** **2026-08-18: overlay implemented and ON** -- the clean-case fade is now a pick-level overlay wired into the published card path (`src/nfl_ats/coach_fade_overlay.py`, `OVERLAY_ENABLED = True`, weeks 1-8), disclosed on the card the same way Best-Pick ties are, and registered as challenger `hc_year_one_fade_overlay` so weeks 1-8 of 2026 record and score both arms prospectively at no window cost; see `docs/coach_fade_overlay.md`. Verified against the real Week 1 2026 card: exactly one flip (BAL -3.5 at IND becomes IND +3.5), MIA at LV correctly left untouched (both coaches year-1). Not yet published -- the card publishes for real at the 2026-09-08 Tuesday lock. **2026-08-20: the "interim changes are too thin / not provably pregame" line above is superseded** (`docs/interim_coach_screen.md`, `docs/archive/data_source_scout_v3.md` section 5). The "not provably pregame" objection is resolved by construction: this build's authoritative source is Pro Football Rumors' own interim-coach list, which carries real public FIRING-ANNOUNCEMENT dates (e.g. "Dec. 6, 2010"), not the backfilled `schedules.parquet` coach field the old note was worried about -- that field is used only as a join/cross-check, with a takeover-date fallback for the 10 of 39 team-seasons where it doesn't reflect the change at all. Sample is still small, honestly reported as such: 39 joinable interim stints (2000-2008 excluded, pre-dates this project's own 2009 data floor), 250 REG-season team-games, 2009-2025. The headline "any game under an interim" flag (`interim_hc_active`) is flat-to-slightly-negative -- 49.20% vs. 50.02% league baseline, effect -0.024 accuracy points, week-blocked 95% [-0.204, +0.165], P+ 0.386 -- a DIFFERENT and larger sample than the old n=29/65.5% figure, and it does not repeat that lean. But splitting by `interim_game_number` finds the old figure's shape in a sharper, cleaner cell: FIRST game only (n=39) covers **58.97%** vs. 49.96% for the rest of the league (`interim_hc_first_game`, effect +0.041 pts, P+ **0.845**), while games 2+ (n=211) actually cover BELOW the league baseline (47.39%) -- the folklore ("teams often cover their first game under an interim") has real support, it just doesn't extend to the rest of the stint the way a single aggregate flag would suggest. Both readings, plus a home/road split within interim games (P+ 0.81-0.95, no predeclared mechanism) and a fired-coach-was-year-1 split (P+ 0.56, essentially a coin flip), plus two era re-slices, are recorded `unresolved_below_power` in `registry/weak_signals.json` (7 entries) per AGENTS.md -- none crosses the refuted-mechanism bar, none is claimed as resolved. ~~Not wired into the published card; `docs/interim_coach_screen.md` section 7 sketches what a live `interim_hc_first_game` overlay would need (a weekly re-fetch of the same PFR list, effort S) without building it.~~ **Built later the same day (2026-08-20).** `interim_hc_first_game_tilt_overlay` is now a dual-tracked, no-window-cost prospective challenger (`src/nfl_ats/interim_hc_first_game_tilt_overlay.py`, fail-open on a stale/missing PFR snapshot; `artifacts/prospective/challengers.json`, `ACTIVE_PROSPECTIVE`) -- never applied to the published card, spends no rotation-registry window. Overlap with the live `hc_year_one_fade_overlay` is real but narrow (both overlays transform the base card independently; only 32 of 250 under-interim games fall in that overlay's weeks 1-8 window). **2026-09-02 coordinator-contract progress** (`src/nfl_ats/coordinator_changes.py`, `docs/coordinator_changes.md`): a source-independent point-in-time OC/DC builder now requires effective and observed timestamps plus source identity, emits nullable side/change-count state, and fails closed on post-decision, missing, ambiguous, or conflicting assignments. Leakage tests prove future historical corrections, future games, and result mutations cannot rewrite prior decision rows. PER-07 remains open because the repository still has no accepted historical OC/DC source; the adapter is not registered or wired, and no ATS experiment or registry/model decision ran. **2026-09-03 source probe:** one bounded fetch of a PFR team-season staff page returned HTTP 403 (bot-walled, no spend); nflverse carries no coordinator history (established in-repo). No further source candidate was exercised today. **2026-09-07 lane E (Codex; coordinator re-ran its tests):** a non-PFR, non-Wayback source works -- the historical REVISIONS of each team's Wikipedia staff template (`Template:<Team> staff`) fetched through the MediaWiki API at a per-season cutoff, so every assignment carries the real revision timestamp as its observation time (never a season-start proxy; transclusions are never expanded, which would leak today's staff into a historical page). Bounded live pull KC + GB 2019-2025 (14 requests, polite UA, 1 s delay, maxlag=5): 42/42 HC/OC/DC assignments dated, 28 OC/DC, **42/42 observed 11.8-167.0 days before the team's first regular-season kickoff** (`data/raw/coordinators/20260907T144526855711Z/coverage_audit.json`). `scripts/ingest_coordinator_history.py` (policy-gated `wikipedia_coordinator_revisions` + `wikidata_coordinator_claims` in config/source_policies.json, immutable timestamped snapshots, cached, request-budgeted), additive `coordinator_changes.build_coordinator_history_features` wiring that excludes HC/undated rows, 12 offline tests incl. the later-revision-cannot-rewrite-earlier-decision regression; docs/coordinator_history_source.md. Still open: full 2009-2025 league pull, in-season change enumeration, playcalling duties. No ATS experiment, registry or model change. **Lane M, league pull + one production look (Codex, 2026-09-07 evening; coordinator read docs/coordinator_change_on_production.md and measured the single registry entry `per07_coord_change_on_production` plus the spent 2020-2021 window):** Wikipedia staff-revision history pulled for all 544 team-seasons 2009-2025 (`data/raw/coordinators/20260907T213814366437Z`): HC 534, OC 512, DC 520 dated assignments, all 1,566 observed 8-241 days before that team's first regular-season kickoff, 481 complete trios; all 128 in-season 2022-2025 ranges enumerated (16 identity edits, ten independently supported coordinator changes, two playcaller changes). The predeclared one-look candidate `weak_stack_coord_change` (three home-minus-away season-start turnover flags on top of production) reads -0.333 accuracy points, 95% [-1.593, +0.927], P+ 0.289 on the full 1,537-game archive (1,503 non-push; 1,019 games covered, 518 baseline fallback; covered-only -0.50 P+ 0.289; assigned 2020-2021 window +0.66 [-0.88, +2.18] P+ 0.766; seasons 0.00/+1.27/-0.40/-2.26/-1.50/+1.12). Incumbent kept; `unresolved_below_power`; the 2020-2021 window spent as unresolved. Source is viable; still open: vacancy/removal state, exact move chronology, playcaller-role separation, and LEAD-29's first-game-under-a-new-playcaller lean now that dated OC changes exist. **2026-09-10:** `scripts/ingest_coordinator_history.py` restored from `b7ed31d^` (git history, no `coordinator_changes.py` resurrection -- it was never imported), a hardcoded 2025 season ceiling in three functions fixed to 2026, and a fresh league pull captured the missing 2026 preseason row (`data/raw/coordinators/20260911T033007437805Z`, 32 teams, 30 with all three roles dated, 32 new network requests in 1m10s with the 2009-2025 history replayed from cache) so `post_bye_new_playcaller_back_overlay`'s `oc_tenure_by_team_season` is no longer inert for 2026 (21 year-1/5 year-2/5 year-3+/1 unknown); see `docs/coaching_leads.md`. |
| PER-09 | 🔬 | Latent player ratings | Player-, unit- and lineup-level looks are all measured and recorded; the only component still to build is the special-teams season-lagged builder, gated on an open rotation window. First season-lagged offense/defense adjusted-plus/minus is reproducible but failed its matched ATS screen. **2026-09-03 special-teams reliability gate measured** (`docs/st_player_ratings.md`, predeclared; `scripts/st_player_ratings_screen.py`, `tests/test_st_player_ratings_screen.py`, artifact `artifacts/st_player_ratings/20260903T200725Z/`): 14,686 token-identified ST plays 2019–2024 (token-vs-play_type agreement 96.2%), odd/even-week split-half Pearson +0.279, Spearman-Brown +0.436 (n=996 at 50+ plays/half) — real but modest, recorded `st_player_rating_reliability` (`unresolved_below_power`, units `correlation`). Season-lagged ST builder + ATS look queued for open windows. **2026-09-03 unit-APM reliability gates measured** (`docs/unit_apm_ratings.md`, predeclared; `scripts/unit_apm_screen.py`, `tests/test_unit_apm_screen.py`, artifacts `artifacts/unit_apm/`): same scrimmage population, four separate Ridge fits — OFF_SKILL SB 0.394 (n=840), OFF_OL 0.325 (501), DEF_FRONT 0.236 (814), DEF_SECONDARY 0.245 (579); all four healthy, recorded `unit_apm_*_reliability` (`unresolved_below_power`, units `correlation`). Season-lagged unit builder + ATS look queued. **2026-09-03 hierarchy measured** (`docs/hierarchical_apm.md`, predeclared; `scripts/hierarchical_apm_screen.py`, `tests/test_hierarchical_apm_screen.py`, artifact `artifacts/hierarchical_apm/20260903T202953Z/`): unit-mean shrinkage (k=500) beats the flat APM on held-out EPA in all three seasons 2022–2024 (pooled MSE delta +0.00179, every CI excludes zero; leaky control decisive, harness valid). Small but consistent — adopted for the next builder. No registry entry (no honest MSE unit). PER-09's remainder is now only season-lagged builders + ATS looks, both gated on open windows. **2026-09-07 lane F (Codex; coordinator re-ran its 34 tests):** the season-lagged unit builder and its ATS look are done. `src/nfl_ats/apm_unit_feature.py` builds six prior-season-seeded, strictly-completed-before-decision walk-forward k=500 hierarchical APM team-unit columns (docs/apm_unit_feature_on_production.md, predeclared, SHA-pinned before scoring; 269,651 source plays 2016-2025); research profile `weak_stack_apm_unit` = weak_stack + those six. Opener-graded against active model 2ceaf63b56b7ce25 (baseline replay matched all 1,537 saved picks): **52.96% vs 53.36%, -0.399 accuracy points, week-blocked 95% [-1.860, +1.062], probability_positive 0.283**, 1,503 non-push, 100% coverage, 116 pick changes; windows 2020-21 -1.97 (P+ 0.046) / 2022-23 +0.19 (P+ 0.527) / 2024-25 +0.38 (P+ 0.603); per season -2.73/-1.27/0.00/+0.38/-1.88/+2.62. Decision: keep the incumbent (EV rule); mechanism not closed -- three `unresolved_below_power` records + `unresolved` looks, family `apm_unit_on_production` (inherits `participation_offense_defense_rapm`, `unit_apm_ratings`). Artifact artifacts/apm_unit_opener_eval/20260907T144504Z/. **2026-09-11 lane PER-09 (expected-lineup aggregation of the PLAYER-level ratings, the piece the 2017 screen never tested):** `docs/latent_ratings_on_production.md` (predeclared sections 1-7 written before scoring), `scripts/latent_ratings_on_production.py`, artifacts `artifacts/latent_ratings_on_production/20260911T000000Z/`. The season-lagged RAPM reproduces **bit-for-bit** (refit target seasons 2020-21, 5,508 rows, max abs difference 0.0). Split-half reliability (odd vs even seasons, raw coefficients, 500+ plays per half in both): **offense Pearson +0.174 / Spearman-Brown +0.296 (n=760), defense +0.154 / +0.268 (n=808)** -- nonzero, the lowest of PER-09's three constructs as expected for per-player coefficients, so `no_split_half_reliability` is unavailable. New feature: per team-game sum of (deadline-visible play probability from production's learned availability rates) x (strictly prior EWMA snap share, role_span 8) x (player rating) = the rated players EXPECTED TO PLAY, plus a full-strength counterfactual and their difference (`divergence`); 2,383 REG games 2017-2025, expected-lineup share sums 10.53 off / 10.39 def against 11 on-field units. Thresholds fixed on 768 pre-window 2017-2019 games (M1 0.056158, M2 0.012329, C 0.013312). Opener screen ON TOP of the served production pick (active model **d49194e04945a5e5**, 456 non-push games, 35 weeks, baseline **53.29%**), rotation window **[2020, 2021]** assigned by the CLI: **Screen A (expected-lineup rating tilt) 52.19%, -1.096 accuracy points, week-blocked 95% [-4.450, +2.298], probability_positive 0.256**, 81 picks changed, Brier improvement -0.0000609 (P+ 0.470), per season -2.27 / 0.00; **Screen B (lineup-divergence tilt) 52.85%, -0.439 points, 95% [-4.626, +3.254], P+ 0.435**, 86 picks changed, Brier improvement **+0.001555 (P+ 0.667)**, per season +4.55 / -5.08. Both beat their own information-free within-week permutation null (centres -1.726 / -1.986, because any tilt at this flip rate loses by arithmetic): observed at the **69th** and **83rd** percentile. Positive control (leaked opener margin) +11.623 points, 95% [+8.171, +15.066], P+ 1.000, 53 of 53 flips wrong-to-right -- detection proven at 11.6 points, not at the ~1-point scale in dispute, so `positive_control_bound` is unavailable too. Predeclared "star out" cell (max abs divergence >= C, 187 of 456 games) **did not separate**: inside -0.535 (P+ 0.453) vs outside -0.372 (P+ 0.431) for arm B. Mechanism diagnostic, measured before the ATS read: `diff_lineup_total` correlates **+0.608** with the opening home spread (the team-quality ceiling, now measured for this construct rather than assumed) while `diff_divergence` correlates only -0.206; both correlate positively with the realized opener ATS margin (+0.071, +0.019). Design property found after the fact and disclosed, not fixed (section 8.9): the 2017 leg of the pre-window threshold sample is under-warmed (one source season, hard reliability shrinkage; mean abs diff 0.0176 vs 0.0572 in 2019), so the "top quartile" thresholds actually select 44.3% of window games -- which penalises the bootstrap-vs-zero read but leaves the permutation null, which shares the thresholds and flip rate, unaffected; the next window should draw thresholds from 2019-onward seasons, declared in advance. Decision: keep the incumbent card (EV rule, P+ below 0.5 on both arms); nothing served, no card change. Nine `unresolved_below_power` records, family `latent_ratings_on_production` (inherits `participation_offense_defense_rapm`); window spent `unresolved`, 3 eligible windows remain. The availability-gap arm (Screen B) is the piece worth the family's next window. Remaining PER-09 component: the special-teams season-lagged builder. |
| PER-10 | 🚧 | Injury scenario mixture | **Distribution kernel complete; scenario producer blocked** (`docs/injury_scenario_mixture.md`, 2026-09-02). A production-isolated, revision-aware kernel now selects the latest complete joint lineup-scenario set visible by the decision cutoff, rejects missing/overlapping identity partitions or probabilities that do not sum to one, and mixes scenario-specific margin centers over the existing out-of-time residual distribution with exact mixture moments and cover/push/loss probabilities. Future revisions are leakage-tested. The row remains open: named-QB depth supplies marginal QB1-active probability and QB1/QB2 values, while the player pipeline supplies expectation-aggregated marginal injury burden; neither supplies joint multi-player lineup probabilities, and no accepted mapping converts named player EPA/value states into scenario-specific margin-point centers. Assuming independence or choosing that mapping would be a new modeling decision. **2026-09-03 dependence measured, independence rejected as a model** (owner directive: comprehensive, never the shortcut; `docs/absence_dependence.md` + `docs/absence_pairwise_dependence.md`, predeclared; 9 tests): full-unit sits never occur (degenerate, honestly reported, no verdict), while pairs sit together 20–27% LESS often than pooled independence implies (all CIs below 1.0) yet MORE often than label-shuffling implies (100th percentile of all nulls) — rotation dominates with residual positive coupling surviving. The comprehensive producer uses heterogeneous per-player marginals plus a small positive coupling term (observed/null ≈ 1.13–1.25); pooled independence overstates joint absence 25–40% and is out. The producer itself remains unbuilt point-in-time. No profile, played card, ATS experiment, registry, or active model changed. |

> **2026-09-02 PER-13 follow-through (supersedes the queued-Stage-2 text in
> the row above):** the close-graded on-production screen measured +0.536
> accuracy points, `probability_positive` 0.872 on its assigned [2011, 2013]
> block (`docs/per13_durability_stage2_on_production.md`). The required opener
> confirmation then ran on the rotation-assigned [2020, 2021] block
> (`docs/per13_durability_opener_confirmation.md`): candidate 52.85% versus
> production 53.73%, paired -0.877 points, week-blocked 95% [-2.386, +0.446],
> `probability_positive` 0.084 over 456 games; positive control +43.86 points,
> P+ 1.000. The opener decision read favours leaving production unchanged.
> Both record commands classify the result `unresolved_below_power`; no
> admissible closing ground applies, and no further PER-13 window should be
> spent merely to chase the sign.

> **2026-08-20 later update to PER-03:** the targeted PFR precise-date
> backfill is complete (`docs/pfr_transactions_sourcing.md`): 4,361/4,361
> target rows have valid dates and URL/year-month matches, with zero malformed,
> failed, mismatched, or duplicate-slug records. The earlier PFR/PFT additivity
> artifact used only 715 relevant precise dates and was heavily 2022-weighted;
> its coverage figures are retained as partial-cache evidence, not treated as
> the final cross-season estimate. No ATS screen was rerun.

> **2026-08-20 later update to PER-03:** USA Today's public NFL player-arrests
> archive is now ingested end to end (`docs/player_arrests_sourcing.md`):
> 56/56 pages, 1,116 unique incidents dated 2000-01-24 through 2026-06-23.
> The resumable source contract hashes immutable raw pages and emits a
> mechanically restricted point-in-time view that excludes outcome,
> description, and link fields. This is ingestion evidence only: player/team
> matching and proof that an incident was public before the relevant prediction
> timestamp are still required before any ATS feature or screen.

> **2026-08-20 complete-cache follow-up to PER-03:** the frozen PFR/PFT
> additivity questions were rerun after all 4,361 predeclared PFR pages had
> precise dates (`docs/pfr_transactions_sourcing.md`, artifact
> `artifacts/pfr_pft_additivity/20260820T155757Z/result.json`). PFR contributes
> 1,839 Tuesday rows that PFT misses (25.86% of their matched union) and 1,708
> Saturday rows (22.53% of the union). On the official-injury population,
> pooling raises Tuesday visibility from 12.05% with PFT alone to 13.07%, adding
> 171 rows; Saturday is already saturated and gains 10. This is source-coverage
> evidence, not an ATS effect: retain PFR alongside PFT in the future frozen
> availability feature, but do not infer an improvement over the 53.4%
> opener-grade production rule from coverage alone.

> **2026-08-20 player-arrest screen follow-up to PER-03:** a point-in-time,
> leakage-tested 14-day incident flag is now declarative
> (`docs/player_arrests_screen.md`). The direction fixed before scoring was to
> fade the affected team. That fade leans wrong at both grades but remains
> category 3: close -0.1371 full-slate accuracy points,
> `probability_positive=0.0572` (247 flagged team-games); opener -0.1015,
> `probability_positive=0.1653` (50 flagged). Both are recorded
> `unresolved_below_power`; no admissible closing ground was established. The
> raw split therefore creates a clearly post-result lead in the opposite
> direction -- back the affected team -- which needs a direct 53.4%-policy
> comparison and live Tuesday refresh contract before prospective activation.

> **2026-08-20 severity follow-up:** a second predeclared player-arrest family
> isolated category labels involving violence against a person before reading
> ATS outcomes (`docs/player_arrests_severity_screen.md`). The 14-day fade is
> flat at the close (-0.0000 full-slate accuracy points,
> `probability_positive=0.4732`, 76 flagged team-games) and leans against the
> fade at the opener (-0.0335, `probability_positive=0.2893`, 22 flagged).
> Both cells are recorded `unresolved_below_power` with no admissible closing
> ground. Severity does not sharpen the broad incident lead; it remains useful
> retained evidence rather than a reason to activate a narrower policy.

> **2026-08-20 direct policy follow-up:** the clearly post-result broad-side
> lead was evaluated against the actual 53.36% production probability rule at
> the frozen opener (`docs/player_arrests_policy_eval.md`). Backing the sole
> recently affected team only when production opposed it changed 25 of 1,503
> graded games and scored 53.76% versus 53.36%: **+0.3992 accuracy points**,
> `probability_positive=0.8562`, with every season's delta nonnegative. It is
> recorded `unresolved_below_power`, not presented as fresh confirmation: the
> direction came from overlapping historical outcomes. Under the forced-pick
> EV rule this supports a freshness-gated, no-window-cost 2026 prospective
> challenger; it does not change production.

## Phase 5 — weather, venue, rest, and travel

| ID | Status | Item | Definition of done |
|---|---|---|---|
| ENV-02 | ⬜ | Stadium/roof/surface history | Venue state and roof decision where available. **2026-08-19: the surface half of this row produced the session's strongest new lead.** Grass-modal visitors playing on artificial turf under-cover: home side covers 52.25% vs 47.74% complement, +1.16 full-slate pts, week-blocked [+0.29, +2.04], P+ 0.995 (NFL REG 2009-2025, n_flag=1,112); survives venue control (within turf venues, grass-modal vs turf-modal visitors +1.46 pts, P+ 0.933; franchise-cluster interval similar, not a one-team artifact); **replicates on CFB** (+1.56 pts, P+ 0.916 on the XLG-03 clean core, `cfb_surface_familiarity_turf_venue_visitor_split`); the grass-venue mirror is ~null in BOTH leagues (P+ 0.320 NFL / 0.429 CFB), so the mechanism is asymmetric — turf-specific, not bilateral familiarity. NFL magnitude concentrates in 2018-2025 (mined era, disclosed). All entries `unresolved_below_power` in `registry/weak_signals.json` (`weather_battery_surface_switch_grass_to_turf`, `surface_familiarity_r1..r3`, CFB C1-C3). Exploitation is live at no window cost: `surface_switch_tilt_overlay` 2026 prospective challenger. **Deepened 2026-08-19 evening** (`docs/weather_followup.md`, `scripts/nfl_weather_followup_screen.py`): compounding the surface-switch flag with outdoor/temp<=45F (`weather_followup_surface_switch_x_outdoor_cold`) scores a raw per-game gap of +4.87pts (vs. the parent cell's ~+4.51pt raw gap), i.e. the mechanism does not weaken under the cold interaction, but the narrower subset (153 games vs. 1,112) full-slate-scales to only +0.17pts, week-blocked [-0.12, +0.45], P+ 0.877 — `unresolved_below_power`, registry. **2026-08-20: the roof-decision half of this row (open vs. closed at the five retractable venues, ARI/ATL/DAL/HOU/IND) was screened** (`docs/roof_decision_screen.md`, 6 cells, all `unresolved_below_power`). Pre-2020 completeness of nflverse's own `roof` field was resolved as a non-issue (measured populated back to 2009 despite a "NEW Feb 2020" changelog note that only marks when the column was EXPOSED, not when the value existed). Home cover at these five venues when open vs. closed: close grade (17 seasons) leans toward the home team covering WORSE when open, +0.0526 pts, P+ 0.8293; but the **opener grade on the 2020-2025 window that matters most for a live decision flips the sign**, -0.0504 pts, P+ 0.2736 -- the two honest gradings of the same mechanism disagree on direction, which per AGENTS.md's "grade at the opener" rule is the stronger reason not to wire this yet (not any crossing-zero objection). A visiting fixed-dome team's cover rate when a retractable venue plays open reads sharply the WRONG way at n=10 (80.0% cover vs. 49.97% complement, P+ 0.0147) -- reported plainly as opposing the hypothesized mechanism at this sample size, not spun as a null (too thin a cell, 1 of 20,000 bootstrap draws dropped for an empty arm, to invoke `wrong_sign_resolved`). A TOTALS-market cell (open roofs under-cover slightly, +0.0351 pts, P+ 0.6363 -- different market, not poolable with the ATS entries) and an exploratory "closed despite a benign forecast" cell (-0.2484 pts, P+ 0.0870, no predeclared direction) round out the family. No live T-90-to-kickoff roof-status source exists in this repo; not wired. **2026-09-02 source feasibility audit:** `docs/roof_decision_sourcing.md` verifies the league's T-90 contract and the current official publication surfaces. They are not a stable cross-venue interface: IND routes status exclusively to social/stadium channels, HOU describes a website/text announcement without a documented per-game endpoint, ATL exposes an event-page field without source timestamps/history guarantees, and the inspected ARI/DAL pages are editorial or omit status. ENV-02 remains open; no scheduler/parser was added and realized nflverse roof values remain inadmissible for reconstructing historical decision snapshots. **2026-09-11: the still-open piece was a deadline-visible predictor, not another live-source attempt** (`docs/roof_state_screen.md`, `scripts/roof_state_screen.py`). Re-measured the five-venue population independently (n=626 REG 2009-2025, matches the 2026-08-20 count) and confirmed MIN is NOT retractable (always `dome`/`outdoors`, never `open`/`closed`). Within-venue open-vs-closed home cover gap reproduces the same cross-grade sign disagreement found 2026-08-20 on a cleaner construction (open/closed compared only against these five venues, not the whole league): close grade -2.4599 pts, P+ 0.339 (home worse when open); opener grade (2020-2025 paired, n=227) +10.728 pts, P+ 0.838 (home BETTER when open). Split-half (odd/even seasons, close grade) also disagrees in sign (-10.571 pts P+ 0.072 vs +10.752 pts P+ 0.857) -- inconclusive, not a resolved zero-reliability refutation since both halves individually still cross zero. Market-pricing check: no detectable line shading for open vs. closed at these venues, +0.1443 pts, P+ 0.427, n=84 pairs. The playable piece: a per-season walk-forward logistic regression (forecast temp/precip at the `pool_decision` cutoff, now covering the FULL 2009-2025 window per `docs/forecast_archive_build.md`'s 2026-09-02 rebuild, plus venue-interacted temperature and venue dummies, refit on strictly prior seasons only) predicts roof state with raw accuracy that is NOT the informative number (84.7%, slightly below the 86.6% always-closed trivial baseline under the ~13% open base rate) but a resolved, real correlation between predicted and realized state, +0.2769, week-blocked 95% [+0.173, +0.382], P+ 1.000 -- fully excludes zero. Stacked as a predeclared fade (home pick to away only) on production's opener pick, rotation family `roof_state_predicted_open_fade_on_production`, window [2020, 2021]: only 2 of 456 paired games had a home baseline pick the flag could touch (7 games flagged, 5 were already away picks), -0.4386 pts, week-blocked 95% [-1.094, 0.000] (touches zero), P+ 0.0634; season-blocked reads fully negative but is a 2-flip degenerate artifact, not resolved power. All 7 cells `unresolved_below_power` in `registry/weak_signals.json` (`roof_state_home_cover_open_vs_closed_close`/`_opener`, `roof_state_market_pricing_check`, `roof_state_home_cover_gap_odd_seasons`/`_even_seasons`, `roof_state_prediction_skill_walk_forward`, `roof_state_predicted_open_fade_on_production`); rotation verdict `unresolved`, window spent. Still not wired to the played card -- a 2-game window is too thin to tell if the predictor helps, and this stays a tracked challenger, not a promotion. |

> **ENV-07 operational closure, 2026-09-02:** **Measured**
> (`data/raw/drought/20260902T221432Z/manifest.json`): the new immutable,
> fail-closed `--live` USDM refresh completed for all 34 stadium counties,
> zero failed/missing, with 31,348 rows from 2008-12-30 through the 2026-08-25
> map and SHA-256 provenance for every output. **Measured**
> (`tests/test_drought_monitor.py`):
> full-day Tuesday AQI is now unavailable at Tuesday noon, and future AQI
> mutations cannot change the checkpoint. **Read** (EPA AirNow web-service
> docs/FAQ): the authenticated legacy lat/long service retires 2026-09-30 and
> EPA recommends live file products for database use. **Measured**
> (`data/raw/airnow_hourly/20260902T223015Z/manifest.json`): the no-auth hourly
> file captured 1,783 usable active US site rows and mapped all 30 current NFL
> stadium counties with exact URL/capture-time/SHA-256 provenance; 16
> non-county AQS identifiers were quarantined rather than guessed. **Measured**
> (`tests/test_airnow_hourly_capture.py`): the scheduled Tuesday 11:40 ET
> capture joins only by immutable capture time, post-decision mutations cannot
> leak backward, and stale rows are nulled. ENV-07 is complete without an API
> key, retiring endpoint, model change, or new experiment.

> **ENV-01 implementation update, 2026-09-02:** the exact playable cutoff is
> now encoded once in `nfl_ats.nfl_week.pool_decision_cutoff` and shared by
> archive ingestion, feature validation, and the live weather challengers:
> `min(kickoff, Sunday 16:00 America/New_York)`. The archive consumer rejects
> legacy `kickoff_nearest` rows, post-lock issuances, unresolved domestic
> fetches, missing game coverage, and parquet/manifest hash drift. ENV-01 stays
> open because the local full-window archive is still the older
> `kickoff_nearest_2009_2025`; the exact `pool_decision` build and validation
> commands are in `docs/forecast_archive_build.md`. No ATS experiment was run.

## Phase 6 — modeling and probability distributions

| ID | Status | Item | Definition of done |
|---|---|---|---|
| MOD-07 | 🚧 | Ensemble/stacking | Out-of-fold predictions only; weight stability constraints. **Promoted 2026-08-18.** The first predeclared vehicle — the weak-signal stack (player value composite + learned availability + three documented opener-bias families, profile `weak_stack`) — took its one registry look on [2020, 2021] at the Tuesday-opener grade (August 2026, `docs/mod07_stack.md`): on 456 paired games the candidate scored **53.29% vs the baseline's 51.32%, +1.97 points, week-blocked [−1.10, +5.00], `probability_positive` 0.8745**, short of the predeclared 0.90 threshold, so the registry verdict is `unresolved` and the window is spent. The entire delta comes from the 49 picks the arms split (candidate 29–20). **The registry verdict did not change and was not the basis for promotion.** Per AGENTS.md a promotion bar is not a decision bar: the pool is forced picks, so declining a candidate that is 87% likely better is taking the other side of an 87/13 bet, and what gets PLAYED is decided on expected value at the grade the pool settles on. Broadened to 1,537 paired games at the same opener grade with the same method and alpha, the candidate scores **52.83% vs the baseline's 52.50%, +0.33 points**, and it was promoted on that basis (commit 68b4dc0). At the **close** on 2,075 games 2018-2025 it scores 51.57% vs 52.05% — a promotion was first refused on that comparison and the refusal was wrong instrument, since the close is the market at its sharpest and systematically understates pool-relevant edge. The predeclared 0.90 threshold still governs what these docs may CLAIM: this is a play decision, not a resolved finding. Prospective 2026 evidence is the next real test. Retuning the stack and re-scoring [2020, 2021] is inadmissible. **2026-08-20: a `weak_stack_v3` candidate (15 new columns across 3 sub-families -- division revenge/sandwich-spot/post-blowout bias, penalty-rate discipline, and two travel/rest flags, all already-recorded `probability_positive >= 0.60` registry leans not yet in production) was built and scored opener-graded against the active `weak_stack` on the same 1,537-game archive** (`docs/weak_stack_v3.md`): **53.03% vs. 53.36%, -0.333 accuracy points, week-blocked 95% [-2.107, +1.467], `probability_positive` 0.3415 -- a real lean against, `unresolved_below_power`, not played.** Close grade and the historical sign rule agree on direction against; Brier/log-loss also lean against. The two strongest deferred candidates by reliability (FluView away-market illness, reliability 0.981; the forecast-weather warm-team/temp-gap cells) were NOT built into this pass -- flagged as the leaner v4 stack worth trying next, per the doc's own "the EV read" section **2026-09-07 lane C (Codex; coordinator re-ran its 21 tests and read the registry back):** the forecast-weather v4 arm had already been scored (docs/weak_stack_v4.md: -1.0645 points [-2.6882, +0.5953], probability_positive 0.0956, not played), so the remaining never-stacked family was FluView. `weak_stack_v5` = weak_stack + one continuous away-market ILI column (`fluview_away_market_ili_asof`, as-of the prior Tuesday midnight, leakage tests in tests/test_weak_stack_v5.py), opener-graded against active model 2ceaf63b56b7ce25 on the same 1,537-game archive (incumbent replay matched all 1,537 saved picks within 1e-12): **-1.072 accuracy points, week-blocked 95% [-2.374, +0.217], probability_positive 0.049, 1,399 non-push games, 93.1% coverage** (v5 52.04% vs incumbent 53.11%); per season 0.00 / +0.92 / -3.85 / -1.19 / -0.79 / -1.19 (2020-2025). The rotation-assigned 2020-2021 window alone reads +0.49 [-1.49, +2.50], P+ 0.645 -- disclosed, not promoted over the predeclared primary. Decision: keep the incumbent (EV rule); mechanism NOT closed -- recorded `unresolved_below_power` as `weak_stack_v5_fluview_away_opener` (+ the 2020-2021 subset entry) and `rotation record` verdict `unresolved`, family `weak_stack_v5_fluview_away`. Artifact artifacts/weak_stack_v5_opener_eval/20260907T140503Z/. Nothing played or activated changed. |
| MOD-09 | 🔬 | Sequence model over drives | Small temporal model, benchmarked against summary features. **Evidence against, 2026-08-17** (`docs/play_level_audit.md`): the premise that play-level rows multiply the training set does not hold — plays are near-independent (ICC 0.013), so the per-game mean is already a sufficient statistic for what they say about a team, and the model still fits on ~4,700 game-level labels however many plays are exposed. The drive layer's summary form already worsened Brier, and a sequence model must beat those summaries while spending far more parameters on the same labels. Do not build ahead of the cheaper noise-reduction work in PBP-05 |
| MOD-10 | 🔬 | Graph model | Player/team matchup graph only after player state is reliable |
| MOD-17 | 🚧 | Unified served numbers: one lattice, one margin, one total | Owner rule 2026-09-05: "if our spread prediction disagrees with the total prediction, we need to understand why we dont have a unified model". Measured that day: the Week 1 card picked KC -3 (production `predicted_margin` +3.19) while the tiebreaker guessed KC 23-20 (margin exactly 3, a push against the pick) because `tiebreaker.py` took its margin from a neighbourhood median instead of the margin model. The unifying object already exists (MOD-05's joint score lattice, `nfl_ats.score_lattice`), so the contract is: every served number -- the pick, the served total, the tiebreaker final, the push and cover probabilities -- is read off ONE lattice centred on the margin model's `predicted_margin` and the totals model's served total; the tiebreaker final is the most probable lattice final on the pick's side of the spread; publishing fails closed on any contradiction. **Still open, the research half:** measure whether a single bivariate residual model (margin residual and total residual fitted jointly on the production feature set, walk-forward) beats the two separate marginal residual models on opener ATS accuracy and total MAE, and report the correlation of the two residuals; predeclare, record through both registries, promote on EV only. **2026-09-05 (research half measured, `docs/mod17_joint_residual_model.md`, `scripts/mod17_joint_residual_screen.py`, `artifacts/mod17_joint_residual/20260905T160219Z/`):** realised margin/total residual correlation +0.0304, 95% [+0.0063, +0.0539] (season-blocked, 17 seasons), `probability_positive` 0.994 -- small, real, and bounds any joint-model gain near r^2≈0.0009 of variance. A predeclared multi-output ridge on the 90-column margin + 24-column totals-wave-2 union (114 cols; mathematically identical to two independent single-target ridges on that union, per a pinned test) scored: margin side (opener sign rule, 1,503 games) **-0.732 accuracy points vs production**, 95% [-2.831, +1.411], `probability_positive` 0.243 -- not promoted. Total side (3,919 games) **+0.00491 mae_improvement** vs the served k=0.1 blend, 95% [-0.00662, +0.01646], `probability_positive` 0.791 -- meets the predeclared EV promotion bar, though out-of-sample R² vs the market is worse with the wider feature set (-0.0762 vs wave-1's -0.0162), so this is a small, shrinkage-heavy win, not a real fit improvement. A second-stage (SUR-lite) model letting each residual see the other's out-of-fold prediction did NOT beat a solo-recalibration control on either target (margin R² -0.0027 coupled vs -0.0008 solo; total -0.0002 coupled vs +0.0002 solo) -- answering the owner's question directly: the two marginal models are not "looking fine" because of hidden compensating errors a joint model would fix; there is too little shared structure between the residuals to exploit. Positive control (leaked margin truth) confirmed the harness's power: +43.979 accuracy points, `probability_positive` 1.0. All three effects recorded `unresolved_below_power` in `registry/weak_signals.json` (`mod17_joint_residual_margin_opener`, `mod17_joint_residual_total_blend`, `mod17_joint_residual_realised_correlation`); no rotation window spent (reused opener archive, disclosed). Status stays 🚧 pending the served-numbers-unification half. **2026-09-05 (production wiring, lane AJ):** wired the research half's decision into the served number. `nfl_ats.served_total` adds two named methods -- `served_total_blend_k01` (today's rule, byte-identical, hash-pinned) and `served_total_joint_residual` (the joint model's own total-column prediction, fit walk-forward on `data/processed/game_features_weak_stack.parquet` with the same (season, week)-block cutoff `nfl_ats.totals_wave2.model_total_view_wave2` already uses, blended at its own measured k=0.1) -- behind an explicit `SERVED_TOTAL_METHOD` constant, now `"joint_residual"` per the EV rule (`probability_positive` 0.791, non-negative point estimate). `nfl_ats.tiebreaker.build_report`/`TiebreakerReport` now compute and persist BOTH numbers every call (`served_total`, `served_total_method`, `comparison_total_blend_k01`, in `tiebreaker.json` and the CLI report), with automatic fall-back to the blend whenever the joint model cannot price a game -- never a hard failure over an optional input. Measured on the live Week 1 card (`nfl-ats tiebreaker --season 2026 --week 1`): served (joint_residual) 43.73 vs comparison (blend_k01) 43.62 (market 43.5, wave-2 totals residual +1.22); the pick-consistent final stayed **KC 24, DEN 20**, strictly on the KC -3 side, identical under both methods. Registered a `totals_served_method` side-ledger prospective challenger (`src/nfl_ats/served_total_challenger.py`, `artifacts/prospective/challengers.json`) that records both served-total candidates for the week's tiebreaker game and backfills the realised total once it is final, wired into `publish-predictions --record-decisions`; `scripts/lockday_rehearsal.py`'s static wiring audit passes (0 errors, 31 active challengers, up from 30). Tests: `tests/test_served_total.py` (16, incl. the blend_k01 hash pin and a walk-forward cutoff leakage guard mirroring `tests/test_totals.py`'s pattern) and `tests/test_served_total_challenger.py` (9), all passing; `tests/test_tiebreaker.py`, `tests/test_publishing.py`'s tiebreaker-specific tests, `tests/test_cli.py`, and `tests/test_lockday_package.py` all still pass against this change (some UNRELATED failures were observed mid-session in `test_publishing.py`/`test_cli.py`/`test_board_terminal.py`/`test_board_improvements.py`/`test_public_board.py`, traced to a concurrent other-lane edit adding `nfl_ats.board_content.verify_number_provenance` to the public-site build path -- not touched by this row, reported separately). Status stays 🚧: the served-total switch is live and tested, but the prospective challenger has zero accrued rows yet and the coordinator has not yet republished the card with the new served total. **2026-09-07 (measured):** the card WAS republished with the new served total -- `tiebreaker.json` for 2026_01_DEN_KC now carries `served_total_method: joint_residual` (served 43.73, comparison blend 43.62; guess KC 24 - DEN 20). Only the prospective rows (first written at the Tuesday 2026-09-08 lock) remain before this row can close. **2026-09-11 (measured, standing audit shipped, `scripts/mod17_served_number_audit.py`, `docs/tiebreaker.md`'s 2026-09-10/11 sections):** the prospective row EXISTS -- `artifacts/prospective/totals_served_method_decisions.parquet` holds one row (2026 week 1, DEN at KC, recorded 2026-09-09T23:13:48Z, blend_k01 43.6115 / joint_residual 43.7238, `realised_total` still NaN until the 2026-09-14 kickoff). What the row's contract had never had was a checkable definition, so nine invariants (C1-C9) were frozen in `docs/tiebreaker.md` BEFORE the audit was first run, and a tenth (C10) added afterwards and disclosed. Run against the live card (active `d49194e04945a5e5`, forecast `margin_predictions/2026-week-01-20260910T210852Z`): **C1, C3, C4, C5, C6, C7 and C9 hold on all sixteen games** -- every printed line matches the served spread, no card probability or displayed confidence falls below 50% on the picked side, every served total is exactly the method total plus the -1 shade, every pick-consistent cell is on the lattice, strictly on the pick side, inside the 3-point hard guard and the 1-point total tolerance. **Thirteen violations in three groups.** (1) **C2/C10, five games: there are three different margins.** `ATL_PIT`, `BAL_IND`, `DEN_KC`, `NO_DET` and `TB_CIN` have `predicted_margin` on the wrong side of their own pick's line. Four are deliberate overlay flips (`coach_fade` on BAL_IND; `pbp08_protection_mismatch_tilt` on the other three) -- a flip mirrors the probability and changes the pick but never moves the margin. The fifth is inside the model: measured by least squares over all sixteen games (max abs error 2.4e-14), the served cover probability is exactly `norm.sf(spread_line - predicted_margin, loc=+0.369057, scale=12.646524)`, so the pick is decided at `predicted_margin + 0.369057` (the `point` field `discrete_push_read.json` already publishes) while the lattice centres on `predicted_margin`, and a third centre exists again in the 50% margin band (`predicted_margin + 1.129237`, asymmetric half-widths, i.e. empirical quantiles rather than `gaussian_median`). The same five games make `nfl-ats tiebreaker` with no published row build its guess for the other side -- rehearsed read-only this session, the CLI guesses KC 23 - DEN 20 "consistent with the KC -2.5 pick" while the card picks DEN +2.5 and its own tiebreaker is KC 22 - DEN 20. NOT fixed here and not narrowed away: re-centring the lattice changes a served number on a rule this row's own text pins to `predicted_margin`, and an overlay flip produces no margin to re-centre on. Bound measured against it: `predicted_margin` is rendered on no reader-facing page (no hit in `board_content.py`, `board_terminal.py`, `public_board.py`, `card_explanation.py`), so the damage today is internal -- `tiebreaker.json`'s `lattice_centre_margin` publishes on the other side of its own `pick_side`. Named next step: a predeclared challenger centring the lattice on the pick-deciding margin. (2) **C8, the published tiebreaker is stale**: `tiebreaker.json` predates the same night's -1 shade wiring -- no `total_low_side_shade_points`, `served_total` 43.7238 where the code now serves 42.7238, score KC 23 - DEN 21 where the code now produces KC 22 - DEN 20. One `nfl-ats publish-predictions` fixes it; handed to the session's publish step because this lane may not rewrite `CURRENT_PREDICTIONS.md`. (3) **Fixed here: the shade was leaking into one arm of a paired ledger.** Because the shade now moves `TiebreakerReport.served_total`, `nfl_ats.served_total_challenger` would have recorded the joint arm shaded (42.7238) against an unshaded blend arm (43.6115) -- a 1.1-point gap of which a full point is the shade -- into a ledger whose one stored row holds the unshaded pair, crediting the shade's known low-side edge to the joint model. `TiebreakerReport.served_total_before_shade` plus one line in the recorder puts both arms back on the method-only scale; measured after the fix, the next recording writes blend_k01 43.6115 / joint_residual 43.7238. The mirror defect in `nfl_ats.tiebreaker_shade_prospective` (its "served" arm now reads an already-shaded published score against a `market_total - 1` challenger, so the two arms are shade-on-blend vs shade-on-market) is guarded rather than redefined: the recorder now skips with a stated reason when the payload's shade is already at or past -1, leaving LEAD-54's owner to decide what that challenger becomes. Gates: `ruff format`, `ruff check`, `mypy src` (224 files) clean; `pytest tests -k "tiebreak or lattice or card or publish" -n 2` 364 passed, 5 skipped, no test edited. Status stays 🚧 on two items: the realised total for the one prospective row (2026-09-14), and the three-margins diagnosis above. **2026-09-11 (the three-margins next step, measured, predeclared in `docs/tiebreaker.md`'s 2026-09-11 section before any number existed):** built the named challenger -- the pick-consistent lattice centred on the pick-deciding margin `point` (`predicted_margin` + the served `gaussian_median` residual location, read from `discrete_push_read.json`, never a new constant) instead of `predicted_margin`, with the overlay-flip branch FROZEN IN WRITING FIRST: when `point` is still on the side the card no longer picks, centre on the line plus the minimum consistent step (`floor(line)+1` for a HOME pick, `ceil(line)-1` for an AWAY pick), rejecting the mirror-across-the-line alternative because it manufactures a conviction the overlay never expressed. **Historical read** (`scripts/mod17_lattice_centre_screen.py`, `artifacts/mod17_lattice_centre/20260911T043520Z/`): 1,537 graded games, 107 week blocks, 2020-2025, off the ACTIVE model's walk-forward opener archive `opener_evaluation/20260910T211255Z`; the per-week probability mapping inverts exactly (worst rebuild gap 1.1e-16), zero hard-guard refusals on any arm. **The challenger removes the C2 defect outright: the centre is strictly on the played side in 100% of games against the served centre's 85.0%, +15.029 accuracy points, 95% [+13.007, +16.939], `probability_positive` 1.0000** -- 231 of 1,537 cards were served with the lattice centred on the side the card does not play, `point` fixes 188 of them and the flip rule the other 43 (the archive's own metadata independently reports 43 opener picks changed by the home-side offset). It is not cosmetic: the two arms print a different final on 747 games (48.6%). **The cost is unmeasurable at this size:** closest-total error -0.02147, 95% [-0.05233, +0.01163], `probability_positive` 0.0985; closest-score error -0.02668, 95% [-0.06153, +0.00912], `probability_positive` 0.0707; on the week's-last-game population (101 games) -0.0198 on both, `probability_positive` 0.3713 / 0.3978. **The predeclared decision rule fires NO** (it required `probability_positive` > 0.5 on closest-total) **and the served card was not touched** -- and the predeclared positive control then showed that metric could never have said yes: centring on the REALISED margin moves closest-total by -0.0221, 95% [-0.0580, +0.0132], `probability_positive` 0.11 (perfect margin knowledge buys no closest-total accuracy, because the total axis is pinned by the served total and the one-point tolerance), while it moves closest-score by +2.371, 95% [+2.149, +2.605], `probability_positive` 1.0000. So the rule stands as written and its NO is reported as its NO; the instrument lesson is that the NEXT predeclaration hangs on closest-score, where the control demonstrably registers. `bounded_by_control` is inadmissible on both error metrics (the control detects 2.4 points, the effect is 0.027) and no interval resolves a sign, so all seven rows are `unresolved_below_power` in `registry/weak_signals.json` under family `lattice_centre_challenger` (`mod17_lattice_centre_closest_total`, `..._closest_score`, `..._pick_consistency`, `..._closest_total_last_game`, `..._closest_score_last_game`, and the two `..._control_*` rows). **Registered as a paired prospective arm** (`src/nfl_ats/lattice_centre_challenger.py`, challenger id `tiebreaker_lattice_centre`, ledger `artifacts/prospective/lattice_centre_decisions.parquet`), wired additively into `publish-predictions --record-decisions` beside the shade and served-total arms, with its `lockday_verify` dedicated-ledger entry and a human display name on the Model Ledger; `scripts/lockday_contract.py` passes (58 active, 42 publish, 0 errors). **Week 1 confirmation (DEN at KC)**, recorder exercised for real with the ledger redirected to a scratch directory so no repository row was written by this lane: the game is one of the four overlay flips, card picks DEN +2.5, served centre +2.7045 (KC's side), `point` +3.0736 (location +0.369057, still KC's side because a flip produces no margin), challenger centre +2.0 by the minimum-step branch; both arms guess **KC 23 - DEN 21** at the currently published 43.7238 total and **KC 22 - DEN 20** at the 42.7238 the code serves now -- identical this week, so nothing a reader sees changes. Gates: `ruff format`, `ruff check`, `mypy src` (225 files) clean; `pytest tests -k "tiebreak or lattice" -n 2` 125 passed, 5 skipped, plus the challenger-wiring tests in `test_cli.py` / `test_board_terminal.py` / `test_lockday_contract.py` / `test_lockday_verify.py`, 12 passed; no test added or edited. Status stays 🚧: the prospective ledger has zero rows until this session's publish writes one, and the served number still centres on `predicted_margin`. |
| MOD-18 | 🚧 | Spread-size regime: calibrate the model by spread instead of flipping it | Owner, 2026-09-07, on the spread-gap zone fade: "this needs to be addressed holistically. current implementation seems like overfitting." Measured the same day on the active model's opener evaluation (1,503 non-push, 2020-2025): model accuracy by spread bucket 56.2% (0-3) / 56.2% (3.5-6.5) / 52.7% (7-7.5) / **48.5% (7.5-10)** / **44.3% (10.5+)** while its stated confidence sits at 55-57% in every bucket; inside 7.5-10 it takes the FAVOURITE 57% of the time where favourites cover only 45.9% (underdogs 54.1%), and is right 45% on its 111 favourite picks vs 53% on its 83 underdog picks (favourite/underdog labels corrected 2026-09-07 by lane H: nflverse's positive home spread means HOME FAVOURED; the coordinator's first read had the sign reversed); final-margin mass 14.1% on 3, 7.7% on 7, 5.1% on 10, 4.5% on 14. The zone flip's bounds were mined on 2018-2025 (one 2011-2017 replication supports them, P+ 0.91) and the four-rule union it sits in was the best of 127 subsets on the same archive. Program (lane H, predeclared in docs/spread_regime_program.md): (1) diagnosis by era and pick side plus a reliability table; (2) candidates -- C1 signed spread-bucket and key-number features in the ridge, C2 a key-number-aware discrete probability mapping, C3 a walk-forward spread-bucket calibration layer, C4 combinations; (3) the decision read THROUGH the played card, with and without the zone member, so the flip is retired if a principled component absorbs it. Grade at the opener, week-blocked, recorded through both registries. Done when the served probability is calibrated by spread size and the zone flip is either retired or kept on a measured composed-card marginal. **Owner, later the same day: "i said it before and it seems like it was rejected/ignored - the football scores dont follow a smooth gaussian.... its a multi-modal distribution."** Read: MOD-05 (2026-08-17) confirmed exactly that (dip p = 0.000; 14.6% of games on 3 vs 5.3% normal) and set it aside for ATS because the POOLED residual tests smooth -- a composition fallacy for a per-line cover probability; MOD-08's Gaussian-vs-ECDF promotion compared two pooled-residual reads and never tested a multimodal margin model. Now binding in AGENTS.md ("Football margins are multimodal, not Gaussian"). Consequence for this row: **C2 (the discrete, key-number-aware cover probability conditional on the line -- the MOD-05 lattice recentred on the prediction, or an equivalent integer-mass mapping) is the PRIMARY direction; C1/C3 are supplements; the Gaussian read becomes a challenger.** | 2026-09-07: opened; lane H running with C1-C4; C2 elevated to primary after the owner's restatement. **Owner, AFK order, same day: "no more arbitrary pick flips because we can't explain a drop in accuracy at certain point thresholds... we need to be able to explain these things so we can understand the weak points in the model."** Acting on it: lane I retires the spread-gap zone fade from the PLAYED card (three mechanism members remain: coach fade, division revenge, player arrests; measured cost on the mined archive 55.42% -> 55.22%, an in-sample selected number; the four-member union keeps accruing as a former-production challenger and the zone fade stays a challenger); lane J puts a "Where the model is weak" table (accuracy, pick side and stated confidence by spread bucket, from the active model's own opener evaluation) on the Model page and in the assistant. Rule now binding in AGENTS.md ("No unexplained threshold flips on the played card"). **Lane H result (Codex; coordinator re-ran its 223 tests and read laneH/results.json; lane H's claim that it recorded its arms was FALSE -- zero registry entries existed, the coordinator recorded all five as `mod18_spread_regime_opener_v1_*`):** every arm leans below the incumbent at the opener on 1,503 non-push games -- C1 spread-regime ridge features -1.73 pts [-3.45, +0.00] P+ 0.02; C2 rounded pooled-residual mapping -0.60 [-1.80, +0.59] P+ 0.15; C3 walk-forward bucket calibration -2.26 [-5.63, +1.13] P+ 0.09; C1+C2 -1.66 P+ 0.03; C1+C3 -3.33 P+ 0.03. Through the frozen four-member card C2 55.29% vs 55.42%; C2 changes 0 of 16 Week 1 sides. **Lane H's C2 was a rounded POOLED-residual read, by its own admission "not a completed conditional key-number model" -- the owner's actual proposal (a discrete margin distribution conditional on the line) is still untested and is the next lane.** Diagnosis by era (nflverse-close grade before 2020): the 7.5-10 dip replicates in 2011-2017 (45.8%, n=238; favourites cover 47.5%) and is muted in 2018-2019 (50.8%, n=61); C1/C3's walk-forward failure says the effect is not learnable as a stable regime shift, which is consistent with the owner's overfitting read. **Label correction:** lane H found the coordinator's favourite/underdog columns reversed (nflverse positive home spread = HOME favoured; verified: mean home result +5.85 on 908 such games) -- corrected in this row, the queued-lead note, UI-20 and lane J's module (`model_weak_spots.py` had inherited the reversed sign; fixed and re-tested before publication). **Lane I result (Codex; coordinator applied the three out-of-scope guard patches it flagged -- `pick_refresh` policy-id guard, `clv` OR-union validator, `signal_ledger` reader copy -- and re-ran: 123 + 533 tests green, rehearsal 41 active paths / 0 errors):** the played card is now the THREE-member union coach fade + division revenge + player arrests, policy id `overlay_union_coach_division_revenge_player_arrests_v2`; the spread-gap zone member is out of the played composition, the flip-line scan, the card explanations and the crew-tilt flip map; the retired four-member card keeps accruing 2026 evidence as challenger `overlay_four_member_union_retired_20260907` (recorder wired into `publish-predictions --record-decisions`, fail-open), and the standalone zone challenger is untouched. Isolated chain proof: Week 1 changes exactly one side, **CLE +7.5 -> JAX -7.5**; scratch headline 55.2% (three-member row 0.5522; retired four-member 0.5542 kept as the comparison reference); number provenance passes. docs/spread_gap_zone_retired.md. Live activation via the coordinator's real refresh follows. **Live (measured 2026-09-07 12:44 ET):** the real refresh activated it -- forecast 20260907T164352Z, card JAX -7.5, headline 55.2%, no rule label on the board. **Lane K (Codex; the owner's actual proposal, verified: 21 registry entries under `mod18_conditional_margin_v1_*`, 106 tests, gates clean):** a DISCRETE integer-margin cover probability conditional on the line, three predeclared forms -- K1 a 2.5-point kernel over prior predicted margins with integer atoms kept, K2 the empirical margin lattice translated so its median matches the prediction, K3 K1 conditioned additionally on which side of the nearest key number the line sits -- opener-graded vs the active model on 1,503 non-push games: **K1 -1.80 pts [-4.64, +1.07] P+ 0.105 (379 picks change); K2 -0.27 [-1.25, +0.73] P+ 0.27; K3 -2.86 [-6.31, +0.60] P+ 0.05**; Brier/log loss also lean worse (K2's Brier interval wholly below zero); through the three-member card K2 -0.20 P+ 0.28. **The instructive part:** K1 fixes exactly the band the owner asked about -- 7.5-10 goes from 48.5% to 55.2% right, and its stated confidence there rises to 59% -- but it loses on 0-3 (54.5% vs 56.2%) and 3.5-6.5 (50.2% vs 56.2%), where the smooth read was already calibrated; the key-number structure matters where the line sits near a mass point and adds noise where it does not. K2's translation displaces the absolute key numbers (push rate at |line| = 3 predicted 4.1% vs 10.1% realised) -- MOD-05's recentring constraint confirmed. Week 1: K2 changes 0 sides, K1 6, K3 4. Decision: keep the incumbent mapping; nothing closed (all `unresolved_below_power`); the next step is a HYBRID that is not a threshold flip -- a conditional read whose weight on the key-number lattice is itself learned from prior games as a function of the line's distance to the nearest key number, so it defers to the smooth read away from 3/7/10/14 and to the lattice near them. **Lane L, hybrid margin mapping (Codex, 2026-09-07 evening; coordinator read docs/hybrid_margin_mapping.md and measured 136 registry entries under family `mod18_hybrid_margin_v1`, 58 live comparison cells + 78 invalidated diagnostic mis-recordings):** the learned hybrid -- cover probability = w(d)*lattice + (1-w(d))*smooth with w(d) = w0*exp(-d/tau) fitted walk-forward on prior non-push Brier, d = distance of |line| to {3,7,10,14} -- H1 scores -0.60 pts [-2.06, +0.86] P+ 0.196 at the opener on 1,503 non-push games and H2 (w also by spread size) -0.80 [-2.62, +1.00] P+ 0.180; through the three-member card -0.33 P+ 0.293 and -0.20 P+ 0.381; H1's Brier/log loss improve by +0.00005/+0.00017 (P+ 0.54/0.55), H2's worsen. Both change three Week 1 model sides (CLE->JAX already on the card, DAL, NO). Incumbent kept; every cell `unresolved_below_power`. **Diagnosis D is the payoff:** the 7.5-10 weakness is a probability-shape problem concentrated in HOME UNDERDOG games -- realized home cover minus the smooth probability is +14.5 pp [+3.0, +26.3] P+ 0.993 on the 69 home-underdog games there (K1's lattice gap -0.4), while the 125 home-favourite games show no gap (-0.1 pp); and at 10.5+ the POINT forecast itself is off: actual home margin exceeds it by +2.91 [+0.83, +5.01] P+ 0.997 (134 games), +5.18 [+2.10, +8.20] P+ 0.999 on the 65 games where the model took the underdog (that stratum is by PICK SIDE, not home/away -- an earlier draft of this sentence misread it as home underdogs; lane P's true home/away split from the same evaluation: 10.5+ home underdogs 32 games covered 59.4% vs 47.0% expected, 10.5+ home favourites 99 games covered 57.6% vs 49.4% with the model right only 39.4%). So the model under-locates the HOME team on big spreads on both sides, and specifically home underdogs at 7.5-10: a named mechanism rather than a threshold. Next lanes (launched 2026-09-07 evening): lane Q predeclares `mod18_home_dog_location_v1` (a home-underdog-points feature and a hinge above 7 so the ridge learns the correction walk-forward, graded at the opener through the played card; a home-side spread-size term was proposed as a third arm once lane P's split arrived, allowed only if lane Q had not yet frozen). **Lane Q result (Fable; coordinator measured 88 registry entries under `mod18_home_dog_location_v1` and re-ran its 77 tests):** baseline replayed the archive exactly, then Q1 `home_dog_points` -0.33 pts [-1.06, +0.39] P+ 0.155 at the opener (27 picks change) and Q2 with the hinge above 7 -0.27 [-1.26, +0.73] P+ 0.273 (62 change); through the three-member card 0.00 [-0.54, +0.54] P+ 0.462 and -0.27 P+ 0.235; Brier/log loss within 0.0001 of the incumbent; windows 2020-21 +0.22 / 2022-23 -1.36 [-2.71, -0.19] / 2024-25 +0.19 for Q1. Incumbent kept on expected value; nothing closed; three opener windows spent unresolved. The diagnosis is the payoff again: the home point error does NOT shrink (10.5+ home underdogs +4.50 -> +4.40 / +3.04 on 32 games) because the ridge weights the term under 0.1 pt/pt with a sign that flips across training epochs, and on the TRUE home split 10.5+ home favourites are also under-located, +2.41 [+0.11, +4.68] P+ 0.98 (102 games). Q3 = |spread| was correctly not added (an exact linear combination of `spread_line` and Q1). Week 1: Q1 changes 0 sides, Q2 would flip NE +3.5 at SEA (p 0.501), not played. Next predeclared candidate: a symmetric home-side hinge max(0, |spread| - 7), and, since the ridge cannot hold the term, a location correction outside the ridge (e.g. a post-hoc home-side offset by spread size learned walk-forward) as the alternative shape. **Lane S result (Fable; coordinator measured 88 registry rows under `mod18_home_side_location_v1`, re-ran its tests, and read its decision table):** both follow-ups predeclared and frozen -- S1 the symmetric hinge in the ridge -0.13 pts [-0.85, +0.59] P+ 0.324 (26 picks change; 0.00 P+ 0.448 through the card; the ridge again assigns a tenth of a point per point with a sign flip across epochs); **S2 the walk-forward home-side offset by spread bucket OUTSIDE the ridge (shrunken mean of actual home margin minus the incumbent's out-of-time point over prior games, 100-game prior, mapping unchanged): -0.20 [-1.60, +1.20] P+ 0.368 standalone (107 change) and +0.33 [-0.80, +1.46] P+ 0.695 THROUGH THE PLAYED CARD (55.56% vs 55.22%, 71 flips).** S2 is the first arm that moves the diagnosed error: the 10.5+ home point error shrinks by a third (+2.91 -> +1.97; home favourites +2.41 -> +1.48 on 102; home underdogs +4.50 -> +3.53 on 32), accuracy at 7.5-10 goes 48.5% -> 51.5% (home underdogs +7.25 [+1.49, +13.89] P+ 0.995, n 69) and at 10.5+ 44.3% -> 47.3%, paid for at 0-3 (-1.99 P+ 0.028) where the offset ran negative through 2023. Against it, stated plainly: the two S2 reads differ by 8 games of 1,503; Brier/log loss lean worse (P+ 0.10-0.11); 2024 -2.26 P+ 0.03 vs 2025 +3.00 P+ 0.93; the archive is mined. Week 1: S1 changes 0 sides, S2 flips NE +3.5 at SEA to SEA -3.5 (p 0.510). Three opener windows spent unresolved; nothing closed. **Coordinator decision, 2026-09-07 evening, on the EV rule (AGENTS.md "A promotion bar is not a decision bar"; precedent the same morning's median promotion at P+ 0.597 through the card): S2 is PLAYED.** The coordinator wired it directly (the promotion-lane launch was blocked by the session's permission classifier; docs/home_side_offset_promotion.md): `MarginModel.predict`/`line_sweep` accept a per-row `center_offset`; `margin-predict` fits the offsets from the archived out-of-time opener stream (history precedence: the active-matched opener evaluation, else the newest evaluation with a recorded model-id warning, else zero offsets -- never blocks the lock), serves them on the `market_residual` point only, and writes `home_side_offset.json` with both reads per game; the late-week refresh applies the same per-game offset when it refits at the frozen line; the uncorrected read is paired challenger `home_side_offset_off_incumbent` (recorder in `publish-predictions --record-decisions` and the lock-day rehearsal, fail-open; refuses a card without the sidecar or a fingerprint drift). **Isolated chain proof (measured 21:05 ET):** weekly-run's step-5 argv in a scratch artifacts root -- SYNCHRONIZED on the unchanged model id, offsets 0-3 +0.03 / 3.5-6.5 +0.38 / 7 -0.17 / 7.5-10 +0.78 / 10.5+ +1.92 (113 prior games) equal to lane S's Week 1 numbers, exactly one side changes (NE +3.5 -> SEA -3.5, 0.498 -> 0.510), the sidecar's uncorrected read reproduces the live card to 5.6e-17; lockday rehearsal 42 active / 0 errors; 14 new tests. **Known gap, disclosed:** the board's headline archive score still comes from the raw model's opener evaluation composed through the union (55.2%), not the corrected 55.6%; the follow-up is to apply the same walk-forward offset inside `opener-evaluation` so the headline and the card share one policy. The calibration cost and the 8-game margin are disclosed here so the 2026 paired rows settle the question at no window cost. **Gap closed 2026-09-08 (measured):** `opener-evaluation` applies the served walk-forward offset week by week (raw residual kept as the fitting stream, served/raw twins in every artifact, `--no-home-side-offset` for the raw comparison); artifact `20260908T110201Z`: model alone 53.76% served vs 53.96% raw at the opener on 1,537 games (109 picks changed), through the played three-member card **55.56%** (+1.80 over its own baseline, [-0.20, +3.81], P+ 0.958) vs 55.22% before; the board headline reads 55.6% and the Model page carries "The home-side push" table. **Lane E (Codex read-only, same morning; coordinator re-ran the standalone read and computed the through-card read): S3 = the push served ONLY on spreads of seven or more** (the buckets where the location error was diagnosed; S2's 0-3 correction cost -1.99 pts P+ 0.028 with no mechanism) reads +0.80 pts standalone [-0.20, +1.81] P+ 0.934, +0.33 through the played card [-0.60, +1.29] P+ 0.736, Brier/log loss better P+ 0.98; **PLAYED from the Week 1 lock (12:20 ET, after the pool's noon spread lock)** (`HOME_SIDE_OFFSET_BUCKETS`, policy `home_side_offset_big_spreads_v2`); headline evaluation 20260908T115957Z composes to 55.89% (headline 55.9%); Week 1 changes one side back, NE +3.5 at SEA. Recorded `mod18_home_side_location_v1_s3_*`, all `unresolved_below_power`; post-hoc restriction on a mined archive, disclosed. Codex lane F (read-only review of S3 + lanes B/D) found two things, both fixed: the site had paired the S2-built card's sidecar with the S3 evaluation (fixed by regenerating the Week 1 forecast under S3 -- `2026-week-01-20260908T120939Z`, offsets 0 / 0 / -0.17 / +0.78 / +1.92, no side changes vs the S2 card except NE +3.5 staying -- and by the Model page ignoring a sidecar whose policy id is not the served one), and lane S's frozen S2 research replay silently inheriting the restriction (`fit_home_side_offsets(all_buckets=True)` keeps the S2 definition for the research script). **Lane G (Codex, write mode after the push; coordinator verified `artifacts/research/laneG/reproduction.json` -- S3 replay exact to 3.8e-15 -- and `cells.json`, and re-ran its 12 tests + the scanner): S4 = the push fitted per (bucket x home side) in the big buckets, S4b the same with a 50-game prior (predeclared sensitivity), `docs/home_side_side_aware.md`.** Reported through the played card: S4 -0.665 pts [-1.125, -0.264] P+ 0.00025, S4b -0.466 [-0.927, -0.067] P+ 0.0037; standalone 53.83% / 54.29% vs S3's 54.56%; Brier/log loss slightly worse; 0 of 16 Week 1 sides change. Per cell (read from cells.json): 10.5+ home favourites -6.06 [-11.82, -0.98] P+ 0.005 (99 games), the other big cells -0.8 to -4.0 with intervals that reach zero. Decision: KEEP S3. The card read's whole interval sits below zero, which is the admissible `wrong_sign_resolved` ground for the direction "S4 beats S3"; the 144 cells are recorded `unresolved_below_power` (`mod18_home_side_location_v1_s4_*`) and the closing reclassification is left to the next look rather than applied from a lane report. Reading: splitting the offset by side thins each cell to 25-125 games against a 100-game prior, so the per-side fit is mostly prior and the extra parameters cost more than they buy; the location error is a spread-size effect first. **Lane H (Codex, write mode; coordinator verified `artifacts/research/laneH/cells.json` and re-ran its 19 tests + the scanner): M1 = lane K's K1 integer-margin lattice read ONLY on spreads of seven or more, over the S3 point (M1b: K3 side-of-key-number conditioning), `docs/big_spread_lattice.md`.** Standalone M1 +0.60 pts [-0.86, +2.08] P+ 0.778 (55.16% vs 54.56%, 157 picks change), M1b +0.33 P+ 0.641; THROUGH the played card M1 -0.13 [-1.36, +1.08] P+ 0.401 and M1b -0.27 [-1.72, +1.20] P+ 0.346; Brier/log loss slightly worse (P+ 0.28-0.41); push rate at |line| 3 still 3.5% vs 10.1% realised (the recentred lattice still does not put the mass back on 3 -- MOD-05's constraint stands); Week 1: only DET -7 -> NO +7 through the card. Decision: keep the smooth read (S3 card). Reading: the lattice's 157 standalone flips overlap the overlay members' flips, so a standalone gain of +0.60 becomes -0.13 composed -- another instance of "composition is not the signal"; 98 cells recorded `mod18_conditional_margin_v1_m1_*`, all `unresolved_below_power`. **Lane I (Codex, write mode; coordinator verified `artifacts/research/laneI/cells.json`, re-ran its 15 tests + the scanner): S5 = S3 with the shrinkage prior at 50 / 25 / 200 games instead of 100 (predeclared two-sided sensitivity, decision rule fixed before scoring), `docs/home_side_prior.md`.** Through the played card every variant loses: 50 -> -0.40 pts [-0.87, 0.00] P+ 0.025; 25 -> -0.47 [-0.99, 0.00] P+ 0.023; 200 -> -0.53 [-0.94, -0.13] P+ 0.001; standalone 0.00 / -0.07 / -0.53; no Week 1 side changes under any prior. Decision: keep the 100-game prior (declared by lane S before scoring, so it is not a tuned value, but the archive it is now measured on is the one S2/S3 were selected on). 144 cells recorded `mod18_home_side_location_v1_s5_*`, all `unresolved_below_power`. **Where MOD-18 stands after the day's five lanes:** S3 is served; the three neighbouring shapes (side split, key-number lattice on big spreads, shrinkage) all lose THROUGH the card while two of them gain standalone; the served rule's remaining weak spot is 10.5+ (47.3% right); the 2026 paired rows (`home_side_offset_off_incumbent`) are the next evidence, at no window cost. **Afternoon fleet, same day (owner: up to six lanes):** **Lane L (Fable, diagnosis; coordinator re-ran its 5 tests + scanner; `docs/big_spread_diagnosis.md`):** the 10.5+ hole is the ROAD picks -- 37.0% [25.5, 49.1] on 54 games (home beats the served point there by +4.63 [+1.29, +7.89] P+ 1.00) against 54.5% on 77 home picks; it concentrates on 13-14 lines (34.1%, n 41) and when the market moves against the pick by close (27.3%, n 33); confident picks (44.7% on 103) lose, marginal ones (57%) do not; and the slope of the actual margin-vs-opener on the served residual is +0.5 in 3.5-6.5 and 7.5-10 but **-0.99 [-1.98, +0.02] P+ 0.03 at 10.5+** -- the ridge's disagreement with big lines points the wrong way. Two phenomena: one smooth spread-size location error shared by 7.5-10 and 10.5+ (raw ~0 at 9 -> +3.6 at 13-14, both sides) and a 10.5+-only accuracy collapse. Three predeclarable mechanisms named (walk-forward per-bucket residual slope; post-lock movement against the pick into the late-week refresh; offset under-sized for derivable reasons), none a flip; lane R launched on the first. **Lane O (Codex, write mode; college replication, `docs/cfb_home_side_replication.md`; coordinator ran its 5 tests + scanner and merged its 1,800 registry cells `mod18_home_side_location_cfb_v1_*`, all `unresolved_below_power`):** on 11,989 out-of-time CFB games 2007-2025 (close-proxy line, the CFB benchmark's own model), on the predeclared clean core (2012-2019 plus 2021-2025) the 10.5+ home point error is +0.27 [-0.20, +0.73] P+ 0.87 (4,074 games; home favourites +0.25 P+ 0.80, home underdogs +0.32 P+ 0.75; 2021-2025 home underdogs +1.65 P+ 0.99); on the full 2007-2025 sample it is +0.12 [-0.29, +0.53] P+ 0.73 (5,381 games) with home underdogs -0.22 -- the same sign as the NFL's +2.9 at a tenth of the size; S3 transplanted +0.11 pts [-0.28, +0.50] P+ 0.70, the all-bucket S2 +0.31 [-0.28, +0.91] P+ 0.845 on the clean core. Reading: college supports the direction of the mechanism weakly and mostly in recent seasons; it does not reproduce the NFL magnitude, and the CFB lines are close-proxies, not openers. **Lane N (Sonnet, `scripts/officials_coverage_report.py`, `docs/officials_coverage.md`; 32 tests):** the officials Wayback archive covers 2009-2011 at 96.9-99.2% of games with a complete seven-person crew, 2012 and 2014 at 99.6%, 2013 at 29.7% and rising (sweep still running); 2012 shows 34 distinct referees vs 16-20 elsewhere (inferred: the replacement-referee lockout); NO consumer reads this archive yet -- the crew-tilt features read the separate nflverse officials feed (2015-2025), so extending them back to 2009 is the next engineering step (LEAD-59). **Registry integrity (measured):** two lanes recording at once corrupted `registry/weak_signals.json` (the atomic writer used ONE shared temp name, so one writer's bytes landed over another's: a valid document followed by the tail of a longer one); repaired by truncation to the valid document, then fixed for good: per-writer temp names, a Windows-safe replace retry, and a `file_lock` held around load->record->save in every registry CLI command (`nfl_ats.io.file_lock`; tests pin concurrent writers and six concurrent record commands landing without loss). **Lane K (Opus, write mode; coordinator re-ran its 13 tests + scanner and read `artifacts/research/laneK/cells.json`; `docs/mass_preserving_lattice.md`): the owner's multimodal proposal done without moving the atoms** -- MP1 = the empirical integer-margin distribution for prior games with lines in a band around L, atoms kept at their ABSOLUTE positions, the model's information entering as an exponential tilt solved so the tilted mean equals the served S3 point (MP1b: a different band width). It is the first arm in the family that reproduces the key-number mass: at |line| = 3 (199 games, realised push 10.05%) MP1 predicts 9.10% where S3 says 3.50% (lane H's M1 3.50%, lane K's K2 4.1%); push Brier at 3 +0.0037 [-0.0005, +0.0083] P+ 0.955 (MP1b +0.0034, whole interval above zero, P+ 0.980); overall Brier/log loss lean better (P+ 0.54-0.57). But for the SIDE it loses: through the played card MP1 -0.40 pts [-2.09, +1.32] P+ 0.311, MP1b -1.40 [-3.00, +0.20] P+ 0.037; the whole standalone gain sits in the "7" bucket (51.4% -> 59.5%, lines quoted exactly on an atom) and is paid back at 3.5-6.5 (lines between atoms). Reading: fixing the push/atom mass is a calibration gain that takes probability from both sides roughly equally, so it moves push and alternative-line answers, not which side to pick. Decision: keep S3's smooth read for the pick; next, by mechanism rather than threshold, serve the mass-preserving read only for push / flip-line / alternative-line answers (where it is measurably better) and test MP1 restricted to lines quoted exactly on 3 / 7 / 10 / 14. 104 cells `mod18_conditional_margin_v1_mp1_*` recorded `unresolved_below_power` (replayed serially after the registry fix; 21 of lane K's first 36 rows had been lost to the race). Week 1: MP1 would change 4 sides through the card, all near coin flips. **Lane R (Opus, write mode; coordinator re-ran its 7 tests + scanner, read `artifacts/research/laneR/cells.json`, replayed its 192 cells `mod18_home_side_location_v1_r1_*` serially; `docs/residual_slope.md`): the residual re-scaled per spread bucket by a walk-forward slope** (beta_b shrunk toward 1 with a 100-game prior; R1b one beta for 7+). Decision: keep S3 -- through the played card R1 -0.73 pts [-1.93, +0.40] P+ 0.099, R1b -1.20 [-2.45, 0.00] P+ 0.024; standalone -0.80 / -1.20. The MECHANISM holds: the positive control passes (walk-forward 3.5-6.5 slope +0.57 [-0.13, +1.22] P+ 0.944 on 468 prior games), lane L's 10.5+ slope reproduces out of time (-1.17 [-2.23, -0.11], whole interval below zero), and where it was diagnosed it works (10.5+ standalone +2.29 P+ 0.745; 10.5+ ROAD picks +7.41 P+ 0.834 on 54 games) while both arms improve Brier and log loss (R1 +0.0010 P+ 0.945 and +0.0022 P+ 0.954; R1b +0.0013 / +0.0028, P+ 0.96-0.97). It pays in 7.5-10 (-3.61 P+ 0.027): the 100-game prior, borrowed from a MEAN estimator, is far too weak for a SLOPE (7.5-10's walk-forward beta wandered -1.89 -> ~0 -> +0.91). Two predeclarable follow-ups (empirical-Bayes shrinkage on the slope's own variance; the re-scale only at 10.5+, disclosed as post-hoc) -> lane U. Week 1: R1 would change 3 sides (ARI at LAC, NE at SEA, NYJ at TEN), not played. **Lane Q (Sonnet; `scripts/backfill_plain_summaries.py`, `docs/plain_summary_backfill.md`, 116 tests):** a template-driven backfill that writes reader-facing summaries onto registry rows recorded without `--plain-summary`, through the registry CLI's own `--replace` path; applied to the 1,798 college cells. Incident, disclosed: an early version of its tests called the registry CLI in-process without pinning `NFL_ATS_REGISTRY_DIR` and overwrote three live rows with fixture values; the coordinator restored them (one from git HEAD, two from lane O's isolated registry) and the tool now forces the registry directory around every in-process CLI call and asserts it before writing. Rule for every future lane: a test that calls the registry CLI in-process must pin `NFL_ATS_REGISTRY_DIR` to a scratch directory. **Lane T (Opus, write mode; coordinator re-ran its 18 tests + scanner, read `artifacts/research/laneT/cells.json` and `week1.json`, replayed its 88 cells `mod18_conditional_margin_v1_kl1_*`; `docs/key_line_lattice.md`): lane K's discrete read served for the PICK only where the opener line is quoted exactly on a key number.** KL1 (3/7/10/14) +0.13 pts through the played card [-0.40, +0.67] P+ 0.646; **KL1b (3 and 7 only) +0.20 [-0.27, +0.73] P+ 0.743** (55.89% -> 56.09%, 272 games touched, 17 card flips); standalone +0.20 P+ 0.688; the 7 atom carries it (52.9% -> 61.4% standalone on the 70 decided games quoted exactly at 7, +2.86 through the card), the 3 atom costs standalone and returns it through the card; push at a line of 3 predicted 9.1% vs 10.05% realised (S3: 3.5%); gates exact (S3 replay 3.8e-15, lane K's read reproduced to 0.0, untouched games identical). Same read served on EVERY game (lane K) loses the card (-0.40, P+ 0.31): the mechanism -- the mass point matters when the line sits ON it -- is what shows up. Disclosed: post-hoc atom set chosen after seeing the 7-bucket result (70 of KL1's 283 graded games come from it); the next arm declares the atom set out of sample (fix {3,7} on 2020-23, grade 2024-26). **Decision (EV rule): KL1b is to be PLAYED.** Week 1 would change one side, NO at DET: DET -7 (0.516) -> NO +7 (0.472). Engineering: lane S is moving the discrete read into src for the push/flip-line answers (the owner's binding rule); the key-line pick override, its sidecar and the paired challenger (`key_line_read_off_incumbent`) go in on top of that AFTER the 12:20 lock, with every refit module aligned, then the card is republished; the Tuesday ledger row for NO/DET carries the S3 side, disclosed. **Lane U (Opus, write mode; coordinator re-ran its 9 tests + scanner, read `artifacts/research/laneU/cells.json`, replayed its 192 cells `mod18_home_side_location_v1_r2_*`; `docs/residual_slope_shrunk.md`):** lane R's diagnosis of its own loss was wrong -- an empirical-Bayes slope weight (R2) moved the served betas materially and the card read did not move at all (-0.73 pts P+ 0.104 vs R1's -0.73 P+ 0.099); the loss is the BUCKET SET: 7.5-10 bleeds under both weightings (card -4.12 [-8.29, -0.50] P+ 0.011; its road picks -8.04 P+ 0.005) because its full-window slope is already near 1. R2b (the re-scale at 10.5+ only) removes the whole loss: card 0.00 [-0.40, +0.40] P+ 0.434, standalone +0.20 [-0.33, +0.73] P+ 0.736, Brier/log loss better (P+ 0.76-0.89); at 10.5+ R2 reads +3.05 standalone P+ 0.834 and +0.76 through the card P+ 0.574 -- the best card reading at 10.5+ either lane produced. Decision: keep S3 (the predeclared decision grade is the card, 0.434); the 10.5+ re-scale is the live candidate for the next predeclaration, out of sample (declare on 2020-23, grade 2024-26), and R2b would change exactly one Week 1 side (ARI at LAC). Seven of 192 cells exclude zero (fewer than chance); three meet the letter of `wrong_sign_resolved` but were left unresolved on multiplicity -- the names are in the lane report for a human look. **Lane S (Fable, patch built in a worktree; coordinator applied it, ran 149 touched-suite tests + the lock rehearsal (42/0) + an isolated chain proof of the lock's own margin-predict argv in a scratch artifacts root; `docs/discrete_push_read.md`):** lane K's mass-preserving read now lives in `nfl_ats.mass_preserving_lattice` (walk-forward prior pool on archived opener lines, 2.5-point band widened to a 200-game floor, absolute integer atoms, tilt solved to the served S3 point; `discrete_read(...)` per game and `discrete_reads_for_frame(...)`), and behind `DISCRETE_PUSH_READ_SERVED = True` every served PUSH probability and cover/push/loss answer (card columns, the line sweep at every alternative line, the spread explorer's three-way split) comes from it, while the pick-deciding two-way cover probability stays on the smooth gaussian_median read -- the owner's binding rule (AGENTS.md, margins are multimodal) now holds for the push and alternative-line answers. Both reads per game are written to `discrete_push_read.json` beside the card. Chain proof (measured): pick probabilities identical to the live card to 0.0 on all 16 games, 0 sides changed; Week 1 push mass DEN at KC (line 3) 7.9% (smooth 3.7%), NO at DET (line 7) 3.5%; whole-number lines gain one card sentence ("the line sits right on 3, a number games land on a lot ... a push, and that chance is counted here"). The key-line PICK override (lane T's KL1b) is the next step on top of this module. Codex lane X reviewed the served read before the lock and found two things, both fixed and pinned: a per-game read failure (an exceptional line with no prior games in any band) would have aborted margin-predict -- scoring and the line sweep now fall back to the smooth read with the error recorded in the sidecar; and alternative-line answers re-selected the prior band per hypothetical line, so a harder line could show a higher cover chance -- the distribution is now conditioned on the game's quoted line and only the settlement threshold moves (chain proof: 0 of 16 games with cover rising in the line). Leakage, the pick invariant, refit reproduction, banned tokens and determinism: no findings. **Lane V (Opus; coordinator re-ran its 26 tests + scanner, read `artifacts/research/laneV/cells.json`, replayed its 66 cells `..._oos1_*`; `docs/out_of_sample_declaration.md`): the two live candidates declared OUT OF SAMPLE with mechanical rules** (atoms whose standalone delta is positive on the declaration seasons; buckets whose walk-forward slope interval lies wholly below +0.5), graded only on held-out seasons, through the played card: the key-line read **+0.56 pts [-0.19, +1.33] P+ 0.880** on 2024-25 (533 graded, declared on 2020-23, the rule picked {7}) and **+0.38 [-0.50, +1.25] P+ 0.761** on 2023-25 (799 graded, declared on 2020-22, the rule picked {3, 7}); the 10.5+ re-scale -0.19 P+ 0.18 and 0.00 P+ 0.42 (the rule picked exactly {10.5+} both times, so lane U's bucket carried no selection discount); the two together add nothing over the key-line read alone. Decision confirmed: serve the key-line read (KL1b); keep the re-scale off; both cuts share declaration seasons and their held-out blocks overlap on 2024-25, so they are two readings of nearly the same football, not two replications; every interval crosses zero, which closes nothing. **Codex lane Y (read-only review of today's board changes; five findings, all applied):** the Findings entry's literal 55.9% / 55.2% replaced by prose that points at the Model page's live number (the no-stale-numbers rule); the provenance fine print's stamp regexes now accept microsecond artifact stamps (a composition run's name had leaked onto the Model page); a card whose push sidecar is from an earlier policy now says "this week's push is unavailable" rather than "no push"; the weak-spots lead sentences say the push is in the record only when the evaluation measured it; research identifiers are gone from the Model page's evidence pills and the watching-leads rows ("Supporting comparison", "Open question" with the plain summary). Time handling, schedule mirror (pinned to SCHEDULE), season-record switch, Python/JS parity, markup: no findings. **Codex lane AA (read-only audit of every number written today against the artifacts; 23 checks):** 16 confirmed, the wrong ones corrected in this row (the 7-atom count 70 not 74; the CFB +0.27 is the clean core, the full sample reads +0.12; lane L's road-pick interval lower end 25.5; lane H's P+ range 0.28-0.41; lane R's log loss +0.0022 and R1b's own pair; lane V's held-out overlap is 2024-25). **Its consequential finding: the 09:00 ET odds capture still ran today** -- a legacy Windows Task Scheduler entry `NFLATS\Odds_TueOpen` (documented as retired) fired at 09:00:00, and since the live opener is each book's EARLIEST Tuesday quote, the 12:20 lock would have formed the card on 09:00 lines. Fixed before the lock: the task disabled (the other legacy NFLATS odds tasks and the PublicBetting tasks still exist and duplicate the daemon's captures at their old times -- to retire or keep as backup, owner's call), the 09:00 snapshot quarantined to `data/market/raw_early_tuesday/` (kept for the 09:00-to-noon gap measurement), and lane AB is making the live opener rule itself robust: the earliest quote AT OR AFTER the pool's noon lock when one exists, the earliest Tuesday quote only as a labelled fallback. Also noted by the audit: the chain-proof numbers for the discrete push read live in the session's scratch root (`chain_proof/artifacts/margin_predictions/2026-week-01-20260908T143736Z`), not under the repo's artifacts, and the linked forecast built before lane S has no discrete sidecar -- the lock's forecast is the first served one. **Lane W (Fable, patch built in a worktree and rebased; coordinator applied it at 11:06 ET, registered the paired challenger `key_line_pick_read_off_incumbent` (recipe fingerprint identical to the home-side incumbent's), added its publish result key, measured 47 CLI/lock-contract/key-line tests + 234 touched-suite tests green, rehearsal 43 paths / 0 errors, and an isolated chain proof of the lock's own margin-predict argv; `docs/key_line_pick_read.md`): KL1b IS SERVED from the 12:20 lock.** `nfl_ats.key_line_pick_read` (`KEY_LINE_PICK_READ_SERVED = True`, policy `key_line_pick_read_v1`, `KEY_LINE_ATOMS = (3.0, 7.0)`, exact-match atoms so half-points never touch): on games whose opener line sits exactly on 3 or 7 the pick-deciding cover probability is lane K's discrete read at the offset-corrected served point -- the decision number is cover + push/2, which lane W measured to be lane T's arm exactly (gap 0.0 on all 1,537 rows; the brief's "renormalised" wording was wrong) -- everywhere else the smooth read; `margin-predict` writes `key_line_pick_read.json` (both reads per touched game) and a metadata block; every refit path (late-week refresh, `card_refit`, spread explorer, the three mapping recorders, the board's guard) reproduces the served number and is byte-identical on a pre-promotion card; the paired challenger records the smooth read verbatim from the sidecar. Chain proof (measured): sidecars present, exactly one Week 1 side changes -- NO at DET, DET -7 (0.516) -> **NO +7 (0.472)** -- and DEN at KC (line 3) stays KC at 0.515; the other 14 games move by 0.0. Reader text on a touched game: "the line sits right on 7, a number games land on a lot, so this pick is read off how games with lines like this actually finished". Codex lane AC reviewed lanes W/AB/Z before the lock; three of its four findings fixed in the tree the lock runs on: the era-weighted recorder's reproduction check now compares the number the card refit returned (it would have refused every card with a key-line pick); candidate arms in the own-arm challengers no longer inherit the served pick override (their own probability is what gets recorded); `tuesday_opener_quotes` restricted to the game's own Tuesday, pregame (an earlier week's post-lock quote had been standing in). The fourth -- the Wednesday refresh mixing a discrete pick probability with the model's smooth three-way split -- was fixed the same hour by Fable lane AE (patch applied before the lock; 133 refresh/key-line/discrete tests + the full suite green): the late-week refresh now rebuilds the discrete three-way split at the frozen Tuesday line before applying the key-line pick read, exactly in margin-predict's order, restores both from the sidecars on the fallback path, and leaves a pre-promotion card byte-identical; the atom test is declared as keyed to the frozen Tuesday line. Left as is, disclosed: five pre-existing registry rows (mod17 joint residual, movement-rule chain, two Best Pick composed rows, an NFL.com refresh row) carry "raw model" in their recorded text. **Own-arm refit challengers aligned the same morning (Codex lane B, gpt-6-astra low effort, read-only patch; coordinator applied it minus the Best Pick nomination hunk -- that refit is a served decision and stays as measured -- fixed its fixture and measured 111 tests green):** new `nfl_ats.card_refit.load_card_refit` replays the card's recorded offset (metadata first, sidecar second, never a refit) and the card's recorded probability mapping in the incumbent arms of expected lineup loss, trade-deadline drag, the QB-revenge stack and the era-weighted half-life overlay; a card without the promotion keeps the historical refit bit-for-bit, with the warning carried in the recorder result. `inactives_refresh` was already aligned through `plan_refresh`; `pool-card-at-lines` is a standalone command and stays independent. **Lane T result (Fable, concurrent with S; coordinator measured 90 registry rows under `mod18_home_side_mapping_v1` and re-ran its tests):** the probability SHAPE conditioned on the home side, two frozen arms on the unchanged point -- T1 a home-side x spread-band centre shift of the smooth read (shrunken prior-game residual means, 100-game prior) -1.46 pts [-3.27, +0.33] P+ 0.053 at the opener, -1.40 P+ 0.028 through the card; T2 lane K's integer-margin lattice conditioned on the home side -2.59 [-5.65, +0.54] P+ 0.049, -1.26 P+ 0.148 through the card; Brier/log loss worse for both. The diagnostic answer: T2 removes the 7.5-10 home-underdog gap entirely (+14.5 pp -> -0.2 pp, right 46% -> 58%) but pays at 0-3 home favourites (-0.2 -> -5.4 pp) and 3.5-6.5 home underdogs (+1.6 -> -4.5 pp), the same trade K1 made; T1 barely reaches the gap (about 46 trailing rows against the 100-game prior). Push rate at |line| 3 holds (8.8% vs 10.1%). Incumbent mapping kept; nothing closed; three windows spent unresolved. Together with lane S this says the big-spread weakness is a LOCATION problem the mapping cannot fix without breaking the small-spread bands, which is why S2 (location, mapping unchanged) is the arm that gets played.; lane P puts the home/away split on the Model page's weak-spots table. **C2 is now the live candidate and it is OPEN, not tried: price the served cover probability off the discrete conditional distribution at EVERY half-point line, not only where the line equals an atom** (predeclared with its grading in `docs/key_line_pick_read.md`, "TODO, predeclared"). Why it moved up (measured 2026-09-08 on 4,431 completed regular-season games, `data/processed/game_features.parquet` 2009-2025): the owner's pool posts every line as a half point, nine of this week's sixteen within half a point of 3, and against a fitted normal the empirical cover rate is -2.14 pts at 2.5, +2.95 at 3.5, +2.66 at 6.5, +4.49 at 7.5, +3.69 at 10.5 -- opposite signs either side of the 3 atom, understating the 7.81-point cliff across it by 2.87x, because the 14.58% of finals landing exactly on |3| fall wholly on ONE side of a half-point line instead of pushing. The narrow exact-match atom read (lane T, served) therefore cannot fire on the pool's lines, which is recorded as `status: inapplicable` in its sidecar and is NOT a finding against the discrete read: `serve_discrete_three_way` still serves every game and every lane K/S/T cell stays `unresolved_below_power`. **Lane C3 (Opus, 2026-09-10; `docs/spread_size_calibration.md`, `scripts/spread_size_calibration.py`, 240 registry cells `spread_size_calibration_v1_*`, all `unresolved_below_power`): the owner's "calibrate by spread instead of flipping it" done as a CALIBRATION, fitted on 2020-2022 and scored on 2023-2025.** The diagnosis first: it reproduces on the CURRENT active model (`d49194e04945a5e5`, opener evaluation `20260910T211255Z`, 1,503 non-push) -- raw accuracy by bucket 56.2 / 56.2 / 52.7 / **48.5** / **44.3** with stated confidence flat at 55.4-56.6%, the 2026-09-07 numbers to the decimal on a model refitted since. As SERVED (S3) the LOCATION fix has taken 7.5-10 to 51.5% and 10.5+ to 47.3% while the stated confidence moved a tenth of a point, so the CONFIDENCE has never been corrected: a reader on a 10.5+ game is told 55.9% about a pick that lands 47.3%. Nine arms of `logit(p) -> a_b + s_b*logit(p)` per spread bucket (100-game shrinkage to the identity), on lane K's discrete mass-preserving lattice and -- as the challenger AGENTS.md allows -- on the smooth read, graded at the opener on 799 held-out games: the lattice with the correction (A1) -2.00 pts [-5.03, +1.01] `probability_positive` 0.100 vs S3 and -1.88 P+ 0.089 vs the played KL1b, Brier +0.00056 P+ 0.641; the smooth read with the same correction (A3) -1.63 P+ 0.145, Brier +0.00049 P+ 0.636. **The decomposition is the finding:** against the SAME lattice with no correction, the spread-conditional recalibration is worth **+0.876 pts [-0.625, +2.387] P+ 0.871 and +0.00114 Brier P+ 0.895** -- the calibration helps, and it is the LATTICE SIDE READ that loses (A0 alone -2.88 vs S3). Per bucket A1 wins exactly where the owner pointed (10.5+ +5.17 P+ 0.817 with the family's best Brier gain +0.00865 P+ 0.859; lines quoted at 7 +10.81 P+ 0.902 on 37 games) and repays all of it in 3.5-6.5 (-5.86 [-10.47, -1.12] P+ 0.007), where the smooth read is already the best-calibrated thing on the board -- the K1/M1/T2 trade again, now with a chronological fit/score split. The fitted tables name a mechanism rather than a threshold: the intercept runs positive in the big buckets (+0.129 at 10.5+ after S3 has taken its share) and the slope falls away with spread size (0.91 / 0.61 / 0.87 / 0.36 / 0.05, the 7 bucket the exception and the thinnest cell at 37 games), i.e. the model's own confidence ordering carries little information on big spreads and pointed the wrong way at 10.5+ on the fit seasons. **Proposal, not served: A8** -- the scale-only correction fitted on and applied to the read that is PLAYED, with the slope floored at 0.05 so it can never invert a side: **0 of 799 picks changed, accuracy identical (54.944%), Brier +0.00065 [-0.00173, +0.00309] P+ 0.706**, and the held-out 10.5+ stated-minus-realised gap goes -5.1 -> +1.4 (50.3% stated against 51.7% realised); it understates by about five points at 3.5-6.5 and 7.5-10, where the fit seasons put a weak slope on both (0.53 and 0.16). A7 is the same correction on the smooth read (+0.00060 P+ 0.691) and is reported only for comparison. A5-A8 are post-hoc: A0-A4 were the frozen set, and the pick-preserving arms were added after they were scored. Week 1 read-only (nothing regenerated): A1 would change 4 of 16 sides (BUF at HOU, CHI at CAR, NO at DET, TB at CIN, all near coin flips), A7 and A8 zero by construction. Incumbent kept for the PICK on expected value; nothing closed; 15 of 240 cells have a whole interval on one side of zero against roughly 12 expected on nested correlated arms, and the 3.5-6.5 lattice loss meets the letter of `wrong_sign_resolved` for the direction "the lattice beats the smooth read at 3.5-6.5" but is left `unresolved_below_power` for a human look (lane G/U precedent). Open follow-up: wire A8 as one displayed-confidence policy shared by the Model page's weak-spots table and the card, and carry the spread-conditional correction into whichever lane gets the lattice side read right on half-point lines. **Serving lane, 2026-09-10 (docs/spread_size_calibration.md, "What happened when the serving lane tried to wire A8"): A8 was NOT served, because the premise it was scored against had already moved.** Lane AH shipped a displayed-confidence transform hours earlier, so the Decision score is no longer the raw stated probability; measured on the active model's own archive (1,503 non-push, walk-forward for every arm, week-blocked paired bootstrap 20,000 draws seed 20260821) A8 in place of that incumbent is **Brier -0.00049 P+ 0.312 and log loss -0.00625 P+ 0.150**, A8's scale stacked on top is 0.316 / 0.224, and the incumbent stacked on top of A8 is 0.531 / 0.399. Replacing it is taking the 31/69 and the 15/85 side, so the incumbent was kept -- the decision is expected value, and here it points at the incumbent. **What WAS served is the one part of A8 that wins: its never-invert guard**, the property that its floored scale can never carry a probability across 0.5. The incumbent had no such property and printed a score below 50% on **179 of 1,503** picks the card would have made; flooring it is **+0.00030 Brier and +0.00060 log loss, both P+ 0.912**, and takes that count to 0 of 1,503. Disclosed against it: the floor fires in the buckets the owner named, so the bucket-average overstatement widens there (7.5-10 -0.89 -> -1.70, 10.5+ -3.73 -> -4.77, lines at 7 -3.64 -> -4.28) -- a coarse bucket average traded against the two proper scoring rules for the per-game number a reader acts on, both of which favour the floor. Shipped as `displayed_confidence_reliability_v2_pick_side_floor`, plus two guards the brief asked for: the display is fitted **fail-closed** on the opener evaluation matched to the ACTIVE model (a missing or mismatched match now raises instead of falling back to the newest archive on disk), and `attach_displayed_confidence` asserts at runtime that no displayed score lands below 50% on a side the card picks. The Model page's weak-spots table carries a plain-English note saying the stated-confidence column is the model's own number while the score beside each pick is the corrected one, and that it is never shown below 50% on a side being picked. Rehearsed through `nfl-ats publish-predictions`: **0 of 16 Week 1 scores floored, 0 sides changed**, card diff the footnote sentence and the publish stamp only; `nfl-ats publish-board` regenerated the four pages (isolated note contribution +957 bytes model.html, +476 index.html, history and findings byte-identical). Still open: the lattice side read on half-point lines, which is what A8's parent correction is actually waiting on. **2026-09-11, lane P1 (`docs/mod18_pool_shaped_read.md`, family `mod18_pool_shaped_read_v1`; numbers reported by the lane, registry rows counted by the coordinator):** the half-point side read is answered and the premise behind the question was wrong. The archive's decision line is the captured Tuesday-opener consensus, not nflverse `spread_line`, and 842 of its 1,537 games (54.78%) are already exactly a half point — the pool-shaped archive already existed and had never been sliced out. Graded on those 842 games every arm loses through the played card: G2≡G4 -0.594 [-2.334, +1.066] P+ 0.248, G1 -0.713 P+ 0.273, G3 -0.831 P+ 0.199. Structural invariants verified: KL1b touches 0 of 842 pool games and is bit-identical to S3 there; G2≡G4 on POOL. The mechanism does not cross the half point — lane T's +8.571 at exactly 7 becomes -3.846 [-10.417, +2.454] P+ 0.117 on 6.5/7.5 — and the family's entire measured value is the rule already served (G2 vs KL1b off-pool +0.000, 0 flips). Positive control recovered its injected +1.0 as +0.950 [+0.360, +1.620] at SE 0.319 against arm SEs of 0.87-1.21, so it bounds nothing; all 234 cells `unresolved_below_power`. No arm above P+ 0.5, so no challenger registered (declining at P+ 0.20-0.27 is the EV side). Week 1: 16/16 lines half points, KL1b touches 0. Consequence: `key_line_pick_read_off_incumbent` cannot accrue evidence while every pool line is a half point. Next: grade the family at a single book's posted half point from `data/market/raw/*/quotes.parquet` at `tue_open`, which needs an opener evaluation refitted at that line. **Unpredeclared, flagged, not acted on (own family `mod18_overlay_union_by_line_shape_v1`):** the played three-member overlay union reads -1.425 [-3.933, +1.160] P+ 0.136 on half-point lines and +4.841 [+1.506, +8.235] P+ 0.999 off them — its whole measured contribution sits on lines the pool never posts. A predeclared mechanism lane (era/book-count composition of the whole-number subset, push handling, per-season split) was opened the same day; no card change until it names a mechanism. **2026-09-11, lane Q1 — mechanism for the line-shape split (`docs/overlay_union_line_shape.md`, predeclared before measurement; 60 rows in family `overlay_union_line_shape_mechanism_v1`, all `unresolved_below_power`; numbers reported by the lane):** none of composition, push handling or spread magnitude accounts for any of the +5.891-point gap (season × book-count standardisation leaves +5.956, decided-by-a-point grading +6.174, line-size standardisation +6.841). Direction and base rate contribute 23%: the model's own pick is -4.538 [-9.641, +0.500] P+ 0.039 on whole-number lines relative to half-point ones, and against a same-size, same-direction random flip the union is positive on both shapes (+0.763 half, +5.285 whole). The remainder is located in one member — division revenge carries 78% of the split (whole archive +0.732 P+ 0.834; half-point lines alone -1.425 [-3.147, +0.241] P+ 0.049, 78 flips) — but not explained. Both season halves show the gap positive; no closing ground. **Verdict: unexplained threshold, published as a diagnosis; it may not gate overlay membership.** The union stays on the card (+1.331 [-0.727, +3.393] P+ 0.898 on 1,503 games). Successor question is a model diagnosis: why the model is 4.5 accuracy points weaker on whole-number opener lines. **2026-09-11, lane Q2 (`docs/whole_number_line_model_diagnosis.md`, family `whole_number_line_model_diagnosis_v1`, 40 rows; numbers reported by the lane):** the model's 4.5-point weakness on whole-number Tuesday openers is diagnosed and it is NOT the discrete mapping. On the active model `7786467eabe418b8` (evaluation `20260911T161354Z`) the gap is -4.538 [-9.612, +0.484] P+ 0.040 (56.53% on 842 half-point openers, 51.99% on 627 graded whole-number ones). Whole lines exactly on 3 or 7 are the strongest part of that domain (+2.355 [-6.011, +10.488] P+ 0.712) and removing the key numbers makes the gap larger (-6.395); push handling leaves it at -4.25 to -4.79 — the atom's treatment is exonerated. Located instead: (1) the whole-number opener is an away-biased price (home beat it by +0.671 points on average vs -0.031 on half points, and the model leans 0.226 the other way); (2) the model's edge is a book-disagreement edge — 57.96% where the captured books disagreed vs 51.79% where they were unanimous (+6.165 [-0.789, +13.340] P+ 0.958), and the shape gap nearly vanishes among unanimous openers (-1.795 [-13.587, +9.714]); (3) stated confidence is flat across shapes (55.85% vs 55.58%) against 56.53% vs 51.99% realised. Composition, line size, model edge, the home-side offset (which reduces the gap by 1.04) and base rate account for none; 39% of the gap is present in the market's own movement signal and 62% survives regrading at the close. Reliability positive (odd/even-week Pearson +0.692), so nothing closes. Fix direction: grade and fit against a single book's posted half-point line (lane P2 running the same day) and give the served confidence something to vary with. No flip on line shape may touch the card: the pool posts only half points, so every served pick already sits on the stronger shape; the consequence is for the archive-graded headline, which blends two populations of different difficulty. **2026-09-11, lane P2 (`docs/single_book_opener_grade.md`; numbers reported by the lane):** the family was regraded at a line a book actually posted. `nfl-ats opener-evaluation` now takes `--opener-line-source` and `nfl-ats opener-line-series` builds the series from the per-book Tuesday-opener archive; line-source runs land in `artifacts/opener_evaluation_line_source/` and never claim to be the active model's evaluation. Book chosen by coverage before grading: DraftKings (1,597 of 2020-2025 games; a single book posts a whole number on 43% of openers, FanDuel 25%). Headline: the active model scores 53.83% at DraftKings' posted opener vs 54.50% at the cross-book consensus on the same 1,477 paired games (-0.677 points [-1.279, -0.134], P+ 0.009) — the published history is graded on a median of prices and the number a pool settles is about seven tenths lower. Only 20 of 1,521 picks change side; the served home-side offset is worth +0.672 [-0.199, +1.547] P+ 0.939 at the consensus and +0.201 [-0.754, +1.188] P+ 0.657 at a posted book line — the modelling defect this surfaced. KL1b fires on 0 of the 864 half-point DraftKings games; G2 on the 485 games at 2.5/3.5/6.5/7.5 reads -1.443 [-4.366, +1.452] P+ 0.166 through the card, so no challenger registered. 76 cells recorded under `single_book_opener_grade_v1` / `_posthoc_v1`, all `unresolved_below_power`. Open next: refit the home-side offset at a posted half-point line; build a pool-line series from the intraday archive; grade FanDuel. UI-20 candidate: a Model-page number for 'how the model scores against one book's posted opener vs the average across books', sourced from the line-source artifact. |
| MOD-19 | 🚧 | Conditional signal atlas: every signal tested across every split we can name, then decided together | Owner direction 2026-09-13 (paraphrased, clarified the same morning): blanket signals are useful, but every one of them, served tilts, challengers and registry weak signals alike, has so far been measured in a vacuum. The method going forward is to measure each signal's marginal inside conditioning splits across any dimension we can name, not only game windows, and to make the card's decision from the conditional reads. Lane `docs/lanes/conditional-signal-atlas.md`. Deliverables: (1) a split library, predeclared and versioned, starting with time in season, spread size and side, home/away, division, rest and travel, weather and roof, primetime, era, market movement and public share, quarterback and coach change, offensive-line and pass-rush snap continuity, injury load; (2) a harness that, for any registered signal, computes its leave-one-out marginal on the played card inside every split cell with the paired week-blocked bootstrap and records every cell with `probability_positive` under a family named `<signal>__<split>`; (3) a per-signal conditional profile on the findings page in plain English; (4) gated paired challengers that fire a signal only in cells that read positive-expected, graded at the opener. Guard rails: predeclare the family before any sign is seen, record every cell including the unresolved ones, never drop a cell for containing zero, and let the paired challenger, not the atlas, decide what is served. First worked case: LEAD-65 (protection tilt by window-spans-offseason: weeks 1-4 probability_positive 0.977, weeks 5-18 0.099). |

## Phase 7 — simulations

| ID | Status | Item | Definition of done |
|---|---|---|---|
| SIM-04 | 🔬 | Full play-by-play simulator | Play call, outcome, clock, penalty, turnover, fourth-down policy |
| SIM-05 | 🔬 | Counterfactual simulator | Compare coaching decisions, injuries, weather, and matchup changes |
| SIM-06 | 🌙 | Differentiable football environment | Learn policy/state transitions jointly without sacrificing auditability |
| SIM-07 | 🌙 | Multi-agent tactical model | Personnel and scheme interaction below the play level |

Simulation is accepted only if it improves held-out distribution calibration
over simpler margin models. More realism is not automatically more accuracy.

## Phase 8 — paper portfolios and decision science

| ID | Status | Item | Definition of done |
|---|---|---|---|

## Phase 9 — football pools

| ID | Status | Item | Definition of done |
|---|---|---|---|
| POL-04 | ❌ | Pick-popularity input | **Closed for lack of data, not effort** (`docs/pool_format_levers.md`). Splash does show a pick distribution, but it unlocks game-by-game as each game kicks off — a deliberate integrity measure, so it is structurally unavailable before ~~the Tuesday lock~~ **each game's real deadline (owner-corrected 2026-08-20: there is no Tuesday pick lock, only a Tuesday LINE lock; refined 2026-08-20: the real per-game deadline is min(kickoff, Sunday 16:00 ET), SNF/MNF locking early — this row's premise should be re-examined, since our own pick for a later game in the week could in principle be informed by an earlier game's already-unlocked pick distribution; not re-assessed here, flagged only)**, and there is no API. The Odds API sells no betting percentages; no free ticket/handle feed offers an API plus history. The only obtainable proxy is the favourite flag, already on the card — and our picks run 54.8% favourites / 46.7% home, so we sit near-neutral against a favourite-loving field anyway |
| POL-05 | 🚧 | Contest utility optimizer | Simulator built and validated against five closed forms (`nfl_ats.pool`, `tests/test_pool.py`, `scripts/pool_levers.py`); independence measured rather than assumed (weekly variance of the correct share is 1.036× binomial over 107 weeks). Results: the format already multiplies a fair share of first place by 2.3×–12.7× at 52.5%; **deliberate contrarianism loses monotonically at every field size** when it costs the full edge (break-even ~2.5 accuracy points per flip), and under a top-15% prize — Splash's usual structure — the lever is neutral at best. A Best Pick ranker is worth +2.4 pp of P(first) at 100 rivals if the recorded +8.7 were real, and **+0.07 pp at the honest +0.9**. **Two accuracy points are worth +11.8 pp.** The format is a multiplier to protect, not a lever to pull. Open only on two observables — the pool's real field size and prize structure — both recordable in Week 1. **2026-09-03:** still queued for the 9/08 lock week; capture alongside the lock, no earlier. **2026-09-11 re-run on the served numbers (`docs/contest_utility_optimizer.md`, `scripts/contest_utility_optimizer.py`, artifact `artifacts/contest_utility_optimizer/20260911T034605Z/`):** the 2026-08-17 read above was scored against a retired card, an arbitrary Best Pick and a hypothetical field, so it was re-run on the measured inputs — ordinary hit rate **52.96%** (1,503 opener-graded games, 107 weeks, `artifacts/opener_evaluation/20260910T211255Z`), served Sunday Best Pick **56.73%** (104 weeks, LEAD-53), our picks on the favourite 46.7%, a fitted hit-rate curve `logit(p) = 0.2392 - 0.02349|spread|`, and the real 285-pick season shape (272 REG in 18 weeks + 13 playoff). Field size and prize structure remain **uncaptured** (`data/pool_observables/` does not exist), so both are declared assumption grids: entries {26, 101, 285, 1001}, payouts {winner-take-all, top 10/15/25%}, field lean {0.50, 0.55, 0.65, 0.75, 0.85}, Best Pick bonus {+1, +2}. The script's detailed simulator ties out **identical to 1e-12** against `simulate_pool_finish` on a shared seed; positive control (perfect card) P(first) 1.000. **Answer: one deviation raises expected payout and it is small and conditional.** (1) Best Pick weighting: at double points the 18 starred picks carry **21.0% of season score variance** and 12.6% of expected score; the star designation itself adds **15.7%** (exact) / **15.9%** [15.1%, 16.9%] (simulated) — at triple points 37.4% / 33.3% / 33.5% [32.2%, 34.8%]. Five-sixths of the variance is ordinary picks. (2) Star position, 285 entries, lean 0.65, all 285 sides held fixed: the ONLY winner is the **contrarian star** (best pick among the games where we hold the underdog) at **+0.20 pp of P(first), 95% [-0.29, +0.60], `probability_positive` 0.85**, costing 0.07 expected points a season = **+0.035 accuracy-point equivalents**; it is monotone in how herded the field is (P+ 0.40 / 0.60 / 0.85 / 0.90 / 0.95 at lean 0.50 / 0.55 / 0.65 / 0.75 / 0.85) and **reverses as the payout widens** (P(paid) +0.17 pp P+ 0.75 winner-take-all, -0.21 P+ 0.375 top-10%, -0.32 P+ 0.20 top-15%, -0.55 P+ 0.05 top-25%). Every other star deviation loses: big-spread -1.69 pp (P+ 0.05, -0.29 acc-pt eq), max-variance -1.39 pp (0.00, -0.24), contrarian-big -1.29 pp (0.15, -0.22), and an **arbitrary** star -0.66 pp (0.00), so having any chooser is worth **+0.11 acc-pt eq**. Side deviation loses too: 10 flips +0.17 pp P+ 0.65 winner-take-all but -0.57 pp top-15% (0.275); 25 flips -0.10 / -2.31 (0.55 / 0.05); 50 flips -1.25 / -6.56 (0.30 / 0.00). (3) Tiebreaker shade: a tiebreak decides whether we are paid in **1.63%** of seasons winner-take-all and **3.87%** at top-15%; times LEAD-54's measured -0.137 pp [-0.935, +0.661] win-rate change that is **-0.0000051 of P(paid), P+ 0.367 = -0.0004 accuracy-point equivalents** — immaterial to three significant figures at every field size, so the shade stays served on LEAD-54's own metric and contest utility has no opinion. (4) **Payout units, the deliverable:** one point of ordinary forced-pick accuracy is worth **+5.80 pp of P(first)** at 285 entries, 95% [+4.67, +6.37], `probability_positive` **1.000**, +11.85 pp of P(top 15%), and **+6.3 finishing percentiles at EVERY field size** (6.13 / 6.30 / 6.32 / 6.34 at 26 / 101 / 285 / 1001) — the field-size-free exchange rate for pricing a research lane. P(first) at 285 entries runs 1.00% / 4.49% / **8.11%** / 13.91% / 21.67% / 42.84% at 50 / 52 / 53 / 54 / 55 / 57% accuracy. (5, added) **LEAD-53's served Sunday Best Pick re-nomination priced in payout units: +2.08 pp of P(first), 95% [+0.83, +3.31], `probability_positive` 1.000 = +0.36 accuracy-point equivalents** — ten times the star-position lever, and the largest contest-format lever in the project; the retired Tuesday nominator was worth +0.11 pp (P+ 0.60, +0.019 acc-pt eq), reproducing LEAD-53's own finding by a second route. Supporting measurement, the only non-simulated cell: accuracy on spreads ≤ 4.5 (53.92%, 829 games) minus spreads ≥ 7.5 (48.31%, 325) = **+5.61 accuracy points, week-blocked 95% [-0.80, +12.14], P+ 0.9566**, positive in both season halves (odd +8.59, even +2.56). **Instrument warning, measured:** the field model hands a ZERO-edge card 1.54x a fair share of first when its side profile matches the field's and 2.73x at our actual 46.7% favourite share, so absolute P(first) levels are optimistic and only the paired deltas are quoted. **Decision: change nothing about how the card is submitted today.** Sides do not move; the star rule stays max-probability and switches to the contrarian filter **if and only if** the pool proves winner-take-all (the researched Splash default is ~top 15%, where the arm is measured negative), so the switch waits on an observation, not on more data; the tiebreaker shade is untouched. 13 cells recorded `unresolved_below_power` via `nfl-ats weak-signals record`, family `contest_utility`, category `modeling` (registry 6,002 → 6,015) — twelve are simulated payout quantities converted to ordinary-accuracy-point equivalents and are flagged in every record as **not commensurable** with measured game-level `accuracy_points`; nothing closes, because a Monte Carlo interval conditional on an assumed field cannot refute a mechanism. No rotation window spent, no served behaviour changed. **Still open on exactly the same two observables** (`nfl-ats pool-observables`, LEAD-52): the prize structure alone flips the sign of the one positive deviation found. **2026-09-11 hygiene fix:** a new `payout_first_pp` effect unit was added and the twelve simulated payout cells were re-recorded under it via `--replace` (exact figures from `artifacts/contest_utility_optimizer/20260911T034605Z/`, not the accuracy-point conversion), so `nfl-ats weak-signals pool --league nfl --effect-units accuracy_points` no longer includes them (3,063 → 3,051 eligible) and only `contest_utility_spread_accuracy_gradient` remains from this family in that pool. |
| POL-08 | 🔬 | Opponent-field simulation | Simulate standings and strategic picks for winner-take-all pools |
| POL-09 | 🚧 | Best Pick ranker | The pool pays one Best Pick per week and our confidence ordering is flat (top-\|residual\| scored 48.6% over 107 weeks). Three signals were predeclared and screened once on the registry window [2013, 2015] (August 2026, `docs/best_pick_ranker.md`): `calibrated_probability` (−8.16 points vs all-pick, `probability_positive` 0.0925) and `key_number_distance` (−6.19, 0.170) both made the weekly top-1 pick **worse** and are closed; `sweep_robustness` scored +5.57 points at 0.7955, clearing the predeclared 0.75 screen gate. Its earned opener confirmation then ran once on [2020, 2021] with the deployed `player` profile: **top-1 60.0% (21/35) vs 51.32% all-pick, +8.68 points, week-blocked [−7.00, +22.88], `probability_positive` 0.865** — clears the predeclared 0.75 gate; ~~verdict `confirmed`~~ **verdict `unresolved`** (downgraded 2026-08-18, see the re-read below), both windows now spent. Consequence per the predeclaration: **use `sweep_robustness` to choose the Best Pick in 2026** — a pool-play decision, no activation and no model change. Two disjoint windows, two grades, same direction; but 86 top-1 picks total and a rank correlation of +0.067 (p=0.099), so expect regression toward the all-pick rate. **Re-read 2026-08-17 (`docs/pool_format_levers.md`): every recorded figure reproduces exactly, but the confirmation is mostly the TIE-BREAK, not the signal.** `sweep_robustness` is a half-point width censored at 8.0, so weeks routinely tie at the top and `select_best_pick` breaks ties alphabetically by `game_id`. **24 of the 35 confirmation weeks were ties** (39 of 51 on the screen). Scoring the same signal under a random tie-break gives **52.24%, delta +0.92 points — not 60.0% / +8.68**; the recorded value sits at the 96th percentile of the tie-break distribution. On the screen window the tie-break pushed the other way (+9.05 rather than the recorded +5.57), so *direction* survives but the "two windows, same direction, both clearing" argument is partly alphabetical coincidence. Also unmeasurable by construction: top-1 standard error is **8.45 points on 35 weeks**, so anything in [43%, 62%] is indistinguishable from an arbitrary nomination, and resolving a 5-point effect needs ~384 weeks ≈ 21 seasons. **This re-read re-scores nothing, but it DOES change the registry verdict**: `best_pick_ranker_opener`'s `[2020, 2021]` window is downgraded `confirmed` → `unresolved` (2026-08-18; tie-agnostic delta +0.92 points, honest `probability_positive` **0.536-0.554** under D2 widening — never near the predeclared 0.75 gate). Keep using the signal to choose the weekly Best Pick regardless — it is still the right card to play, since both alternatives (`calibrated_probability`, `key_number_distance`) are measured negatives and the pool is forced picks — but budget the edge at +0.9 points, not +8.68. The published card and dashboard now disclose a tie whenever one occurs. **Promotion moved the tie onto the card that is actually played.** 2026 Week 1 on the active `weak_stack` card IS tied: `2026_01_ARI_LAC` and `2026_01_WAS_PHI` both score 8.0, next 5.5, so the published nomination of ARI is the alphabetical `game_id` tie-break rather than a lean (verified 2026-08-18 on `artifacts/margin_predictions/2026-week-01-20260818T013139Z`, filtered to `market_residual`). The retired `player` card was the untied one, at 7.0 with 6.5 next; the earlier note here had the two cards the wrong way round. ~~**The disclosure claim above is true only of the dashboard** — `dashboard/app_pages/picks.py` computes the tie note, `publishing.py` has no tie logic, so the tracked `CURRENT_PREDICTIONS.md` presents an arbitrary nomination as if it were a lean. Being fixed~~ **Fix shipped and tested 2026-08-18** (`docs/week1_readiness.md` §2): the shared computation was extracted into `nfl_ats.best_pick.best_pick_tie_count`/`best_pick_tie_note`, `publishing.py` now threads the tie note through `_forecast_best_pick` → `_publication_context` → `_publication_header`/`_best_pick_note`, and `publish_active_predictions`'s return payload carries `best_pick_tied: bool`. Proven by `test_published_card_discloses_a_tied_best_pick` and `test_published_card_does_not_disclose_an_unambiguous_best_pick` (`tests/test_publishing.py`), and verified against the real, live, tied Week 1 card via a redirected `--destination`/`--readme` run that touched no tracked file. **The tracked card itself, `CURRENT_PREDICTIONS.md`, is deliberately left stale** — republishing it competes with the ledger-contamination decision, so regeneration is deferred pending the `docs/week1_readiness.md` §owner-decision item on the 16 contaminated CLV ledger rows, not done as part of this fix. **2026-08-18 (owner decision): the weekly NOMINATION rule switches to a measured winner** — a same-day screen (`scratchpad/bestpick_opener/`, `scripts/best_pick_opener_ranker_eval.py`) found the alpha=2000 candidate probability's distance from 0.5, restricted to that week's below-median cross-book opener dispersion (fallback to the full week on missing/degenerate data), +3.92 points vs its unfiltered parent, `probability_positive` 0.813, interval [−3.92, +11.76], 102 paired weeks — third reuse of the same 107-week population, still unresolved, but the strongest lean measured in either screen and EV-positive against the now-signal-free `sweep_robustness` incumbent on a forced pick. Sides never change, only which game is nominated. New module `src/nfl_ats/best_pick_nomination.py` (`best_pick.py` stays frozen); the production tie-break composes chooser 6's filter with a SEPARATE chooser's dispersion tie-break, a combination never itself scored (flagged in both the module and `docs/best_pick_ranker.md`). 2026 scores both arms: v1 via the existing `is_best_pick` ledger flag (unchanged), v2 via a new `best_pick_nomination_v2` prospective challenger (`artifacts/prospective/challengers.json`). See `docs/best_pick_ranker.md` § "2026-08-18: the weekly NOMINATION rule switches" **Composed rule scored as played, 2026-09-07 evening (lane U, Fable; coordinator measured the 6 registry rows under `pol09_best_pick_composed_v1` and re-ran its tests; docs/best_pick_composed_rule.md, scripts/best_pick_composed_rule_eval.py):** the never-scored composition (chooser-6 filter + dispersion tie-break) on the same 107-week opener archive (fourth reuse, no window), scored on the ACTIVE model's pick side (the 2026-08-19 v3 audit had scored it on the alpha=2000 candidate arm's side, which differs in 255 of 1,537 games; the candidate-arm row reproduces that audit's 54.37% as a tie-out): **v2 as played 56.31% top-1 (58/103)** vs v1 sweep 55.66% (+0.97 pts [-10.68, +11.65], P+ 0.536; nominee differs in 69 weeks, outcome in 35) and vs raw top-|residual| 52.83% (+3.88 [-6.80, +14.56], P+ 0.729); with the 10+ spread discount 57.28% (+0.97 vs v2 as played [-2.91, +4.85], P+ 0.592, on a 3-2 split of the 5 weeks it changes). The tie-break layer is inert: v2 differs from chooser 6 alone in 2 of 107 weeks; the pool ties in 5 weeks, dispersion differs from alphabetical in exactly 1 (lost). All six cells `unresolved_below_power`. Week 1 reproduces MIA +3.5 through the production module, untied. Decision: the served rule stays; the discount stays the side-ledger challenger it already is (P+ 0.592 on five weeks is not re-plumbed the night before lock). **2026-09-11:** superseded in practice by LEAD-53 (Sunday-morning Best Pick re-nomination, served since 2026-09-10, ranks by the played side's refreshed cover probability at the frozen line) and POL-05's fitted hit-rate curve; this row stays open only for the opener-time ranker comparison (`artifacts/best_pick_opener_ranker`), which has no served consumer. |
| POL-10 | 🚧 | Prospective 2026 evidence | Grade the active model and every frozen challenger on games nobody has looked at, at BOTH grades (the recorded/opener line primary, close secondary). Built 2026-08-17, three weeks before Week 1 locks (`docs/prospective_evidence.md`), because prospective scoring is the only way to settle `mod07_weak_signal_stack` (unresolved, P+ 0.8745) and firm up `best_pick_ranker` (60.0% on 35 top-1 picks) **without spending either of the two remaining opener windows** -- and it only produces evidence if picks are recorded before kickoff every week from Week 1, so a season that goes unrecorded is simply gone. Three gaps closed: nothing computed whether a 2026 pick WON (the MKT-04 ledger scored line movement only, never joining `result`); the weekly Best Pick was recomputed at render time and overwritten every publish, so Week 1's nomination would have ceased to exist once Week 2 published; and the MOD-07 stack was not registered, so no challenger card was being produced at all. Now: `nfl-ats prospective-score` settles at both grades reusing `clv.pick_correct` so FND-04 push semantics are literally the same function; `is_best_pick` persists in `PAPER_DECISION_COLUMNS`, written only while EVERY game of the week is still ahead, first-write-wins, exactly one per week enforced on read; challengers resolve by configuration fingerprint rather than directory recency (the baseline and challenger share an artifacts directory, so newest-wins would have silently recorded the baseline's picks as the challenger's); and `weekly-run` gained four optional trailing steps whose failure never aborts the fail-closed publish. **Anti-backdating is enforced twice** -- refused at write, and re-asserted at scoring, so a hand-written row cannot be laundered into evidence by running the scorer. A Week 1 challenger card was generated as a rehearsal and the two arms disagreed on 3 of 16 games. **The rehearsal rows were then reset (2026-08-17)** so that Week 1's first ledger write is the real Tuesday-lock card on 2026-09-08 — the MKT-04 ledger anchors each game at its FIRST publication, which would otherwise have scored August's picks at August's lines instead of what was actually entered. **2026-08-19: the challenger ledger grew to 8 ACTIVE_PROSPECTIVE entries** — `mod07_weak_signal_stack`, `hc_year_one_fade_overlay`, `best_pick_nomination_v2`, plus five new frozen pick-level overlays built this session at zero window cost: `injury_value_lost_tilt_overlay` (parameter-free sign tilt on the registry's strongest lean, +1.32 pts P+ 0.8875), `division_revenge_tilt_overlay` (both grades lean the revenge side, P+ 0.88/0.86), `backup_qb_fade_overlay` (backup side under-covers at both grades, P+ 0.17/0.10), `surface_switch_tilt_overlay` (cross-league-replicated surface lead, see ENV-02), and `spread_gap_zone_fade_overlay` (model picks at |line| 7.5-10 hit 46-48% on three separate windows incl. a never-mined 2011-2017 replication at P+ 0.914; bounds frozen before the replication). Each is tracked independently against the un-flipped active card in `_cmd_publish_predictions` (8 additive fail-open blocks); none touches the production pick path; all eight ride the same **`--record-decisions` requirement on the Sep 8 lock-day run**. **2026-08-20: challenger ledger grew from 11 active to 16 ACTIVE_PROSPECTIVE (19 entries total; measured, `artifacts/prospective/challengers.json`).** Four new this session, all dual-tracked, no-window-cost: `interim_hc_first_game_tilt_overlay` (see PER-07), `forecast_weather_kn_warm_team_cold_late_tilt` and `forecast_weather_kn_precip_high_total_tilt` (see ENV-01), `injury_signal_refresh_tilt` (below). The Sep 8 lock-day run must adjudicate all 16, not the 8 the previous session's note describes. **Two research docs sharpened WHERE the already-wired observed-movement policy's prospective edge concentrates** (`docs/movement_attribution.md`, `docs/opener_error_analysis.md`), building on `docs/observed_movement_channel.md`'s production `refresh-picks`/Sunday-realism rule (committed `9d70817`, itself not yet described in this file's phase tables -- flagged, not backfilled here). Attribution: of 494 games where Tuesday-to-close movement disagreed with the model's opener pick, injury news explains the largest share -- following the market on the 123 games with a pregame skill-position injury asymmetry AND `\|open_move\| >= 1.0` is worth **+17.07 accuracy points, week-blocked 95% [+0.79, +31.67], P+ 0.976** (a correlated decomposition of the already-recorded `observed_movement_threshold_1_0` entry, not independently poolable with it). Weather-attributed moves lean the OPPOSITE way (P+ 0.11-0.19, thin n=24-39). About 42-48% of the disagreement GAMES show no visible cause in these three archives, but those games carry a smaller share of the total flip VALUE, not half of it -- value-weighted, the unattributed share is ~19% unfiltered and ~36% at the threshold cut, where it still scores positive rather than zero (+8.20 pts, P+ 0.797, n=122) -- the market knows things outside these three archives too, just less of it than a naive game-count split would suggest. `injury_signal_refresh_tilt` operationalizes the injury-attribution finding as a live challenger reading Wed/Thu/Fri practice-status filings. Separately, a 12-family mined battery of the active model's own opener-grade errors (`docs/opener_error_analysis.md`, 24 cells recorded) found and CORRECTED a label-swap bug: an apparent contrarian "fade the market when it confirms our pick" lead was backwards -- read correctly, the model does BETTER when observed movement agrees with its pick (+3.60 pts) and WORSE when it disagrees (-5.99 pts, whole week-blocked CI below zero), which is the *same* mechanism the production movement policy already exploits, not a new contrarian overlay; the corrected finding sharpens where that policy's edge concentrates rather than adding a new one. The battery's strongest surviving lead is a big-spread dampener: the model loses accuracy at opener spreads of 10+ points (-7.91 pts, whole week-blocked CI below zero, P+ 0.020) and 7-9.5 (-4.38 pts), while the two smallest spread buckets are both positive -- a candidate Best-Pick-eligibility discount. **2026-09-03 correction:** the "not yet built" above is stale — the discount was built 2026-08-20 as the `best_pick_big_spread_eligibility` side-ledger challenger (`docs/best_pick_big_spread_challenger.md`, `ACTIVE_PROSPECTIVE`, 10+ spreads excluded from v2's pool with fallback; Week 1 stays MIA +3.5 under both rules). **2026-09-03 readiness:** lockday rehearsal PASS (29 active, 0 errors), scheduler daemon alive, refresh path fails closed pre-lock. First genuine rows land at the 9/08 lock. **2026-09-11: the first prospective grades exist and are on the site.** Measured this session with `nfl-ats prospective-score` after fixing its outcome source (it had been reading only the processed feature table, which has no 2026 `result`, so every entrant reported 0 settled; it now overlays `artifacts/settlement/game_results.parquet`): on the two finished Week 1 games (SEA 13 NE 10, SF 27 LA 7) the active model is 2-0 at the decision line, the base model without pick overlays 2-0, and among 39 challenger arms 0 are ahead of the card, 23 match it pick for pick and 8 are behind -- worst `movement_rule_composed_v1` at 0-2, then seven at 1-1 or 0-1-1. 14 of 16 recorded picks are still pending, so nothing here resolves anything; it is the first measurement, not a verdict. Every arm's 2026 record now shows under its name in the Model page ledger, and the History page carries the same numbers in "This season so far". **2026-09-11 first settled games (measured):** `settle_thu` re-run by hand — 965 graded rows across 72 arms, 104 settled, 861 pending. Both played games graded at every ledger: NE at SEA (SEA by 3 at NE +3.5, card won) and SF at LA (SF by 20 at SF +3.5, card won); every ACTIVE_PROSPECTIVE challenger has a graded row for both. One grading caveat to carry: five overlay arms (`hc_year_one_fade_overlay`, `injury_value_lost_tilt_overlay`, `division_revenge_tilt_overlay`, `surface_switch_tilt_overlay`, `spread_gap_zone_fade_overlay`) were recorded 2026-09-08 16:35 UTC at NE +3.0, before the pool posted +3.5, so their NE at SEA rows grade as a push where the pool's line is a win; the rows are post-kickoff and `--replace-week` leaves them, so their Week 1 tallies carry a one-game line mismatch (owner decision from 2026-09-09 still pending). **2026-09-11 (LEAD-29 lane):** challenger set grew with `interim_playcaller_first_game_back_overlay` — 14 Week 1 rows, all mirrors of the served pick. |
| POL-11 | 🚧 | Observed-movement late-week pick refresh | **Built and wired live 2026-08-20** (`docs/late_week_refresh.md`, `src/nfl_ats/pick_refresh.py`, CLI `nfl-ats refresh-picks`) -- a production feature with no prior row in this file, flagged rather than silently added by two independent passes this session. Owner correction 2026-08-20: pool picks are editable up to each game's own kickoff; only the grading LINE freezes at the Tuesday-noon lock -- a previously-unspent structural edge, since a Friday injury designation, a kickoff-nearest weather forecast, or simply a fresher model run can inform a pick the frozen Tuesday line never had a chance to price. Four named refresh passes (Thursday pre-TNF, Saturday, Sunday morning before 4:00pm ET) recompute the active model's probability with current features but always GRADE against the original Tuesday card (read from the paper-decision ledger, never rewritten in place); the real per-game deadline is `min(kickoff, that week's Sunday 16:00 ET)`, so SNF/MNF picks lock early with the rest of the week. One market-based decision rule IS applied to the actual played pick, distinct from every other pick-level overlay (those stay challenger-tracked only, never touching the real pick): if the currently-captured line has moved `>=1.0` point from the frozen Tuesday line, the refreshed pick follows the market's side; otherwise the model's own recomputed pick stands. **The evidence base** (`docs/observed_movement_channel.md`, 6 predeclared cells, week-blocked bootstrap seed 20260819, paired against the production pick on the same 1,503-game opener archive): `observed_movement_threshold_1_0` (Tuesday-to-close, full slate) +1.863 pts, 95% [-0.469, +4.267], P+ 0.935; `observed_movement_oracle_full_slate` +1.730 pts, [-1.110, +4.564], P+ 0.883; the deadline-respecting Sunday-morning-realism variant (2023-2025, n=799 -- a measured LOWER bound, since the `intraday_hourly` archive's real coverage ceiling is ~10:55 ET Sunday, well before the true 16:00 ET deadline) reads stronger: `observed_movement_threshold_1_0_sunday_am_realism` **+3.254 pts, 95% [+0.251, +6.267], P+ 0.981**. All six entries `unresolved_below_power` -- no positive control has been run for this channel (so none is `bounded_by_control`), and no whole interval sits below zero (so none is `wrong_sign_resolved`). Per AGENTS.md's "a promotion bar is not a decision bar," this is an EV decision, not a claim the channel is resolved: `probability_positive` 0.935-0.981 clears the standing forced-pick EV bar, so the policy is wired and executing, not merely proposed. The 1.0-point threshold is the stronger of the predeclared {0.5, 1.0} grid at both readings, fixed before either number was seen, not tuned after the fact. **2026-08-20 same-day follow-up sharpens WHERE this edge concentrates; it does not re-decide whether to play it** (full figures under POL-10): `docs/movement_attribution.md` attributes most of the Tuesday-to-close disagreement games to injury news -- the ≥1.0-threshold, injury-flagged subset reads +17.07 pts, P+ 0.976, a correlated decomposition of `observed_movement_threshold_1_0`, not independently poolable with it -- and wires a dedicated `injury_signal_refresh_tilt` challenger that front-runs the Wed/Thu/Fri practice-report signal instead of waiting for the close itself to move. `docs/opener_error_analysis.md` independently confirms the same mechanism after fixing a label-swap bug: the model covers 56.96% when observed movement already agrees with its pick vs. 47.37% when it disagrees (whole week-blocked CI below zero on the disagreement side, but this describes where the active model underperforms, not a beat-the-baseline candidate, so still `unresolved_below_power` by design). Deliberately deferred, not built: wiring any research overlay (injury value-lost, division revenge, etc.) into the refresh's own decision path, and settling `final_pick_per_game` against real outcomes inside `prospective-score` (needs a real week of refresh data first) **Inactives (T-90) channel, 2026-09-01 (WP5 design + WP17 build):** feasibility study `docs/inactives_channel.md` -- 238/272 (87.5%) of 2026 REG games are playable under `pick_refresh.pick_deadline` (SNF/MNF structurally excluded; the Sunday 16:05-17:00 ET slot IS playable, computed not assumed); `nfl.com/inactives/` chosen as primary source (robots clear; mirrors the `/injuries/` scraper), RotoWire fallback; ESPN's JSON API and nflverse confirmed NOT to carry a T-90 inactives feed; `snap_counts.parquet` zero-snap rows are a leak-free grading label for a backtest but never a pregame feature. Built: `src/nfl_ats/inactives_capture.py` + `scripts/capture_inactives.py` (point-in-time snapshots under `data/players/inactives/<stamp>/`), seven `inactives_*` scheduler rows at each slot's T-90 window, 18 tests + 7 scheduler pins. Caveat stated plainly: nfl.com served only its preseason placeholder all session and Wayback was unreachable, so the populated-page parser is inferred by analogy to `/injuries/` and exits non-zero with `empty_reason="unrecognized_page_structure"` the first time a real page does not match. Not done: wiring the capture into `refresh-picks` (the existing `refresh_sun` 10:00 ET pass fires before both Sunday inactives windows) and the predeclared Section 5 experiment; no window spent. (WP17, 2026-09-01) **2026-09-03 refresh-path verification:** `refresh-picks --season 2026 --week 1` (no `--record-decisions`) fails closed with "No recorded original card" — the Tuesday ledger does not exist until the 9/08 lock, so there is nothing to refresh against and nothing was written. **2026-09-07 (measured):** the "not done" wiring above is stale -- `refresh-picks` runs `record_inactives_refresh_overlay` on every pass (src/nfl_ats/cli_commands/publishing.py), and the seven `refresh_*_inactives_*` scheduler jobs (added 2026-09-02) fire 5 minutes after each inactives capture closes; all seven were exercised by hand today via `capture_scheduler.py --run-job` and stop at the correct "no Tuesday ledger yet" guard. Also fixed today: `refresh-picks` no longer requires `--season/--week` (defaults to the active forecast), which is what had made every scheduled pass die on 2026-09-06. **2026-09-11 Friday audit (measured by the coordinator):** the served surfaces disagreed on which side is being played. The pick-revision ledger's last state (Thursday passes, `artifacts/prospective/pick_revisions.parquet`) had DAL at NYG -> NYG (coach fade after the model's own read crossed by 0.07), ATL at PIT -> PIT and WAS at PHI -> PHI (leading books moved a full point, follow rule), and a read-only `refresh-picks` pass this morning reproduces all three; but the 2026-09-11 01:18 ET `publish-predictions` republish (served the pass-protection crosswalk fix: NO at DET -> NO, DEN at KC -> DEN) rewrote the card table from a fresh compute that does not run the refresh-time follow rule and dropped the late-week section, so the tracked card and the site's picks table showed DAL and WAS while the site's what-changed table said NYG and PHI 'are the ones being played'. Fixed in code, not by hand: (1) the board's picks table now renders the latest recorded revision's side per game (decision score recomputed for that side, floored at 50%), so the 'has not caught up' footnote no longer fires; (2) `refresh-picks --publish-card` lists every eligible game whose current side differs from the published table (`RefreshedGame.published_pick_side`, `card_changed_games`), not only this pass's delta, and says the section wins where it disagrees with the table — run for real (`--note friday_audit_republish`), 3 rows written to `CURRENT_PREDICTIONS.md`. **Still open, blocked on a permission the session could not obtain:** the paper-decision ledger still holds only the lock state (16 rows, recorded 2026-09-09 20:28 UTC), so `refresh-picks` freezes the lock's composition flags and treats NO at DET / DEN at KC as DET / KC while the card serves NO / DEN; the designed repair is `nfl-ats publish-predictions --record-decisions --replace-week --no-board` (drops pre-kickoff rows in every ledger with a timestamped .bak, leaves the two played games untouched, re-records the served state). Rule to keep: a mid-week `publish-predictions` that changes a served rule must carry `--record-decisions --replace-week` in the same command, otherwise the ledger, the refresh baseline and the card diverge. **Same day, later (measured):** the Friday lineups job's daily `publish-predictions --with-board` rewrote the card at 16:14 UTC and dropped the late-week section again, so `scripts/refresh_lineup_forecast.py` now runs `refresh-picks --publish-card --note lineups_refresh` and `publish-board` after every daily republish; the section was re-appended by hand for today (3 rows). **Same day, consistency guard (lane; live output reported by the lane):** `nfl-ats card-ledger-check` (`src/nfl_ats/card_ledger_check.py`) compares the paper ledger's side and composition flag per pre-kickoff game against a fresh `current_played_card_view` (extracted from `record_paper_decisions`, same derivation) and the pick-revision ledger's last state against the board's rendered side; exit 1 on any disagreement. Live 2026-09-11: 7 disagreements — NO at DET and DEN at KC on side and flag, ATL at PIT and DAL at NYG on the composition flag alone (inputs moved further since the morning entry); zero ledger-vs-board disagreements. Wired as a never-failing warning into `refresh-picks` JSON, logged after the daily lineup refit's publish, and printed by `publish-predictions` with the repair command whenever it serves without `--record-decisions`. The repair itself (`--record-decisions --replace-week --no-board`) is still unrun. |
| POL-12 | 🚧 | Tiebreaker score guess | The pool breaks ties on the final score of the week's LAST game (owner, 2026-09-01; Week 1: DEN @ KC, Monday). Shipped same day: `nfl-ats tiebreaker` (`src/nfl_ats/tiebreaker.py`, `tests/test_tiebreaker.py`) — market-implied score from the freshest local odds snapshot (median across books, totals captured every run since `odds-ingest --markets spreads,h2h,totals`), calibrated against all 4,630 lined finals 2009–2025 via a widening (margin, total) neighborhood; reports the closest-total-optimal median guess AND the neighborhood's most common exact finals, because the pool's tiebreak metric is not recorded anywhere — capture it in Week 1 alongside POL-05's field-size/prize observables. Honest error bars printed with every guess (measured 2026-09-01): market total MAE 10.5 (median 9.0, bias +0.5 — actuals run half a point over), implied per-team score MAE ~7.4. **Open: the owner-anticipated over/under training regime — design FROZEN 2026-09-01 in `docs/totals_model.md`** (predeclared before any totals fit: market-residual target, explicit 40-column pregame allowlist from the canonical table, production's exact impute→scale→Ridge(10) pipeline, expanding walk-forward min-500, MAE blend sweep with `clv.week_blocked_bootstrap`, registry recording, `TOTALS_RESIDUAL_WEIGHT` wiring into the tiebreaker). Execution is one session of work; owner confirmed queued-not-now (2026-09-01). The model's margin blend already ships: the guess margin = market + 0.2 × the active model's disagreement, the 0.2 measured on 1,537 opener-graded games (`tiebreaker.MODEL_RESIDUAL_WEIGHT` docstring carries the sweep) — the raw model is WORSE than the market as a point estimate (MAE 10.00 vs 9.91) despite beating it on sides, which calibrates expectations for the totals regime too **Regime RUN (WP1, 2026-09-01).** `docs/totals_model.md` executed as frozen: `src/nfl_ats/totals.py`, `nfl-ats totals-backtest`, `tests/test_totals.py`, one backtest (`artifacts/totals_backtest/20260901T184010Z`), one registry entry, the tiebreaker wiring. Measured on 3,935 walk-forward REG games 2010-2025: the raw model total is WORSE than the market total (MAE 10.5495 vs 10.4249), replicating the margin axis; the MAE-minimising blend `total_line + 0.1 * predicted_residual` is worth +0.0008 total points, week-blocked 95% [-0.0062, +0.0077], `probability_positive` 0.583 over 261 week blocks -- `unresolved_below_power`, recorded `totals_market_residual_blend` (family `totals_market_residual`, units `mae_improvement`). `tiebreaker.TOTALS_RESIDUAL_WEIGHT = 0.1` is derived from that sweep; playoffs reported separately (188 games, +0.0237), never pooled. **Wave 2 (WP18, same day):** wave 1's 41 columns plus 24 drive-pace columns from `game_features_pbp.parquet` (`docs/totals_model_wave2.md`, positive control P+ 1.000): wave 2's blend beats wave 1's on a paired per-game basis by +0.0020 MAE points, week-blocked 95% [-0.0024, +0.0063], **P+ 0.8235** -- the EV favourite; recorded `totals_market_residual_wave2_vs_wave1`. **Wiring applied 2026-09-02:** `src/nfl_ats/tiebreaker.py` prefers `totals_wave2` when `game_features_pbp.parquet` is present, retains the independently selected 0.1 blend weight, and fails closed to market-only rather than silently substituting wave 1 when the present wave-2 input is stale, incomplete, or misaligned; an entirely absent PBP table keeps an explicitly labelled wave-1 fresh-clone fallback. POL-12 remains in progress only because the pool's actual tiebreak metric still needs its Week 1 capture. **Tiebreaker neighborhood fixed (WP14, same day):** the totals blend surfaced a half-point-quantization edge -- a +0.042 nudge to the centre dropped the whole 38-game `total_line == 41.5` bucket and moved the published Week 1 guess DOWN two points while the model argued higher. The calibration neighborhood is now kernel-weighted (`max(0, 1 - d)`, `d = sqrt((dmargin/1.0)^2 + (dtotal/1.5)^2)`, bandwidths inherited from the old first window, widened continuously until the Kish effective sample size reaches the same 150 floor; weighted medians and weighted exact-final counts). Measured: the guess now moves monotonically with the line (41 -> 42 -> 43 as the blended line rises 43.00 -> 43.50, versus the old 43 -> 41 -> 43). Consequence stated plainly: the Week 1 DEN@KC card reads **KC 22 - DEN 19** (was KC 23 - DEN 20) at the unblended market total as well, a 2-point difference far inside the 10.5-point market-total MAE. (WP1, 2026-09-01) **LEAD-54's -1 low-side shade wired into the served guess (2026-09-11).** Read `docs/tiebreaker_low_side_shading.md`'s decision (the predeclared -1 shade at `probability_positive` 0.816 full-sample / 0.843 out-of-sample on the closest-total AE metric, nothing resolved against it): `src/nfl_ats/tiebreaker.py` now adds `TOTAL_LOW_SIDE_SHADE_POINTS = -1.0` (source-tagged `TOTAL_LOW_SIDE_SHADE_SOURCE`) to `build_report`'s `guess_total_line` immediately after the existing market+model blend, so the neighborhood, the pick-consistent score lattice and the fallback median guess all recentre on the same, now one-point-lower, served total -- the margin blend (`MODEL_RESIDUAL_WEIGHT` 0.2) and the totals blend that produces the pre-shade total are untouched, and the score split still preserves the projected margin. The existing pick-side-strict, lattice-consistency guard (`nfl_ats.score_lattice.pick_consistent_top_score`, `TiebreakerConsistencyError`, publishing fails closed) is unchanged, just handed the shaded target. The shade is recorded, not narrated: two new `TiebreakerReport` fields (`low_side_shade_points`, `low_side_shade_source`) surface in `tiebreaker.json` (`total_low_side_shade_points`/`_source`) and as a new `total_low_side_shade` lineage source; reader-facing text carries only the already-shaded score, no jargon added. Measured (read-only rehearsal against the live checkout, `tiebreaker_report()`, nothing written): the week's last game DEN @ KC (2026-09-14, Monday), market total 43.5 -- served total moves **43.72 -> 42.72**; the printed integer guess stays **KC 23 - DEN 20 (total 43)** in both cases this week, because the pick-consistent lattice's nearest admissible cell (margin exactly 3, the only feasible parity near the KC -2.5 centre) sits at total 43 whichever side of it the continuous target lands on -- a disclosed case of the shade moving the underlying number without moving this week's discrete score, not evidence the wiring is inert (the fallback-branch and model-blend unit tests below do show the printed guess moving on finer lattices). Tests edited to match, per the moratorium (no new test files/functions): `tests/test_tiebreaker.py` (six assertions plus one rewritten comparison block), `tests/test_served_total.py` and `tests/test_totals.py` (two assertions each plus an import), `tests/test_played_card_lineage.py` (two exact-list lineage-source assertions widened, and its 3-row `_finals()` fixture broadened to 17 scores after the shade pushed that tiny fixture's lattice construction to exactly zero feasible mass -- a fixture-size artifact, not a production issue). Gates measured this session: `ruff format --check .` / `ruff check .` clean on every file this lane touched (two pre-existing, unrelated failures remain in `scripts/playcaller_change_screen.py` and `scripts/sharp_book_weighted_movement.py`); `mypy src` -- Success: no issues found in 218 source files; `pytest tests -k tiebreak -n 2` -- 75 passed. |

> **2026-09-02 POL-11/POL-12 follow-through (supersedes the stale "not done"
> and "proposed next edit" phrases in the rows above):** `refresh-picks` now
> records T-90 inactives only to its separate challenger ledger, with seven
> post-capture scheduler passes and rejection of stale, future, wrong-week,
> malformed, duplicate, partial, or game/team-misaligned inputs; the Section 5
> ATS experiment remains unrun and no window was spent
> (`docs/inactives_channel.md`). The tiebreaker now serves the validated
> 65-column totals-wave-2 view at its already-recorded 0.1 blend weight, falls
> back to wave 1 only when the whole PBP table is absent, and uses market-only
> when a present table fails identity checks (`docs/totals_model_wave2.md`). A
> live 2026 Week 1 command served wave 2; no card or active-model artifact
> changed.
>
> **2026-09-03 totals program declared** (`docs/totals_program.md`, owner
> directive "a massive backlog that just hasn't been invented yet"):
> 6-session queue — over/under classifier (feasible today: total_line
> present 4,742/4,902; predeclared `docs/totals_over_under_screen.md`,
> family `totals_over_under_on_production` declared open, assign/record NOT
> run, pools exhausted), totals feature waves, calibration, team-totals/H2
> feasibility (ABSENT from nflverse — quota-gated), tiebreaker integration,
> live totals (no source). No window spent, no model change.

> **2026-08-20 later update to POL-10:** live registration is now **17
> ACTIVE_PROSPECTIVE challengers / 20 entries total**, measured from
> `artifacts/prospective/challengers.json`. The added
> `best_pick_big_spread_eligibility` rule (`docs/best_pick_big_spread_challenger.md`)
> prospectively excludes 10+ spreads from v2's eligible nomination pool,
> falls back to v2 if that would empty the mandatory weekly choice, and never
> changes the published Best Pick. Week 1 remains MIA +3.5 under both rules;
> the first informative evidence awaits a week where their nominees differ.

> **2026-08-20 lock-day canary follow-up to POL-10:** the publish command's
> 15 live publish-time challengers are now represented by one explicit
> registry-ID-to-result-key map, and a test compares that map with the live
> `ACTIVE_PROSPECTIVE` registry. The audit found and fixed one real response
> gap: the default no-record path omitted the v3 nomination challenger's
> explicit skipped result. Registration drift or an unwired new publish-time
> challenger now fails the CLI test before Sep 8 rather than silently losing
> its first prospective week. The three non-publish-time active challengers
> remain on their separate refresh/prospective-record paths.

> **2026-08-21 production follow-up:** the forced-pick decision now uses the
> exact four-member OR-union found in the overlay-composition audit: coach fade,
> division revenge, player arrests, and spread-gap zone are each evaluated
> against the raw model card; a game is flipped exactly once if any member
> fires. On the 1,503 opener-graded archive games this exact policy scores
> 833/1,503 = **55.4225%**, versus 814/1,503 = **54.1583%** for the former
> production coach-to-arrests chain, a paired +1.2641 accuracy points with
> week-blocked `probability_positive=0.85715`. The archive score is the maximum
> of 127 correlated subsets and therefore selection-inflated; it is not the
> operating expectation or a resolved-effect claim. Publication is fail-closed
> on the production inputs, and all pages consume the composed final card. The
> primary paper ledger freezes all four member flags, the union decision,
> schedule/arrest provenance, and the former-policy side; refresh reuses the
> frozen union rather than reopening Tuesday sources. Active challenger
> `overlay_production_chain_coach_arrest_incumbent` records that exact former
> policy from the same primary row for prospective paired scoring.
> The 2026 Week 1 August preview was regenerated across Markdown and all three
> static pages: the policy changes BAL-at-IND via coach fade and CLE-at-JAX via
> spread-gap, with no overlap; MIA-at-LV remains Best Pick. No ledger row was
> written before the September 8 lock.

## Phase 10 — dashboard and operations

| ID | Status | Item | Definition of done |
|---|---|---|---|
| UI-19 | 🚧 | Assistant season-record answers | Once the 2026 ledgers hold settled rows, answer "how is the model doing this season" from the History page's settled ledger (season record strip + challenger assessments) instead of the archive figures alone, keeping the archive-vs-prospective distinction the site already draws. Done when a record question on a settled week cites the live strip first and the archive second, pinned by fixture tests. Not buildable before the first Tuesday lock writes genuine rows. **2026-09-08 (Codex lane D, gpt-6-astra low effort, read-only patch applied by the coordinator; measured 305 board/assistant tests green, real publish-board clean):** topic `season_record` answers "how is the model doing this season" from the settled strip first (wins-losses-pushes, weeks settled, the played card's rate) and the archive second, says plainly when nothing is settled yet, Python/JavaScript parity, golden questions pinned; a shared season-record accessor now flows through the site headline during assembly. Stays open until the first settled 2026 week proves it on real rows. **2026-09-11 (dashboard lane, on the first two settled 2026 games): proven on real rows and widened.** Measured this session: `nfl-ats prospective-score` was settling nothing (0 of 16 recorded Week 1 picks) because it read outcomes only from `data/processed/game_features.parquet`, whose 2026 rows carry no `result` yet; the finals live in `artifacts/settlement/game_results.parquet` written by `nfl-ats settle --write-graded`. `_prospective_outcomes` in `src/nfl_ats/cli_commands/prospective.py` now overlays that artifact the same way `board_content._load_game_outcomes` already did, and the same run then settles 2 games for the card and 31 of 41 entrants. New History-page section "This season so far" (`SeasonSoFar`/`SeasonChallengerRecord` in `board_site_content.py`, `_season_so_far_section_html` in `board_terminal.py`): the card's record on settled games with a plain "two games is two games" caveat, the strongest-pick-of-the-week record, the tiebreaker guess and its points/margin error once that game is final, and the rules-being-tried-out that picked differently. The assistant's `season_record` topic now answers from the same block (best pick, caveat, tiebreaker, challenger tally) instead of the headline strip alone. Measured on the live artifacts: card 2-0 (NE +3.5 and SF +3.5 both correct), 100% right on 2 finished games, strongest pick not yet played, 31 rules graded (0 better, 23 level, 8 worse). |
| UI-20 | 🚧 | Continuous dashboard improvement (standing lane) | Owner mandate, restated 2026-09-05 after another session shipped nothing visible: every session ships at least one reader-facing improvement inside the Terminal design system and ends with `publish-board` run, the rendered-page diff reported, and the push made so Pages rebuilds (AGENTS.md "The dashboard improves every session"). This row never closes; it carries the candidate list. **Queue (2026-09-05):** (a) per-pick plain-English "Why this pick" from ENG-12 `explanations.json` on This Week; (b) "Research this week" section on findings.html from the registries' `recorded_at` (screened / resolved counts, plain summaries, P+); (c) SOURCES panel computed at build time when no lock-time `source_policy.json` exists; (d) a season-record strip once Week 1 settles (UI-19); (e) the challenger table's Week 1 previews once the lock writes rows; (f) surface the ENG-39 injury-block state on the card (complete / degraded / blocked) so a reader sees whether injuries informed the picks; (g) the tiebreaker guess with its market-total context; (h) history page: per-week opener-vs-close grading side by side. | **2026-09-05 lane W: (a)/(b)/(c) shipped in code.** (a) `GameRow.explanation_text` (`board_content.py`) reads `explanations.json` beside the synced forecast (`card_explanation.PickExplanation`) and renders under every pick row as a collapsed `<details>` ("Why this pick"), falling back to an explicit not-recorded sentence when the file/row is absent -- zero default-visible percentages added (`board_terminal._why_this_pick_row_html`). (b) new pure `findings_registry.recent_registry_activity(registry, rotation, as_of, days=7)` groups weak-signal + rotation entries recorded/screened in the last 7 days by category, badges only `refuted_mechanism`/`bounded_by_control`/`closed_negative` as "Resolved the other way" (never "failed"), renders "no new screens recorded this week" on an empty window; wired into `FindingsPageContent.recent_activity` / `render_findings_page`. (c) `_load_source_policy_view` now falls back to a live `report_for_publication` read (same call `publishing.py` makes) when no `source_policy.json`/metadata block exists, labelled "computed at build time ... not recorded at lock" (`SourcePolicyView.computed_live`). 68 new/updated tests across `test_findings_registry.py`, `test_board_content.py`, `test_board_terminal.py`, and new `test_public_board_wave1.py`; `ruff format`/`check` and `mypy src` clean on touched files. **Not yet run**: `publish-board` to prove it end-to-end -- the live weak_stack feature table drifted mid-session (another lane rewriting `data/processed`, confirmed via `git diff`-free but freshly-mtimed parquet files), so every real-artifact test/build in this session's window raises `DataContractError` ("Refit Gaussian probabilities ... do not reproduce the supplied card's home_cover_probability"), unrelated to this diff (traced into `_load_spread_explorer_params`, a function this lane never touched). Coordinator should re-run `publish-board` once that table settles. **2026-09-05 lane X: every listed player now carries a play probability, not only the model's QB** (owner complaint: "why is that only quarterbacks have the lineup percentage filled in?" -- measured cause read in `scripts/build_week_lineups.py`: `probability = qb_probability if position == "QB" and gsis_id == model_qb_id else None`). New `nfl_ats.lineup_availability` module reuses the SAME learned/fixed availability rule (`nfl_ats.availability.resolve_unavailability`) already used for the QB, applied per-player, plus a genuinely new no-designation base rate for players with no injury designation this week -- conditioned on `recent_role` after measuring that a bare position average understates a starter's true probability (naive position-only rate 30-38% unavailable; a "returning contributor" with no designation is 3-5%). `scripts/build_week_lineups.py` now wires every `gsis_id`-bearing player to `play_probability`/`probability_source` (`base_model_qb`, bit-identical, verified 32/32 against the forecast's own `{side}_qb_start_probability`; `availability_model` for everyone else; `unavailable` only for the 7 rows with no `gsis_id`). Rebuilt the real artifact (`artifacts/lineups/current/lineups.json`, `generated_at` 20260905T135654Z): 2179 players total, 2140 `availability_model` + 32 `base_model_qb` + 7 `unavailable`; by position group min/median/max play probability -- front 0.102/0.968/0.968 (n=569), offensive_line 0.114/0.950/0.950 (n=360), other 0.064/0.980/0.980 (n=298), secondary 0.107/0.968/0.968 (n=368), skill 0.109/0.952/1.000 (n=577). Added a point-in-time leakage test proving a report dated after `generated_at` never changes a player's number, plus a legend line and per-row tooltip on the lineup panel (`board_terminal._lineups_html`/`_lineup_team_html`) and a non-QB availability answer in `board_assistant_lineups.player_availability_answer`. **Measured, not this lane's bug:** the QB's own `{side}_qb_start_probability` is a dead constant 1.0 for all 32 rows right now -- the pre-existing ENG-39 defect (production's feature table has zero 2025/2026 injury coverage); this feature's `availability_model` numbers for everyone else are computed fresh and unaffected. 12 new tests in `tests/test_lineup_probabilities.py`; `ruff format`/`check` and `mypy src` clean on every touched file. Reproduced the same pre-existing `data/processed` drift lane W already reported (`test_board_terminal.py`'s real-artifact tests still raise the identical `DataContractError`, unrelated to this diff). Did not run `publish-board` (coordinator's job per the fleet brief). **2026-09-05 lane Y: (g) and (h) shipped, plus four coordinator escalations landed the same session (lineup-percentage legibility, tiebreaker one-lattice consistency fix, scroll-gated-motion removal, and a site-wide compliance-boilerplate purge).** (g) `board_content.TiebreakerView`/`_load_tiebreaker_view` reads a persisted `tiebreaker.json` (beside the forecast and beside the card; schema mirrors `TiebreakerReport`) -- collapsed "Tiebreaker guess" panel on This Week (`board_terminal._tiebreaker_panel_html`), never recomputed. `nfl_ats.publishing` now computes ONE `TiebreakerReport` per publish and reuses it for the card's tiebreaker line, `tiebreaker.json`, and the existing `lineage.json` records; a `TiebreakerConsistencyError` degrades to "not published" for the tiebreaker only, never blocking the card. The board assistant answers "what's the tiebreaker" from the same file (`_tiebreaker_body`); "tiebreaker" is no longer deflected. (h) `board_site_content.SeasonGradeRow`/`HistoryWeekGrade` render opener-vs-close side by side on History, per season (reusing the Model page's own `_season_rows`) and per recorded week (`prospective_scoring.settle_prospective_picks`, now also given a close-line reference via `clv.live_close_reference`); a missing grade renders an explicit, dynamically-computed sentence, never a blank. **Escalation 1 (lineup legibility, owner: "a rookie QB2 reading 47%... tells a reader nothing"):** a percentage now renders only when `ProjectedPlayer.worth_showing_probability` (a real injury designation this week, checked identically for the base-model QB and everyone else) -- `scripts/build_week_lineups.py` gained `has_injury_designation`; everyone else shows a muted "no designation" marker, never a constant base-rate number dressed as one. **Escalation 1b (colour coding, owner: "what is the color coding?"):** the probability cell's tone is now availability risk on the number itself (green >=85%, amber 50-85%, red <50%) -- never the scored QB's matchup-impact sign, which moved to the impact-note text instead. **Escalation 2 (tiebreaker consistency, owner: "our project over/under total needs to line up with our spread prediction"):** `nfl_ats.tiebreaker.build_report` now reads the served score off MOD-05's `score_lattice` centred on `(model_view.predicted_margin, guess_total_line)`, restricted to finals strictly on the card pick's side of the spread -- never the push/wrong-side rounding the old median-based guess could produce; see `docs/tiebreaker.md`. **Escalation 3 (motion, owner: "i absolutely hate this dynamic page load thing"):** the IntersectionObserver scroll-gated reveal and KPI roll-up (`_MOTION_SCRIPT`, `.content-motion-*`/`.motion-item`/`--motion-delay`) are deleted entirely; every element renders visible at load. **Escalation 4 (boilerplate, owner, verbatim: "ive told you repeatedly to drop these fucking legal bullshit words"):** the footer disclaimer block/CSS and the gambling-helpline line are removed from every page; every "not a wagering recommendation"/"descriptive research summary"/"research preview"/"not proof of a profitable or stable edge" phrase is purged from the card, the per-pick explanations, and the assistant's deflection text. Per-pick explanations (`card_explanation._render_text`) were rewritten in football terms -- naming the biggest factors via `market_decomposition.explain_game_structured` off the real attribution-waterfall feed -- and now hard-reject snapshot ids/timestamps/hashes/the words "snapshot"/"artifact"/"lineage" (`check_language`). The banned-phrase list is the shared `card_explanation.BANNED_BOILERPLATE` (re-exported from `board_content.py`), used by `check_language` and by `tests/test_public_board.py::assert_public_safe`'s now-inverted absence check. **Measured, this session:** `.\.tools\uv.exe run --no-sync nfl-ats publish-board --site-destination <scratch>` succeeded end-to-end (all four pages), confirming both (g) and (h) render against the REAL current artifacts (the `data/processed` drift lane W/X hit had resolved by the time this lane ran); full repo suite `.\.tools\uv.exe run --no-sync pytest -n 2` -- **4,950 passed**, 0 failed; `ruff format --check .` / `ruff check .` / `mypy src` clean repo-wide. **Noted, not this lane's scope:** `scripts/build_week_lineups.py` was rewritten concurrently by another lane mid-session into a walk-forward `play_probability` model (UI-20-AB) that supersedes the position-based no-designation rate this lane's legibility fix was built on top of; `has_injury_designation` survived that rewrite and the full suite is green, but the coordinator should re-verify the legibility/colour-coding rule still reads correctly against the new model's own probabilities. New/updated tests: `test_public_board_wave2.py` (new), `test_board_content.py`, `test_board_site_content.py`, `test_lineup_probabilities.py`, `test_board_assistant_lineups.py`, `test_assistant_golden.py` + its `questions.json` fixture, `test_score_lattice.py`, `test_tiebreaker.py`, `test_publishing.py`, `test_card_explanation.py`, `test_public_board.py`, `test_board_content_coverage.py`, `test_board_improvements.py`. Docs: new `docs/tiebreaker.md`; `docs/site_content_pipeline.md` 2026-09-05 section. **2026-09-05 lane AB (UI-20-AB): the per-player lineup probability is now a real forecast, not a base rate** (owner, verbatim: "the percentages should obviously make sense my dude... it needs to be a forecast about the game and it needs to consider depth chart" -- lane X's base rate made a rookie QB2 read 47% and a healthy QB3 read 95%). New `src/nfl_ats/play_probability.py`: a walk-forward, isotonic-calibrated `HistGradientBoostingClassifier` for P(plays)/P(starts) using depth-chart rank, this week's own injury report, recent snap history, roster status, and (for QBs) the team's own QB1's injury status. Archived nflverse's full (all-position) depth-chart history for the first time -- `nfl_ats.quarterbacks`'s existing archives are QB-only -- handling a measured schema seam (legacy week-labelled rows through 2024, daily dt-timestamped rows from 2025) and nesting the archive under `data/players/raw/depth_charts/` rather than `data/players/raw/<stamp>/` after a measured collision with `nfl_ats.players.latest_player_snapshot`'s glob. **Measured** (`scripts/build_play_probability_panel.py`, 391,045 depth-chart-row-weeks, 2013-2025, `nfl_ats.play_probability.walk_forward_evaluate`): overall Brier 0.040 (full information) / 0.099 (serving-realistic, `roster_status` forced to `"ACT"` to match what is actually available at serve time) vs. 0.176 (depth-rank-only baseline) vs. 0.221 (the current base-rate approach lane X built). Season-blocked bootstrap (12 seasons), serving-realistic vs. the current approach: +0.124 Brier points, 95% [0.102, 0.146], probability_positive 1.0 -- recorded via `nfl-ats weak-signals record` as `play_probability_model_brier_improvement` (`unresolved_below_power`; positive on 12/12 season blocks but one predeclared measurement, not a positive-control-bounded closure). Calibration by depth slot is tight (within 1 point for every slot but the small-n `WR4+`/`other`). `scripts/build_week_lineups.py` now scores every `gsis_id` player through this model (`probability_source: "play_probability_model"`, new `start_probability` field); the active margin model's own forecast input for its scored QB survives as a separate, never-deleted `model_qb_start_probability` field. Rebuilt the real artifact: 2026 Week 1 QB1 0.988-0.999 (mean 0.997), QB2 0.186-0.212 (mean 0.186), QB3+ 0.003-0.121 (mean 0.020) -- matches the owner's own sanity-check ranges. Secondary `started` label measured far coarser (rank-1 CB ~0.4% vs. QB ~82%, a real property of a flat 50%-of-specific-position-share threshold on multi-role positions, documented rather than smoothed over). 11 new tests (`tests/test_play_probability.py`); 4 of lane X's `tests/test_lineup_probabilities.py` tests updated for the new `_team_payload` contract (a stub predictor now stands in for a real trained model so those tests probe wiring, not model correctness) -- all 13 pass; `tests/test_board_assistant_lineups.py`, `tests/test_script_contracts.py`, `tests/test_lineup_view.py`, `tests/test_board_terminal.py` unaffected (251 passed together). `ruff format`/`check` and `mypy src` clean. Full details, every measured number, and the calibration/started tables: `docs/play_probability_model.md`. **2026-09-05 lane AH (humanising audit): the policy-overlay panel the owner flagged live ("Policy overlay_union_coach_division_revenge_player_arrests_spread_gap_v1 (bbdd60a1...)... this is for humans not the opus autist") and every other reader-visible fingerprint/slug/`P+`/`week-blocked` mention found across all four pages and the published card are gone.** Fixed at the render layer (`board_terminal.py`) and the two source layers that fed it: `board_content._build_policy_note` (no more policy id/hash in `rich_narrative`; "raw model" -> "the computer's first read"), `public_board.humanize_identifier` (new shared `snake_case -> words` translator, reused by `board_content`/`board_terminal`/`board_assistant`/`publishing`/`source_freshness_policy`/`model_ledger` for challenger ids, source ids, registry signal names, category labels, and the active model's own method label), and `_humanize_probability_positive`/`_humanize_classification` (both new) replacing every `P+ 0.79`/`unresolved_below_power` literal with "79% likely real"/"not enough evidence yet". Registry-sourced free-form research prose this lane's file list does not cover (Watching Leads' and Signal Registry's own methodology notes, "Research this week"'s registry feed) is wrapped in `<code>` at the render layer rather than hand-rewritten -- documented, not silently exempted. **Second fix, same session, owner: "please do not let those percentages get out of date anymore":** the headline "Played policy - archive score" was a hardcoded constant (`OVERLAY_UNION_ARCHIVE_SCORE_FRACTION`); it now reads the played four-member union's row from the newest `overlay_subset_composition` run whose OWN baseline per-game artifact matches the active model (new `public_board.find_matching_opener_evaluation`/`find_matching_overlay_composition`/`played_union_subset_accuracy`), falling back to the constant with a visible "archive score not recomputed for this model" caption -- never a silently stale number. Live headline moved 55.4% -> **55.6%** (measured: `artifacts/overlay_subset_composition/20260905T164409Z/result.json`, candidate_accuracy 0.5556 vs 0.5409 baseline, matched to active model `ab29832a4e099766`). `public_board.load_played_chain_accuracy` repointed the same way (per explicit instruction) to the played union; the OLD two-member coach-then-arrest chain it used to return is still read directly off the same artifact for the "Prior chain" KPI/selection-caveat comparison, which needs it as a genuinely distinct number, not retired. **Third addition, same session:** `board_content.verify_number_provenance` (+ `NumberProvenance`/`NumberProvenanceError`) checks the raw opener baseline, close-graded classification, played-policy archive score, and per-pick probabilities against the active model and RAISES (fail closed, not degraded) naming the exact artifact + rerun command on a mismatch; wired into both `publish-board` and `publish-predictions` (`cli_commands/publishing.py`) so a stale composition run can never reach a published page or card again. A model-page fine-print "Where these numbers come from" `<details>` block (artifact kind + date + model label, no hashes) lets a reader verify the match themselves. New shared render-contract test `tests/test_board_humanised.py` scans all four rendered pages plus a real published-card fixture for hex/versioned-slugs/raw stamps/`P+`/`week-blocked`/`BANNED_BOILERPLATE` (reusing `card_explanation.BANNED_BOILERPLATE` rather than a second list) and fails on any hit -- the one test this lane could not have shipped without, since it is what will catch the next silent regression. **Measured, this session:** `ruff format --check`/`ruff check`/`mypy src` clean on every touched file; `tests/test_board_content.py`, `test_board_terminal.py`, `test_board_site.py`, `test_public_board.py`, `test_public_board_wave1.py`, `test_public_board_wave2.py`, `test_assistant_golden.py`, `test_assistant_js_parity.py`, `test_played_card_expectation.py`, `test_number_variables.py`, `test_board_humanised.py` -- 382 passed; broader sweep (`test_publishing.py`, `test_source_freshness_policy.py`, `test_model_ledger.py`, `test_findings_headline.py`, `test_board_content_coverage.py`, `test_card_explanation.py`) -- 181 passed after updating every test that pinned the old wording/hashes/percentages to the new contract. Full-suite run in progress at handoff. Touched: `board_content.py`, `board_terminal.py`, `board_site_content.py`, `board_assistant.py`, `dashboard/findings_content.py`, `publishing.py`, `public_board.py`, `model_ledger.py`, `source_freshness_policy.py`, `cli_commands/publishing.py`. **2026-09-05 lane AU (layout A: "board + inspector" restructure, owner: "layout A is definitely the best. lets go with that.").** This Week's board and per-game deep dive are now a single two-column grid (`board_terminal._board_section`/`_inspector_section`, `.week-grid`) instead of two stacked full-width sections: LEFT keeps the existing forced-pick `table.board` (all six columns unchanged, including the "Flips at" column `tests/test_board_flip_line.py` pins) plus its tiebreaker card and sources line, with the board's pre-selected row (Best Pick, else the first game) carrying a new `.is-selected` inset-amber highlight; RIGHT is a per-game inspector -- one `.dive-panel` per game (16, all rendered, 15 `hidden`) carrying the header, a relocated "Why this pick" explanation (`GameRow.explanation_text`, moved off the old per-row collapsed `<tr class="explain">` into the panel so it is never rendered twice), the attribution waterfall, the cover-probability curve plus its folded-in line-offset spread explorer, the two lineup blocks, and (for the week's last game only) a short tiebreaker-game note. Selection needs no JavaScript: every board row's matchup cell is a real `href="#<game_id>"` anchor (`.row-link`) into its panel's own id, shown via CSS `:target` (plus a `:has()` progressive enhancement that also hides the otherwise-still-visible default panel once another is targeted); with JavaScript on, a shared click handler (`board_terminal._DIVE_SCRIPT`'s `selectGame`, now exposed as `window.atsSelectGame` so the ticker's click handler can reuse it) swaps panels and the row highlight in place with no scroll and no page reload. The old standalone "Why this pick" section's own tab-strip selector (`.dive-selector`/`.dive-tab`) is retired -- the board's rows are the only selector now; the dead CSS rules that styled it are removed too (the `@keyframes active-tab-trace` declaration itself is kept, since it was already permanently `paused` dead weight before this change -- `tests/test_board_improvements.py::test_actual_page_content_has_ambient_compositor_only_telemetry` still pins it). **Measured, this session:** `.\.tools\uv.exe run --no-sync nfl-ats publish-board --site-destination <scratch>` succeeded end-to-end against real artifacts (all four pages); the rendered This Week page has 16 board rows, 16 inspector panels, 15 `hidden`, one `.week-grid`/`.board-col`/`.inspector-col` each, zero `class="explain"` rows, and no live `.dive-tab`/`.dive-selector` selector (only historical comment text in two places); `ruff format --check`/`ruff check`/`mypy src` clean on `board_terminal.py`. Test commands and counts: `pytest tests/test_board_terminal.py tests/test_board_content.py tests/test_board_site.py tests/test_board_improvements.py tests/test_board_humanised.py tests/test_public_board.py tests/test_public_board_wave1.py tests/test_public_board_wave2.py tests/test_lineup_probabilities.py tests/test_board_assistant.py tests/test_assistant_js_parity.py -n 2` -- 334 passed; plus a non-required safety sweep (`test_assistant_golden.py`, `test_board_content_coverage.py`, `test_board_flip_line.py`, `test_board_season_mode.py`) -- 149 passed. Updated `tests/test_board_terminal.py` (3 tests changed, 6 new: default-panel/every-game-has-a-panel/row-anchors-resolve/two-column-grid-desktop-CSS) and `tests/test_public_board_wave1.py` (3 tests changed to check panel-hidden state instead of `<details>`) for the new structure; no content assertion was deleted, only relocated. Did not touch `docs/` (coordinator publishes) or any other lane's files. **2026-09-05 CX10:** Assistant generation times and live-source notes now use plain words, the card shows saved injury-input evidence (including an explicit unrecorded state), and lineup plays/starts labels explain snaps, starting slots by playing time, and availability-risk colours, with fixture coverage. **2026-09-07: every board row prints when its pick locks.** The Kickoff cell carries a muted second line -- "Locks Thu 8:35 PM ET" / "Locks Sun 4:00 PM ET, before kickoff" -- computed from the forecast's own kickoff instants through `pick_refresh.pick_deadline` (the owner's min(own kickoff, Sunday 4:00 PM ET) rule; `board_content.pick_lock_label`), repeated in the inspector header and the game-room receipt (`board_interactive` payload `locks`); a game with no kickoff instant prints nothing rather than a guess. `CADENCE_NOTE` now states the Sunday 4:00 PM ET half of the rule. Fixture tests in `test_board_content.py`, `test_board_terminal.py`, `test_board_interactive.py`. **2026-09-07 lane B (Codex, verified by the coordinator: 487 dashboard/assistant tests pass, rendered sentence grepped on a real-artifact scratch publish):** the board assistant answers per-game, per-team and per-day pick-deadline questions from the board's own lock labels (Sunday-capped Monday games included; a missing label is stated as missing, never guessed), and a no-game deadline question explains the week's earliest and latest lock plus the Sunday 4:00 PM ET rule; a muted one-line note under the board reads the week's actual lock range ("This week's picks lock between Wed 8:20 PM ET and Sun 4:00 PM ET; each row shows its own time."), omitted when no row has a label. Six golden timing questions added (fixture now 109), JS/Python parity kept. **2026-09-07 (owner questions):** the "Flips at" column no longer prints `IND within ±4` (owner: "what does that even mean") -- a pick nothing switches inside the scanned span now names the two spreads that span covers in the pick's own orientation (`IND holds from +7.5 to -0.5`), and a coach-fade-pinned row's tooltip says the rule backs that side whichever way the model leans. And a switch caused by the 7.5-10 spread-gap rule rather than by the model changing its mind is labelled in the cell (`ARI +10 → LAC (spread-gap rule)`; owner: "how is it possible that our confidence on ARI +10.5 is 66% but the pick flips if the spread moves just half a point?" -- measured: the model still gives ARI ~75% at +10; the rule enters its zone there). New `GameRow.flip_reason`, `_flip_line` returns the reason; tests in test_board_flip_line.py. **2026-09-07 lane J (Codex; coordinator re-ran its 431-test set):** the Model page now carries a "Where the model is weak" table computed at build time from the active model's own opener evaluation (never a stored run id; one honest sentence when no matching evaluation exists): by spread bucket -- games, how often the model was right, how often it took the favourite, how often the favourite covered, its stated confidence, and its accuracy on favourite vs underdog picks -- with a two-sentence plain-words explanation (confidence does not move with spread size; margins pile up on 3/7/10/14). Live numbers: 0-3 56.2%, 3.5-6.5 56.2%, 7-7.5 52.7%, **7.5-10 48.5% (45.0% on favourite picks, 53.0% on underdog picks; the model takes the favourite 57% of the time there and favourites cover 45.9%)**, 10.5+ 44.3%, stated confidence 55-57% throughout. The assistant answers "where is the model weak" / "how does it do on big spreads" from the same table (two golden questions, JS/Python parity kept). `src/nfl_ats/model_weak_spots.py` + tests. **2026-09-07 evening (lane P, Fable; coordinator re-ran the 436-test board set and grepped the rendered scratch page):** the Model page's weak-spots panel gains a home-favourite / home-underdog split per spread bucket from the same active-model opener evaluation, with assistant answers for "how does the model do with home underdogs"; measured 7.5-10 home underdogs 69 games covered 59.4% vs 44.9% expected, 10.5+ home favourites 99 games covered 57.6% vs 49.4% with the model right 39.4%. **2026-09-08 (Fable):** the Model page's weak-spots section gains "The home-side push": this week's served offsets by spread size (from the card's own sidecar: +0.03 / +0.38 / -0.17 / +0.78 / +1.92 points, learned from 504 / 468 / 65 / 160 / 113 prior games) beside what the same rule did on the 2020-2025 archive per bucket (games, picks changed, right with and without the push), both reads shown; the weak-spots tables now describe the record WITH the push on. **Assistant answers shipped the same morning (Codex lane A, gpt-6-astra low effort, read-only patch applied by the coordinator; measured 159 assistant/weak-spots tests green):** topic `weak_spots_home_push` answers "what is the home-side push" / "why does the model lean home on big spreads" / "what changed in the forecast this season" from the same reader text, Python and JavaScript in parity, two golden questions pinned. Shipped too: `scripts/refresh_now.cmd`, the one-click local refresh (owner, 2026-09-08: "a button to refresh the lineups/injury reports/spreads all at once"). **2026-09-10 (UI-20 lane): the main page shows a pick a mid-week refresh moved, before the card is republished.** "What changed this week" now treats the late-week pick-revision ledger as the authority on which side is being played, not just the published card. Before: the section compared the published card against Tuesday's locked card only, so the 7:15 PM ET pass that moved ATL at PIT to PIT and WAS at PHI to PHI (a one-point leader-median move on each) and the 11:55 AM pass that moved DAL at NYG were invisible to a reader -- the section listed three changes, none of them the ones that had just happened. After: five rows in the order they happened, each with an ET day and time, the was and now teams, and a plain reason; the market reason now names the direction of the move ("The books that move first took this line a full point, and the card followed them. The line moved 1 point toward PHI."). A row whose new side has not reached the picks table above is marked "not in the picks table yet", and one line under the table names those games and says the side shown there is the one being played. When Tuesday's card is not on file, the ledger alone can still populate the section, and the nothing-changed state stays a single sentence rather than an empty box. `board_content._build_week_changes` / `_late_week_change_reason` / `_catch_up_note` and `board_terminal._week_changes_section`; no new tests (moratorium), 210 existing board tests green, full suite 4,527 passed with two failures owned by other lanes (scheduler argv timing, registry explorer). `publish-board` re-run: index.html grew 1,057 bytes on the same forecast, three rows to five plus the catch-up line. **2026-09-10 (UI-20 lane, second pass): every row of "What changed this week" now names a football reason instead of a placeholder.** The mid-week DAL at NYG revision had the ledger policy "the model recomputed" and a zero line move, so its published reason was the empty sentence "The computer's own read on this game moved." New `nfl_ats.pick_revision_factors` rebuilds the factor decomposition of the model's market-residual read at the forecast the locked card was built from and at the forecast the refresh produced -- each forecast folder stores its own feature values, so the two reads are decomposed through one refit of that week's ridge (measured 0.15 s to fit, 0.27 s for the whole lookup) -- diffs the per-family point contributions, and names the one or two families that moved most toward the new side using the phrases the site already uses (`market_decomposition.FAMILY_PHRASES`). Measured for DAL at NYG: the model's read moved 0.073 points toward Dallas, all of it from expected player availability (-0.055) and estimated value lost to injuries (-0.018), because New York picked up offense, offensive-line, skill and special-teams unavailability on Thursday's report; that crossed the raw read from the Giants (0.5019) to the Cowboys (0.4996), and because the coach-fade adjustment plays this game against the model's read, the served pick moved to NYG. Rendered: "The computer's own read crossed over to DAL -- a 0.07-point move, mostly on expected player availability and estimated value lost to injuries. The coach fade adjustment plays this game against that read, so the card moved to NYG." Guards, all runtime assertions rather than tests: the refit must reproduce each stored residual through `family_contributions_from_ridge`, the non-model part of the stored residual (the served home-side offset) must be identical in both reads, and the raw read on each side must reconstruct the ledger's previous and new pick sides -- any failure falls back to the old sentence. The two overlay rows (NO at DET, DEN at KC) gained the same treatment from the other end: their trigger was already named ("pass-protection mismatch"), so they now add which side the computer's own read is on -- "The computer's own read still favours DET, so the card is taken the other way." `publish-board` re-run: index.html +353 bytes, exactly three changed spans, no other page touched; no new tests (moratorium), 200 board tests and the full suite 4,529 passed / 9 skipped. **2026-09-10, the "why this pick" panel was three weeks and one model stale.** Measured: `artifacts/waterfall_feed/latest.json` pointed at a feed built 2026-08-22 for model `3083f6cbc5e45acb` on feature table `0a18e2d9...`, while the active model is `d49194e04945a5e5` on `2fb3451b...`; `board_content._build_attribution` rendered it into every game's attribution panel on index.html with no model or digest check, so the per-factor points a reader saw came from a model the card no longer runs. The only thing that regenerated the feed, `scripts/waterfall_feed.py`, had been deleted in the repository cut (b7ed31d). Restored from `b7ed31d^` and stripped of comments and docstrings; it needed three repairs, all of them the card moving on without it. (1) The served big-spread home-side correction (`home_side_offset_big_spreads_v2`) had been added to the published centre after the script was written, so its bit-exact reproduction guard fired at 0.785 points on the two games with spreads in the 7.5-10 bucket (ARI at LAC, CLE at JAX); the builder now reads the card's own `home_side_offset.json` sidecar, reconciles against centre-plus-offset, and carries the correction as its own labelled waterfall step rather than hiding it in the market or calibration line. (2) The deployed probability read is now `gaussian_median`, which the script did not know; the pick-space shift for it is the residual median (measured 0.369057 on 886 residuals, and `sign(residual + 0.369057)` reproduces all 16 served sides). (3) Its argparse description read `__doc__`, which the docstring ban empties. Regenerated as `artifacts/waterfall_feed/20260911T034354Z` stamped `active_model_id d49194e04945a5e5` and `feature_table_sha256 2fb3451b...`, 16 games. The guard that stops this silently recurring: `board_content.require_active_waterfall_feed` raises `StaleWaterfallFeedError` (a `ValueError`, so the CLI exits 2 with a plain line) when the feed's model id or feature digest differs from the active model, checked once where the feed is loaded and again inside `_build_attribution`; aimed at the old feed it prints "The per-game attribution feed was built for model '3083f6cbc5e45acb', but the active model is 'd49194e04945a5e5'. Rebuild it before publishing: .\.tools\uv.exe run --no-sync python scripts/waterfall_feed.py" and writes no pages. Regenerate command, to run after each Tuesday publish and after any refresh that changes the forecast: `.\.tools\uv.exe run --no-sync python scripts/waterfall_feed.py`. `publish-board` re-run clean: index.html 3,189,793 bytes, 354 tokens added / 370 removed of 269,385 -- the 16 attribution panels and the publish stamp; the home-side correction row now appears on exactly the two big-spread games. No new tests (moratorium); gates 4,529 passed / 9 skipped, `ruff format --check`, `ruff check`, `mypy src` clean. **2026-09-11 (UI-20 lane): the weak-spots table gains a time-of-season row, computed from the active model's own opener evaluation at render time, never from a doc.** MOD-06's screen (b) found production's own served opener picks going 56.01% in weeks 1-9 against 53.05% in weeks 10 and later, with the stated confidence flat at 0.557/0.558/0.558 across the season -- exactly the weak-spots material the Model page exists to surface, and the doc said so explicitly. New `nfl_ats.model_weak_spots.build_season_timing` (separate from `build_weak_spots`, so the existing fixture that omits `season`/`week` columns is untouched) splits the opener archive into weeks 1-9 and weeks 10-18, computes accuracy and stated confidence for each half, and reports the early-minus-late gap on both -- all with the same `nfl_ats.clv.week_blocked_bootstrap` resampled-range treatment the MOD-06 doc itself used (weeks as blocks, a percentile interval, `probability_positive_from_draws` rendered as "N% likely real" to match the existing `_humanize_probability_positive` convention). Wired into `board_site_content.load_model_weak_spots` beside the existing home-correction load, and rendered as a new "Early season vs. late season" subsection on `model.html`'s weak-spots table (`board_terminal.py`), guarded by the same active-model match the rest of the table already uses. Measured live off the active model's own archive (`artifacts/opener_evaluation/20260910T211255Z`, matches the doc exactly): weeks 1-9 766 games, 56.0% right, resampled range [52.4%, 59.6%], stated confidence 55.9%; weeks 10-18 737 games, 53.1% right, resampled range [49.3%, 56.7%], stated confidence 55.6%; early minus late +3.0 points, resampled range [-2.1%, +8.3%] (87% likely real), while stated confidence moved only +0.3 points over the same split, resampled range [-0.3%, +0.9%] (82% likely real) -- the mismatch MOD-06 flagged, now visible on the page itself rather than only in a doc. `publish-board` re-run clean (all four pages); the new fragment on `model.html` is 1,380 bytes, isolated by extracting the `weak-spots-season-h` block directly since the file's other lanes' concurrent edits make a whole-file byte diff a mixed signal. `tests/test_model_weak_spots.py` (10 tests, unmodified) and `tests/test_board_humanised.py::test_model_page_is_humanised` (real-artifact render-contract scan for ids/hashes/timestamps/`P+`/`week-blocked`/snake_case) both green against the live render; no new test files or functions (moratorium). `ruff format`, `ruff check`, `mypy src` clean on `model_weak_spots.py`, `board_site_content.py`, `board_terminal.py`. **2026-09-11 (lane 1, verified live by the coordinator on the regenerated page):** candidates (a) per-pick 'Why this pick' and (f) the injury-report banner were already shipped. Found and fixed a live contradiction: all 16 'Why this pick' panels read 'injuries was a bit stale' while the banner said reports were fully in, because `card_explanation._freshness_clause` lumped the permanently not-configured Sportradar source into the injury freshness bucket; `not_configured`/`not_due` are now neutral (matching the SOURCES legend), `scripts/regenerate_pick_explanations.py` re-rendered the current week's text from its stored components (16 changed, no probability touched). **Coordinator, same day:** the picks table now shows the side actually being played — the latest recorded mid-week change per game (DAL at NYG -> NYG, WAS at PHI -> PHI this week), with the decision score recomputed for that side and floored at 50% — so the 'picks table has not caught up' footnote no longer fires; and the tracked card's late-week section now lists every game whose played side differs from the published table (3 this week), not only this pass's delta. **2026-09-11 (lane 2, (b)/(h)):** (h) the History page's opener-vs-close per-season and per-week tables were already shipped (2026-09-05); 2026 Week 1 shows 'No close line archived for this week' because no close quote is captured yet, a capture gap not a render defect. (b) 'Research this week' on findings.html was live but out of spec: it dumped all 5,119 matching registry entries into ten category blocks (one held 4,181 rows), pushing the page past 2.3 MB, and the top entries were disguised instrument checks (positive controls, perfect-foresight ceilings). New `findings_registry._is_instrument_control_text` flags those; the section is now a flat list capped at eight one-line findings ('… chance it helps: NN%'), header states looked at / resolved / still open (5,338 / 37 / 5,301 measured); findings.html is 345 KB. Three tests in `test_public_board_wave1.py` edited to the new contract, none added. **2026-09-11 (lane 3, Rival rules panel; rendered text verified by the coordinator):** the This Week panel read 'None are recorded for this week yet' while the challenger ledger held 600 Week 1 rows across 41 rules. Root cause: `board_content._build_rival_rules` filtered the paper ledger to rows whose policy id equalled the live constant; Week 1's rows were recorded under the prior policy id two hours before that session bumped the served policy, so the filter emptied the played card every time — the same bug fixed on 2026-09-10 in `_build_prospective_scoreboard` and missed here. Filter removed; the panel now summarises how many whole-card rules disagree and where (31 pick a whole card, 29 disagree somewhere, BAL at IND most contested at 26 of 31), lists only the disagreeing rules with the team each takes, and shows each rule's record on its disagreeing games beside the card's own record there once settled (8 rules already behind the card, the same 8 History's season-so-far block names from the same ledger). Also found: `nfl-ats settle --write-graded` had not run since the two games went final, so every graded ledger still read NaN; re-running it (104 settled / 903 pending) also cleared the served-refresh-chain model-id mismatch that had publish-board failing closed. **2026-09-12 (Saturday):** the headline "played card" archive number had vanished from the board (`archive score not recomputed for this model`): the number is read from `artifacts/unserved_tilt_marginals`, whose runner `scripts/unserved_tilt_marginals.py` was deleted in the b7ed31d repo cut, so the Friday 12:09 refit (model `7786467eabe418b8`) left nothing keyed to the active model. Runner restored, wrapped as `nfl-ats served-card-archive` (reuse-or-measure, same shape as `served-refresh-card`) and wired into weekly-run step 7 so a refit can no longer blank the number; re-measured at 56.75% on 1,503 games. Second reader-facing change: the Injury reports line now states, from the latest league feed copy, how many game-status designations are actually in hand for the teams still to play, when the newest entries arrived and when the feed was last checked -- because on Saturday morning the feed carried 8 designations, all for the Wednesday/Thursday games, while Friday's designations for the Sunday slate existed only in the news (see LEAD-64). Report times on the board are now Eastern, not UTC. **2026-09-12 afternoon (owner asks, all shipped):** (1) a Books now column beside the pool line: the median line across the books in the latest capture, written for the picked side, with the move since the pool line in the hover (PIT -6 against a pool -3.5); (2) Best Pick confidence on the rail and a note under the table saying how it is chosen (highest model chance among the games where the books agreed most closely on the line, re-nominated only Sunday morning); (3) an explicit Quarterback sentence at the top of Why this pick, and a QB1 out tag in the Pick column, whenever a listed starter is Out or Doubtful: who is out, whether the listed backup is out too, the start chance the model priced, and how many points the model's quarterback inputs move the game and toward whom (from the attribution feed); (4) the lineup panel now lists players in projected order (a ruled-out player sinks below the available ones, its listed slot shown in parentheses) instead of depth-chart order that showed Tua at QB1 after he was ruled Out; (5) the Flips at column shows one number in the adverse direction only, or 'holds through' the adverse sweep edge -- the old 'holds from +0.5 to -7.5' wording and the flip search in the favourable direction are gone (owner directive, see AGENTS.md). **Correction the same afternoon (owner: item 2 missed the mark):** the reader wants the Best Pick ORDER, not a confidence number. Every refresh pass now writes `artifacts/best_pick_ranking/latest.json` (the re-nomination table: every game still to play, the played side's model cover chance, the spread's spread across books, eligible or not) and the board renders it under the table as a numbered list -- 1. MIA +3.5 58.6%, 2. DET -6.5 55.7%, 3. CIN -3.5 50.8% ... with the games the books disagree on listed after, unranked -- stamped with the pass time. **2026-09-12 evening:** the Books-now cell now says in words how the books have moved since the pool line ("half a point against NO" in the warning colour, "2.5 points toward PIT", "unchanged"), read from the same current-line artifact as the number (`GameRow.market_move_label`). Survey candidates left: a legend line for the confidence words; a kickoff countdown only as client-side script from a data attribute, never rendered text (it would go stale). |
| OPS-02 | 🚧 | Artifact retention policy | Keep manifests/ledgers, compact or prune large derived files **2026-09-01 (WP6):** measurement + dry-run planner shipped (`scripts/artifact_retention.py` `--report`/`--plan [--older-than-days N]`, `docs/artifact_retention.md`, 34 tests). Measured footprint: `artifacts/` 2.2 GB (435 runs / 145 families), `data/` ~3.5 GB. Protected-set discovery is programmatic (doc/registry citations, the active-model manifest, the prospective/clv_ledger ledgers); the default 30-day plan finds 0 candidates today, 14-day finds 14 (~9.9 MB). **Backup gap closed 2026-09-02:** after owner approval, `backup_data.py --include-artifacts` copied and SHA-256-verified 44,147 files (2.4 GB); follow-up status measured `data/` 43,046 files / 3.6 GB and `artifacts/` 44,047 / 2.3 GB at 100.0% coverage, zero pending. The scheduled weekly mirror now includes artifacts. Still open: no delete/quarantine mode exists, and the policy requires separate approval before one is added. **2026-09-03 lineup replacement (owner-approved):** `build_week_lineups.py` now overwrites stable `artifacts/lineups/current/lineups.json` atomically and removes superseded stamped runs (~320 MB reclaimed); `load_lineups` prefers the stable path with legacy fallback; provenance lives in the payload's depth-snapshot citation. Pinned by 3 loader/cleanup tests. |
| OPS-06 | 🚧 | Off-site backup and second capture host | Owner, 2026-09-10: mirror the data to a friend's server over WireGuard and run the capture schedule on both machines, each covering the other's outages, with a stated reconciliation rule when both capture the same window. **Shipped 2026-09-10:** tunnel config, SSH alias, pinned host key and restic passphrase written outside the repository; `scripts/offsite_backup.py` (encrypted restic snapshot of `data/`, `artifacts/`, `registry/`, weekly prune and check, quota warning at 80 GiB); scheduler job `backup_offsite` Sunday 22:30 ET; daemon restarted on the new schedule (112 enabled jobs). Design and bring-up order in `docs/offsite_backup.md`. **Same evening, measured:** tunnel up and reboot-persistent, bootstrap key rotated out, repository seeded (snapshot 56efac8f, 2.48 GB), `--run-job backup_offsite` OK, restore test hash-matched. Second-host port shipped: per-platform `uv`, POSIX argv for the ten PowerShell captures, `NFL_ATS_SCHEDULER_ROLE=capture` (captures only, never a card), `scripts/sync_captures.py` gap-fill + `DUP` diff log + verified delete, 56 three-hourly `sync_captures_*` jobs, `start_capture_scheduler.sh`, systemd user unit; `uv` and Python 3.12 installed on the server. Owner approved the Odds API key living on the server. **Open:** the sandbox refused to copy code or keys to the server; `docs/offsite_backup.md` lists the two owner-run commands, then `uv sync`, maiden `--run-job` on the server, daemon start, `loginctl enable-linger friend` from the server owner, and the first `sync_captures` run here. 512 MiB RAM on the server is unmeasured against a real capture. **Later that evening:** code and keys on the server, venv built, systemd user unit active under the capture role (81 enabled / 96 disabled), `nflverse_injuries_thu` captured OK there. Found while bringing it up: `config/source_policies.json` is read by every raw capture and is untracked in git, so a fresh copy of the repository cannot capture until it is copied by hand; `odds-ingest` also needs `data/processed/game_features.parquet`, which `sync_captures.py` now pushes. Owner decision pending on tracking `config/`. First real sync at 18:30 ET: the server's nflverse capture was recognised as a duplicate of the local 16:30 one (2 files, 1 differs in size), logged, deleted on the server, and the feature table was pushed. Without linger the systemd user unit dies with each SSH logout, so the server daemon runs under nohup until the server owner runs `loginctl enable-linger friend`. |

## Phase 11 — ambitious research

| ID | Status | Item | Definition of done |
|---|---|---|---|
| SKY-01 | 🌙 | News-to-availability model | Time-stamped reports mapped to player status with source provenance |
| SKY-02 | 🌙 | Video-derived tracking proxies | Only with lawful data and reproducible computer-vision evaluation |
| SKY-03 | 🌙 | Causal matchup effects | Separate persistent skill from opponent selection and game script |
| SKY-04 | 🌙 | Market microstructure model | Quote arrivals, book leadership, latency, and information diffusion. **2026-09-03 book-leadership descriptive measured** (`docs/book_leadership.md`, frozen design; `scripts/book_leadership.py`, `tests/test_book_leadership.py`, artifact `artifacts/book_leadership/20260903T201202Z/`): 855 games / 7,424 snapshots / 27,548 move events (2023–2025 spreads) — Bovada 5,044 first-move credits, williamhill_us highest share 0.64, big US retail books follow at 0.30–0.45; staleness medians 35–100s. Caveats disclosed (provider-clock trust, coarse cadence, first-is-not-right). Descriptive only: no registry entry, no window. The full model (latency, diffusion, sharpness) remains a moonshot. |
| SKY-05 | 🌙 | Automated research agent | Propose experiments but require fixed budgets and human approval |
| SKY-07 | 🚧 | Open benchmark suite | **Deterministic offline foundation implemented** (`src/nfl_ats/open_benchmark.py`, `docs/open_benchmark_suite.md`, 2026-09-02): canonical CSV/JSON exports pin schema, source URLs, SPDX license, hashes, row counts, chronological split boundaries, point-in-time timestamps, and withheld test labels. Submission exports are deterministic, complete, unscored, and pinned to the exact dataset manifest (11 contract tests pass). **2026-09-03 input scoping (blocker 1 resolved as scope):** per tracked `config/source_policies.json`, the only redistribution-clean input is nflverse (raw + derived `allowed_with_attribution`, CC-BY-4.0) — so a publishable release is game identity/kickoff/spread/scores plus in-repo features computed solely from nflverse inputs, with per-dataset upstream license checks still owed (some nflverse datasets carry different terms). Excluded by policy: Odds API raw quotes, weather vendors, Sagarin, PFR, Sportradar, CFBD (aggregates-only or review-gated). Remaining owner decisions: license posting + source URLs, hosting, test-label custody, leaderboard metrics. No model comparison was run. |

## Phase 12 — open lead queue

Sixty unmeasured proposals added 2026-09-04 against the 85-row idea ledger
(`docs/archive/idea_ledger.md`) — every one below is cut to avoid duplicating
it. Nothing here has been scored, no registry window has been spent, and no
number below is a finding: each row states its mechanism, its PREDECLARED
direction (the family is declared before any sign is seen, per AGENTS.md),
and the instrument that decides it. Default routing: screen on the free
CFB XLG-03 benchmark where the mechanism exists in college; NFL ATS looks
go through `rotation declare` for a fresh family window and are graded at
the opener; anything playable without touching outcomes (source builds,
dashboard, simulations) is marked ⬜ and may start immediately. Whatever
gets measured is recorded through both registries with
`probability_positive`, never "contains zero".

| ID | Status | Item | Definition of done |
|---|---|---|---|
| LEAD-28 | 🔬 | Post-bye novelty × playcaller interaction | Unscouted-looks novelty off a bye accrues to NEW playcallers; veteran staffs get only rest (which the market overvalues — the bye fade). Predeclared direction: BACK post-bye teams with first/second-year playcallers, FADE the rest. Interaction specified before scoring. **2026-09-05 CX15 measured source gap:** 494 accessible raw CSV/parquet schemas contain coaching fields only in head-coach schedules and the interim-HC table, not offensive-playcaller assignments; needs team/season/name/role/playcalling responsibility, effective start/end and public announcement/observation timestamps with source URL (archived staff pages plus dated duty-change announcements), admitted before the pool cutoff; no fetch or score, unresolved source gap, see `docs/coaching_leads.md`. **Screened 2026-09-07 evening (lane R, same family and doc as LEAD-29):** post-bye team-games split by OC tenure at the September 1 observation (OC as playcaller proxy, stated), 2009-2025 close grade, 422 post-bye games with known tenure: BACK year-1/2 OC 145-158 (47.85%), gap -2.33 [-8.53, +3.85], P+ 0.229; FADE year-3+ OC 63-56 covered (52.94%), signed gap -2.90 [-12.14, +6.35], P+ 0.268; interaction -5.09 [-17.13, +6.78], P+ 0.203. The secondary 2020-2025 opener grade leans the other way (P+ 0.590 / 0.774 / 0.762 on 110 / 46 games): era-dependent sign, unresolved either way, nothing closed. Recorded `unresolved_below_power` (5 entries with LEAD-29). **2026-09-10 follow-up (this lane; `scripts/playcaller_change_screen.py` rebuilt standalone after the 2026-09-09/10 repo-cut deleted the original script and `coordinator_changes.py`, reproducing the numbers above within ordinary bootstrap-seed noise; `docs/playcaller_change_leads.md`):** a production screen (clean-case back/fade tilt stacked on PRODUCTION weak_stack's opener pick, `lead28_post_bye_new_playcaller_on_production`) leans FOR the predeclared direction on both reads -- assigned window [2020, 2021]: +0.877 accuracy points, week-blocked 95% [-0.887, +2.643], P+ 0.8287, 22 of 456 graded picks changed; full 2020-2025 descriptive archive: +0.333 points, 95% [-0.728, +1.390], P+ 0.7337, 71 of 1,503 changed. This is the opposite lean from this row's own unconditional close-grade interaction cell above (P+ 0.203) -- a different estimand (whether the flagged group covers more, vs. whether flipping the model's own pick on the residual disagreement helps), both individually unresolved and neither resolving the other. Split-half reliability of the interaction trait (7 odd/even season pairs, 2011-2024): Pearson r +0.2691, 95% [-0.5674, +0.9187], P+ 0.754, Spearman-Brown +0.4241 -- leans positive, interval crosses zero, not a `no_split_half_reliability` closure (that ground needs a reading resolved AT zero). Pooling each half wholesale instead: odd seasons -2.50 pts P+ 0.296, even seasons -2.20 pts P+ 0.311, same sign as the pooled full-history read. 6 new `registry/weak_signals.json` entries under family `playcaller_change` (2 production reads here, 1 reliability here, 3 more with LEAD-29); rotation window [2020, 2021] declared/assigned/spent via `nfl-ats rotation record` (verdict `unresolved`). Status unchanged: unresolved either way, nothing closed, no overlay wired, no card changed. **2026-09-10 overlay wired (this lane, docs/playcaller_change_leads.md's new "Post-bye new-playcaller BACK overlay" section):** registered `post_bye_new_playcaller_back_overlay` as an `ACTIVE_PROSPECTIVE`, no-window-cost challenger (precedent: `veteran_rest_back_overlay`) implementing ONLY the back half of the production tilt above (flip onto a post-bye team with a year-1/2 OC when production's pick is on the other side; it never fades the veteran-OC side, so the +0.877/+0.333 figures above are the COMBINED back+fade tilt, not a remeasurement of this challenger's own rule -- disclosed in the doc, with a measured split showing the back component supplied 16 of 23 (window) and 53 of 73 (full archive) of the combined tilt's flips). Wired: `src/nfl_ats/post_bye_new_playcaller_back_overlay.py` (transform + recorder), an additive recorder call in `src/nfl_ats/cli_commands/publishing.py`'s `orchestrate_publish_predictions`, and a `CHALLENGER_DISPLAY_NAMES` entry ("Post-bye new playcaller back"). Confirmed live against 2026 Week 1 (model `d49194e04945a5e5`): 0 flips (Week 1 has zero post-bye games by construction; separately, `data/raw/coordinators/20260907T213814366437Z/coordinator_history.parquet` has no 2026 preseason OC rows yet, so tenure is unknown league-wide for 2026 until a fresh snapshot is captured -- a disclosed known-gap, not a bug), `record_post_bye_new_playcaller_back_overlay_decisions` recorded 14 of 16 games (2 already past kickoff), all 14 byte-identical to production's own pick. No rotation window spent, no card changed. |
| LEAD-29 | 🔬 | Interim playcaller bump | Fired-OC midseason playcaller changes produce a first-game bump like interim HCs (scheme surprise + effort). Predeclared direction: BACK the first game under a new playcaller. PER-07 covered interim HC only; coordinator dates from the PFR staff probe (LEAD-59 enables historically). **2026-09-05 CX15 measured source gap:** the local interim table covers HCs only and no dated OC/playcaller assignment table exists among 494 inspected raw tables; needs effective start/end, announcement/observation timestamps, explicit playcalling duties and source URLs per team-season; read `docs/archive/data_source_scout_v4.md` section 12 for the staff probe (LEAD-59 now concerns officials), no accepted staff output path, no fetch or score; see `docs/coaching_leads.md`. **Screened 2026-09-07 evening (lane R, Fable; coordinator measured the 5 registry entries under `lead29_playcaller_change_v1` and re-ran its tests; docs/playcaller_change_leads.md, scripts/playcaller_change_screen.py):** the source gap is closed by lane M's dated Wikipedia staff revisions. Predeclared, then measured: first regular-season game after an in-season OC/DC change whose revision instant precedes kickoff (12 events 2022-2025, 9 offence / 3 defence, minimum lead 96 h): 6-6, gap +0.00 pts, week-blocked 95% [-28.71, +33.40], P+ 0.450 at the opener; games 2-4 (descriptive) 17-14, +4.89 [-11.41, +21.53], P+ 0.693. Five of the 12 first games coincide with interim-HC first games, so this is not independent of `interim_hc_first_game`. Recorded `unresolved_below_power`; no overlay proposed (P+ below 0.5); no window spent. **2026-09-10 follow-up (this lane; same rebuilt `scripts/playcaller_change_screen.py`, `docs/playcaller_change_leads.md`):** a production screen (clean-case BACK tilt on PRODUCTION weak_stack's opener pick, `lead29_playcaller_first_game_on_production`) is degenerate on the assigned rotation window -- 0 of the 12 counted events (all 2022-2025) fall inside [2020, 2021], so 0 picks changed and the arm is byte-identical to baseline, P+ 0.500 by construction, not a real test. The full 2020-2025 descriptive archive (which contains all 12 events, though only 4 of 1,503 graded picks actually disagreed with production's own pick) leans slightly AGAINST the predeclared direction: -0.133 accuracy points, week-blocked 95% [-0.401, +0.132], P+ 0.1605; season-blocked 95% [-0.263, 0.000] touches but does not sit strictly below zero, so this is **not** a resolved wrong sign. Split-half reliability: 12 events across only 4 seasons (2022-2025) is too thin for a formal season-pair correlation (method `not_applicable`, same convention `docs/snow_game_home_prep.md` uses for other rare per-event flags); the odd-season half (7 events, 2023+2025) reads +7.18 pts, P+ 0.6407, the even-season half (5 events, 2022+2024) reads -10.03 pts, P+ 0.2858 -- the halves disagree in sign, expected noise at n=7/n=5, not a zero-reliability finding. 6 new `registry/weak_signals.json` entries under family `playcaller_change` (2 production reads here, 1 reliability here, 3 more with LEAD-28); rotation window [2020, 2021] declared/assigned/spent via `nfl-ats rotation record` (verdict `unresolved`, degenerate window effect noted). Status unchanged: unresolved, nothing closed, no overlay wired, no card changed. **2026-09-11 (lane; registration verified by the coordinator in `artifacts/prospective/challengers.json`, numbers reported by the lane):** registered as a zero-window-cost ACTIVE_PROSPECTIVE challenger `interim_playcaller_first_game_back_overlay` (`src/nfl_ats/interim_playcaller_first_game_back_overlay.py`, display name 'Back a team in its first game under a new play-caller', additive fail-open recorder in `orchestrate_publish_predictions`). Parameter-free rule: BACK a team in its first regular-season game whose kickoff is strictly after a midseason OC/DC change (the 12 adjudicated `staff_change`/`playcaller_role` events, 2022-2025), flip only when the opponent is not also uniquely flagged, else mirror. Module applied to the frozen opener archive: 4 of 1,503 picks flip, -0.1331 pts, week-blocked [-0.4008, +0.1322] P+ 0.160, season-blocked [-0.2630, 0.0000] P+ 0.043 — reproduces the recorded production screen within bootstrap noise, recorded as `playcaller_change_interim_overlay_module_full_archive`. Leans against the predeclared direction on 4 flips but is not a resolved wrong sign, so it accrues prospectively. Week 1 preview: 0 flips (no 2026 midseason change yet), 14 mirror rows recorded. Known gap: the two 2026-09-11 coordinator snapshots carry an unadjudicated `coverage_audit.json` (16 raw edits, 4 noise) and no `change_validation.json`, so the module resolves to the last adjudicated snapshot; the first real 2026 midseason change needs a hand adjudication before it fires. |
| LEAD-43 | 🔬 | Altitude fourth-quarter split | Thin-air fatigue lands late: visitors fade in 4Q at Denver beyond the full-game altitude price. Predeclared direction: BACK Denver at home (4Q-split mechanism, not the screened deficit×days acclimatization cell). **2026-09-05 CX15 measured opener screen:** fixed BACK all Denver home games changes production by -0.066534 accuracy points, 95% [-0.728960,+0.592895], probability_positive=0.38295 on 1,503 non-push pairs (47 Denver games, 25 flips); inferred decision retains production, registry `cx15_denver_home_on_production_opener` unresolved_below_power; oracle +0.798403 [0.397081,1.258278], probability_positive=1.0, separately recorded; descriptive Denver-minus-league Q4 margin +2.694462 [1.229311,4.191776] points, probability_positive=0.99985, and season odd/even late-margin correlation -0.605557 [-0.885533,-0.123608] concern different estimands, not a terminal fixed-home verdict; `docs/coaching_leads.md`. **2026-09-10 measured, extended to 2009-2025 + EPA + market-conditioning + visitor's-next-game + Mexico City (`docs/altitude_fourth_quarter.md`, `scripts/altitude_fourth_quarter_screen.py`):** Denver-minus-league Q4 scoring margin +2.373 [+1.054,+3.724] P+ 0.99985 (139 Denver games), Q4-rate +2.691 [+1.092,+4.343] P+ 0.99955, and both reproduce on independent play-level EPA (+2.567 [+1.169,+3.988] P+ 0.99985 margin; +2.909 [+1.155,+4.676] P+ 0.9996 rate); survives conditioning on the market line (closing-proxy full range +2.218 [+0.925,+3.541] P+ 0.9997; true 2020-2025 opener +2.964 [+0.972,+5.012] P+ 0.99835 — barely shrinks, doesn't collapse); opener cover rate 53.19% vs league 49.63%, +3.557 accuracy points [-10.238,+17.240] P+ 0.693; visitor's own next game leans the predeclared fatigue-carryover direction on an independent game (scoring P+ 0.865, EPA P+ 0.927, both crossing zero). Production screen on the CURRENT active model (d49194e04945a5e5): fixed BACK Denver home still -0.0665 accuracy points [-0.736,+0.602] P+ 0.391 (27/47 flips), oracle control +0.865 [+0.461,+1.334] P+ 1.0 — reproduces CX15 almost exactly after a model refit; Mexico City same construct n=1 in-window game only (thin by data availability, not by choice), unresolved. Split-half reliability stays negative on CX15's exact alternating-games method extended to 17 seasons (-0.516 [-0.768,-0.109] P+ 0.010, whole interval below zero) but a second, adjacent-odd/even-season-pair construction is inconclusive (scoring -0.370 [-0.879,+0.882]; EPA -0.244 [-0.757,+0.761]); per docs section 4 this measures season-to-season ordering of a noisy 8-9-game half-mean, a different estimand from the fixed effect (which stays positive on every other cut here), so `no_split_half_reliability` is not invoked and nothing closes. Decision unchanged: production retained (screen P+ 0.391 < 0.5), lead stays open; 14 new registry entries under family `altitude_fourth_quarter`, all `unresolved_below_power`. |
| LEAD-53 | 🚧 | Sunday-AM Best Pick re-nomination | Re-nominate the weekly Best Pick on Sunday-morning refresh probabilities (POL-11 machinery): does late info improve the top-1 slot? Predeclared direction: refreshed nomination BEATS the Tuesday nomination. Prospective from Week 1, no window (paired within-week comparison). **2026-09-05 CX17** (read: `docs/prospective_bestpick_tiebreaker.md`): Tuesday anchor and first playable Sunday-morning nomination pair are wired to a separate weekly ledger with recorded-line settlement; the served nominee stays fixed and this row remains open prospectively. **2026-09-09 first historical read** (`docs/best_pick_deadline_renomination.md`): four arms that renominate by changing the DISPERSION POOL or by following the late move, 2023-2025, 51 weeks — deadline re-rank -5.88 pts `probability_positive` 0.182, flip-avoidant 0.00 at 0.500, biggest-favourable-move -9.80 at 0.077, control +25.49 at 1.000; all `unresolved_below_power`. **2026-09-10 second historical read, this lane** (`docs/best_pick_sunday_renomination.md`, `scripts/best_pick_sunday_renomination_eval.py`, artifact `artifacts/best_pick_sunday_renomination/20260911T021349Z`): moves the PROBABILITY instead of the pool, on twice the population — **107 weeks / 1,537 games, 2020-2025**, pool-cycle **Sunday 12:30 ET** instant (the only pre-kickoff Sunday capture present in all six seasons; hourly Sunday captures start in 2023), zero weeks dropped, median 14 books, 17 weeks frozen because the Tuesday nominee had already kicked off. Grading is the pool's Best Pick rule: frozen Tuesday opener line, frozen played side. Tie rule predeclared and never alphabetical (statistic → lower Sunday dispersion → smaller \|line\| → earlier kickoff → expectation over the still-tied set); it agrees with production's served `select_nominee` in 105 of 107 weeks and never reached the final level. Results, all `unresolved_below_power`, family `best_pick_renomination`: **S3 full Sunday refresh (refreshed probability + Sunday dispersion pool) 57.28% top-1 vs the Tuesday nominee's 52.43%, +4.85 acc. pts, 95% [-5.83, +15.53], `probability_positive` 0.814**, nominee differs in 70 of 103 weeks (18-13 on the 31 it changes); **S2 refreshed probability alone 55.34%, +2.91 [-5.83, +11.65], 0.742**, nominee differs in 40 (12-9 on 21); S1 literal re-rank at the Sunday line -0.96 [-4.81, +2.88], 0.334, a **measured** near-no-op (largest \|Δp\| at the arm's own line is 0.0066 over 1,537 games, against 0.2768 at the frozen grading line); functional control 0.00 at 0.503, so all of S2 is the INSTANT, not the ranking statistic; positive control +34.62 [+25.96, +44.23] at 1.000 — the instrument resolves 35 points on 104 weeks and was never shown able to see 3-5, so it bounds nothing. Mechanism measured: the refreshed margin tracks the Sunday line at a median slope 1.0067, so S2/S3 are the market's Sunday move re-scored against the frozen line. Per-season S3 delta 0.00/+16.67/+5.88/+11.76/-5.88/0.00 (2020→2025); S3 split-half +4.00 odd / +5.66 even weeks and +7.69 / +1.96 across era halves. Sensitivity at an 11:00 ET instant (2023-2025, 52 weeks): S2 0.00 at 0.508, S3 +3.92 at 0.679 — 90 minutes matters more than 52 weeks can separate, so the live challenger's pre-noon window is not established as the right one. **Tension to resolve, stated plainly:** the 2026-09-09 A3 arm (biggest favourable move alone) scored -9.80 at 0.077 on the overlapping subset while S2 scores +3.85 at 0.708 there; the composite (Tuesday edge + move) is currently ahead of the move alone, and neither is resolved. **Decision on expected value: serve the Sunday re-nomination.** Not wired — `nfl_ats.publishing` reads the frozen Tuesday `is_best_pick`, and the registered live challenger `best_pick_sunday_renomination` implements S1, the weakest arm measured. Serving S3 needs two changes to the ranking inputs in `best_pick_refresh_prospective`: rank by the frozen side's cover probability at the frozen line, and rebuild the dispersion pool from Sunday cross-book spread over still-playable games only. The live challenger still owns the half this read cannot reach (a mid-week feature rebuild moving the probability on its own). **2026-09-10, this lane: S3 is WIRED AND SERVED** (`src/nfl_ats/best_pick_renomination.py`, `nfl_ats.pick_refresh.served_best_pick`, `docs/late_week_refresh.md` section "Sunday-morning Best Pick re-nomination"). The Sunday `refresh_sun` pass now ranks the still-playable games by the refreshed probability that the side it actually serves covers at the FROZEN Tuesday grading line, over a pool rebuilt from that day's cross-book `spread_std` across playable games only (the kicked-off-games defect the read records), with the predeclared tie rule (statistic -> lower dispersion -> smaller |frozen line| -> earlier kickoff; a surviving tie falls to ascending `game_id` and is reported as arbitrary, since a served card cannot take the expectation over the tied set). `refresh-picks --publish-card` moves the star on `CURRENT_PREDICTIONS.md` and rewrites the Best Pick sentence in plain English; the move is recorded on the pick-revision ledger as new `best_pick_before` / `best_pick_after` columns plus one row for the newly starred game, and `publish-predictions` / `publish-board` read it through `resolve_card_view`'s new `renominated_game_id`, so card, site and ledger carry one star. The frozen Tuesday `is_best_pick` on the paper-decision ledger is never rewritten. **One deliberate deviation from the historical arm, stated plainly:** the statistic is the cover probability of the side the pass SERVES, not of the Tuesday side -- identical on every game whose pick has not moved, and the only coherent choice once the late-week follow has flipped one (measured 2026-09-10 on Week 1: the Tuesday-side ranking would have starred WAS at PHI at 0.632 on a game whose served pick is PHI, whose own probability is 0.368); both numbers are reported on every pass. The arm is computed on every pass and reported as `candidate_game_id`, but served only on the Sunday pass, before the lock and before the held nominee's own deadline; everything fails open to the current star. The live challenger `best_pick_sunday_renomination` was implementing S1 and now ranks with the served S3 inputs. Rehearsed 2026-09-10 (`--run-job refresh_sun --dry`, then `refresh-picks --note lane_rehearsal`, nothing recorded, card untouched): at the Thursday instant S3's candidate is MIA at LV -- the Tuesday nominee -- at a played-side 0.5690 over 7 pool candidates, untied; at a simulated Sunday 10:00 ET instant it also holds MIA at LV, with the pool falling back to the Tuesday openers because no Sunday quotes exist yet. The instant itself stays unresolved (11:00 vs 12:30 ET do not separate on 52 weeks) and the mid-week feature-rebuild half remains untested. |
| LEAD-55 | ⬜ | Beat-writer participation aggregator | Timestamped Wed/Thu beat-reported participation (limited/full/DNP) vs official designations; disagreements flag hidden status. No ATS direction until the feed exists (source build, then a reliability gate, then a family). Done when a 2026 prospective collection writes timestamped rows weekly. **2026-09-11 (lane, negative source finding; unverified beyond its report except `config/source_policies.json` which the coordinator read):** every site that publishes day-resolved Wed/Thu/Fri practice participation is barred from automated collection by its own terms — Rotowire's practice-report JSON endpoint and CBS Sports' injury grid both prohibit crawling/scraping, ESPN's robots.txt disallows the Anthropic crawler site-wide, NFL team sites carry the league's bot clause (already RED for nfl.com); FantasyPros and walterfootball allow fetches but carry no day-resolved grid. All four checked-and-rejected sources are registered RED in `config/source_policies.json` with the clause cited so the search is not repeated. No ingest script was written. The compliant path already exists in-repo with zero new code: `scripts/capture_sportradar_injuries.py` plus the per-day `_sportradar_injury_job` entries in `scripts/capture_scheduler.py`, gated on `SPORTRADAR_API_KEY`, which is absent from the environment. **Owner action: set the Sportradar key**; the existing jobs then write the timestamped daily rows this row asks for. |
| LEAD-59 | 🚧 | Historical officials archive | **Probed 2026-09-04, half-answered** (`docs/officials_archive_probe.md`, two single fetches): 2015–2025 needs NO build — the nflverse officials feed is already snapshotted locally (2,896 REG referee rows, 99.86% crosswalk) and covers every queued crew-trait gate. 2009–2014 is blocked on both paths today (direct PFR boxscore: HTTP 403 bot wall, same as 2026-09-03; Wayback replay: HTTP 429 throttle). Unblock in order: MKT-14's polite Wayback pass, else an ESPN-listings probe, else owner decision to declare 2015+ the population. **2026-09-05 (lane AN): the polite Wayback pass BUILT** — `scripts/officials_wayback_sweep.py` (resumable, >=8s floor between requests, exponential backoff from 60s on 429/5xx, hard stop after 5 consecutive game failures; CDX-lookup-then-replay against `web.archive.org` only), `config/source_policies.json`'s new `internet_archive_pfr_boxscores` entry, 15 offline tests (`tests/test_officials_wayback_sweep.py`). **Measured**: a 6-request diagnostic backoff probe (immediate retry, then +60s/+120s/+240s/+480s) against the script's own CDX endpoint drew HTTP 429 every time — the 2026-09-03/04 throttle had not cleared even at an 8-minute single backoff, so the packaged script was not run live this session (would reproduce the same block) and no background continuation was launched. Yield this session: 0 games, 0 rows. Docs: `docs/officials_archive_probe.md`. Stays 🚧, not ✅ — a future session should re-run the sweep directly (mechanics are proven, no probing needed) before falling back to path 2 or 3.** **2026-09-07 (measured):** the throttle had cleared after three days idle; the sweep ran and exposed two defects on its first live pages, both fixed and pinned with real-capture fixtures (docs/officials_archive_probe.md, 2026-09-07 sections): (1) the CDX lookup took the EARLIEST capture, which for a boxscore URL is a pre-game placeholder with no officials block -- the query now carries `from=<gameday+1>` and the selector re-applies it; (2) the 2014-era page names the table `id="ref_info"` with bolded labels, not `id="officials"` -- both ids accepted. After the fixes every fetched page parses the full seven-person crew. Run `20260907T140309Z` (season 2014) was still in flight at commit time -- 127 of 256 games fetched, resumable on the same run id; one interruption was the coordinator's own status read colliding with the manifest's atomic replace (WinError 5), so progress is now read from the html directory. **2014 complete (measured, run `20260907T140309Z`, finished 2026-09-07 ~13:15 ET):** 254 of 256 games fetched (2 CDX lookups failed, 0 without a capture), **1,738 crew rows across 248 games, all seven positions** (Referee/Back Judge 249, the other five 248), 394 HTTP requests, no hard stop -> `data/processed/officials_pfr_wayback/20260907T140309Z/officials_2009_2014.parquet` (gitignored data). The 2009-2013 sweep was launched the same afternoon on a fresh run id (about 1,280 games, ~6 hours at the 8-second politeness delay, resumable). Next after it lands: extend the crew battery (docs/referee_battery.md) back to 2009 with the full seven-person crews and re-run the officials flags on production. **2026-09-07 evening (measured by the coordinator):** the 2009-2013 sweep (run `20260907T175420Z`) fetched 418 pages and parsed 0 officials on every one (`no officials block found`); `grep -l "Umpire\|Referee"` matches 0 of 418 files, whereas the 2014 run parsed 243 of 256. Inferred cause: the selection policy takes the EARLIEST post-game capture, and for 2009-era pages that capture predates PFR adding the officials block; a later capture of the same page should carry it. Sweep stopped at 423 manifest rows; lane N (launched the same evening) probes later captures on web.archive.org, switches selection to newest-capture with a parse-0 fallback over further captures, adds `--retry-unparsed` so the stopped run resumes without refetching parsed pages, and documents the re-sweep command in docs/officials_archive_probe.md. **Lane N result (Fable; coordinator re-ran its 31 tests and lint):** hypothesis confirmed by a 25-request live probe -- PFR added the officials block between 2012-08-19 (absent) and 2012-10-25 (present); every capture from 2012-10-25 onward parsed all seven positions on the 2009/2011/2013 probe games (14 of 14 pages), every earlier capture parsed 0 (4 of 4); both layouts (`ref_info` 2012-2015, commented `id="officials"` 2016+) were already handled, so no parser change. Changes: newest post-game capture first, CDX `limit` dropped (`limit=-3` drew HTTP 504), `--fallback-captures` (default 2), `--retry-unparsed` resume that re-parses on-disk pages before fetching newer captures and upserts manifest rows; rehearsed offline on copies of both runs. Re-sweep launched by the coordinator the same evening: 2014 run first (~16 requests; also fixes 5 stale parsed-0 rows), then the 2009-2013 run (~2,570 requests, at least 5.7 h at the 8 s floor; one Wayback lane at a time because CDX returned 429 on 2 of the probe's first 3 calls). **2026-09-08 (Opus lane Z, patch applied by the coordinator; 73 officials/crew/scanner tests green, mypy clean; `docs/officials_archive.md`):** `nfl_ats.officials_archive` turns every Wayback sweep run into one canonical seven-position crew table (newest post-game capture wins across runs; every row joins to exactly one schedule game or it raises; the sweep's own parser reused), exposes it in the nflverse feed's schema keyed on the legacy game id, and `load_officials()` is now the single loader at all five read sites -- with the archive OFF by default (`INCLUDE_ARCHIVE_DEFAULT = False`), a bit-for-bit pass-through, because switching it on widens the population every crew trait is computed over and that is a card-affecting change for LEAD-59's own re-run step. Timing contract stated: a Wayback capture is post-game by construction, so the archive proves which crew officiated, never when the assignment was published -- the same footing the 2015-2025 nflverse family already stands on (labelled `crew_identity_public_pregame_not_provably_captured_pregame`), and the prospective refresh channel is made to refuse archive rows. Real archive today: 1,416 games, 1,415 complete crews (2009 248, 2010 254, 2011 254, 2012 256, 2013 149 and filling, 2014 255); merged with the feed 31,818 rows 2009-2025 with the 2015-2025 slice identical. **Lane AD (Opus, same afternoon; coordinator re-ran its 15 tests + scanner and replayed its 23 cells; `docs/officials_archive_battery.md`):** switching the archive on for the served battery is an exact no-op -- zero trait values, flags or picks change -- because every crew trait is built from an INNER join to `game_penalties.parquet` (2015-2025 only; `experiment_runner._build_referee_trait_data`, `officials_flag_features.home_away_penalty_game_table`), so the 2009-2014 crews are dropped before a trait exists; and the referee-type builder LEFT-joins with `fillna(0.0)`, so archive-on fabricates 1,486 zero-penalty games and `pd.qcut` fails -- that builder feeds `crew_tilt_refresh_v1`, the only crew cell tracked live, which therefore cannot run archive-on without a src fix. With that wiring fixed in the research harness (served opener replay exact, 3.8e-15): the rookie-crew rule rebuilt with the archive beats its recorded feed-only build by +0.73 pts [+0.00, +1.48] P+ 0.972 and goes from -0.60 vs production (P+ 0.037) to +0.13 [-0.20, +0.53] P+ 0.700; the repeat-crew rule +0.80 [-0.73, +2.24] P+ 0.849 over its feed-only build; LEAD-33's crew-scramble statistic (positions whose official's modal crew differs from the referee's) -0.33 standalone (P+ 0.08) but +0.20 through the played card [-0.27, +0.67] P+ 0.759; archive-aware tenure on 2015-2019 +0.40 [-0.08, +0.90] P+ 0.919 (where the 2016 left-censoring bit). Two era-floor arms were added after a structural zero was seen and are disclosed as a second look; 2013 was still filling under the lane. Side-finding: `on_production_opener_confirmation.model_config` omits `probability_method`, so every on-production opener screen through that wrapper graded on ECDF while the card serves gaussian_median -- to fix. Next: the src fix (crew traits built from the crew table, penalty-derived traits left-joined honestly), then re-run the battery on production with the archive on. **2026-09-11 (lane; coverage numbers reported by the lane from `scripts/officials_coverage_report.py`, unverified by the coordinator):** the '418 pages, 0 officials' state was already superseded by the 2026-09-07 evening newest-capture fix; run `20260907T175420Z` had finished (last write 2026-09-08 12:03). The real defect was never markup: PFR boxscores carried no officials block before late 2012, so the old earliest-capture policy fetched pages with nothing to parse; the newest-capture policy pulls 2026 Wayback crawls whose officials table sits inside an HTML comment, which the regex parser already reads. Offline re-parse of both runs' caches: 0 mismatches; the 2014 parquet reproduces bit-identically (1,787 rows). Only 13 of 1,280 2009-2013 games were outstanding (transient fetch errors) — relaunched with `--retry-unparsed` on the same run id, left running. Complete crews: 2009 253/256, 2010 254/256, 2011 254/256, 2012 255/256, 2013 254/256, 2014 255/256; 41 distinct referees. Repo-cut casualty restored: `scripts/officials_coverage_report.py` (its test left un-restored under the moratorium). Next unchanged: crew traits built from the crew table before re-running the referee battery on production with the archive on. **2026-09-12 evening (opencode lanes, verified by the coordinator):** the three src changes `docs/officials_archive_battery.md` asked for are in: `officials_flag_features.py` builds crew-composition traits from the crew rows and left-joins the penalty table (NaN, never 0, where no penalty row exists), the rookie floor is the population's own first season plus one (`_rookie_eligible_season_floor`, constant kept as the empty-population fallback; summary key now `n_genuine_debut_after_floor`), and `crew_tilt_refresh_overlay._referee_name_season` drops referee-games absent from the penalty-type snapshot instead of inventing zero-penalty games (a literal inner join would have dropped 287 real modern games whose zero holding count is genuine). Measured no-op with the archive off: both crew-tilt trait tables and all five flag-feature builders bit-identical before and after. `scripts/officials_archive_battery_eval.py` restored from before the b7ed31d cut; the battery re-run with the archive on (lane G, six stages, replay gate reproduced the served opener evaluation to 3e-15, 23 cells re-recorded with `--replace`, one script edit: the card stage reads `mod18_pool_shaped_read_eval.composed_picks` because `spread_regime_opener_eval` died in the cut) measured: on 2020-2025 every archive arm is bit-identical to its feed twin (0 flips), as before, because the archive only reaches pre-2015 games; the archive's own era, 2011-2014 close-proxy grade on 996 games, reads rookie -0.301 [-1.102, +0.509] P+ 0.239 and second-meeting -0.100 [-0.799, +0.506] P+ 0.390; the proxyline-vs-openerline reads the doc said to start from are +0.665 [-0.131, +1.474] P+ 0.952 (rookie) and +0.865 [-0.612, +2.279] P+ 0.879 (second meeting); crew scramble through the played card +0.266 [-0.265, +0.801] P+ 0.841. One cell closed on an admissible ground: `officials_archive_second_meeting_archive_era_2015_2019` -0.968 [-1.911, -0.080] P+ 0.019, whole interval on the wrong side, recorded `refuted_mechanism` / `wrong_sign_resolved` for that era cell only (the same arm is +0.253 P+ 0.840 on 2020-2025, unresolved). Everything else `unresolved_below_power`. Open: with the archive on, `_build_referee_type_trait_data` still raises on duplicate bin edges (diagnostic only; no served arm uses type traits). |
| LEAD-61 | 🚧 | Second-half line archive (2020+) and a prospective 2H channel | Added 2026-09-05 from LEAD-02's sibling cell: on the 2005-2016 VegasInsider archive, backing the underdog when the second-half line disagrees with the full-game line read +7.84 accuracy points, week-blocked [+0.17, +14.47], P+ 0.976 on 37 flagged games (`half_line_script_2h_underdog`, `unresolved_below_power`; docs/lead02_half_line_script.md). No 2H line exists in the 2020-2025 Odds API archive, so the lead cannot be confirmed at the opener or played. Build: (1) check whether the Odds API's `spreads_h2` market is priced on the Tuesday board and archivable under the existing capture contract at zero or near-zero credit cost (measure with one read of the manifest headers, no spend beyond the registry floor); (2) if so, add it to the Tuesday and Saturday capture jobs; (3) once a season of pairs exists, confirm on production as a late-week refresh channel. Predeclared direction stays BACK the underdog on 2H-vs-full disagreement. **2026-09-05 (lane AM, step 1 probe, 2 of 2 granted requests spent, 0 credits charged):** the four candidate market keys (`spreads_h1`, `spreads_h2`, `totals_h1`, `totals_h2` — checked all four, not just `spreads_h2`, since `totals_h1`/`totals_h2` sit on the same endpoint and the sibling LEAD-02 doc's 2H cell scores both spread and total legs) are **not priced on the endpoint the existing ingester calls.** Request 1 (`nfl-ats odds-ingest --markets spreads_h1,spreads_h2,totals_h1,totals_h2 --regions us`, the project's own ingest CLI/`market_data.fetch_odds_api` path) returned HTTP 422 with no body surfaced by that function's error handling. Request 2 (a diagnostic script reusing the identical `ODDS_API_URL`/param construction, this time capturing the response body/headers, written to the private untracked snapshot `data/market/raw/20260905T175822Z-halves-probe/manifest.json`) got the same 422 with body `{"message":"Markets not supported by this endpoint: spreads_h1, spreads_h2, totals_h1, totals_h2","error_code":"INVALID_MARKET", ...}`. Quota headers on both calls: `x-requests-remaining` unchanged at 99985 (same as the prior same-day capture `data/market/raw/20260905T160024Z/manifest.json`, `requests_used` still 15), `x-requests-last: 0` — the rejected calls cost zero credits, so this probe spent nothing against the 600-credit floor. **Inferred, unconfirmed:** the error text says "not supported by this **endpoint**" (not "by this sport"), which is consistent with The Odds API's period/alternate markets being served only from the per-event `/v4/sports/{sport}/events/{eventId}/odds` endpoint rather than the bulk `/v4/sports/{sport}/odds` endpoint this ingester calls — that endpoint was not tested (2-request budget exhausted) and would need a different, likely per-game (not per-board) cost model if pursued. Per this row's own predeclared branching, markets absent on the current path -> **step 2 not attempted, row stays ⬜.** A future session would need a fresh network allowance to probe the per-event endpoint's existence/cost before any capture-job change is justified. Full numbers: scratchpad `reports/laneAM_half_market_probe.md`. **2026-09-05 (lane AO, step 1b, 1 of 3 granted requests spent):** confirmed. The per-event endpoint `GET /v4/sports/americanfootball_nfl/events/{eventId}/odds` returns HTTP 200 with all four half markets (`spreads_h1`, `spreads_h2`, `totals_h1`, `totals_h2`) plus `spreads_q1`, tested together on one call, one live event (SEA vs NE, 2026-09-10 kickoff, event id read from the already-archived `data/market/raw/20260905T160024Z/response.json` -- no events-list request spent). Book coverage on that event at 5 days out: `spreads_q1`/`totals_h1` 6/6 books, `spreads_h1` 5/6, `spreads_h2`/`totals_h2` 3/6 (thinner). Cost: `x-requests-last: 5` for 5 markets x 1 region, consistent with the same 1-credit-per-market-region model the bulk endpoint already shows (3 markets -> 3 credits) -- by subtraction, the 4 half markets alone cost 4 credits/event. Weekly cost for a full 16-game slate captured at both Tuesday-open and Saturday (this row's own formula): 128 credits/week (160 with `spreads_q1`), against 99,980 remaining (measured, this probe's own quota header) and today's existing 18 credits/week baseline (6 bulk-board jobs x 3 credits) -- clearly affordable, no floor is at risk (the repo's only defined floor, 600, is textually scoped to the historical backfill endpoint, `config/source_policies.json:117-120` + `src/nfl_ats/odds_backfill.py:45`, not this one). Status moved **⬜ -> 🚧**: markets exist, cost is trivial, nothing blocks a build. Full measured facts, market/book coverage table, and a per-event capture job design (which job it rides on -- `odds_tue_open`/`odds_sat` via the existing `requires=(...)` mechanism -- manifest shape reusing `parse_odds_api_response`/`write_market_snapshot` unmodified, and a quota-floor check) are in `docs/half_game_markets.md`; the capture job itself was **not implemented in this lane** per the task's own scope. Numbers: scratchpad `reports/laneAO_half_market_event_probe.md`. **2026-09-05 (lane AP, step 2 build):** implemented per the build plan above -- `src/nfl_ats/market_data_halves.py` (new: week-filtered event ids from the newest bulk snapshot, per-event fetch, quota-floor refusal, one combined snapshot via unmodified `parse_odds_api_response`/`write_market_snapshot`, the latter gaining one additive `snapshot_suffix` keyword so the output directory is `<stamp>-halves`), CLI `nfl-ats odds-ingest-halves` (`src/nfl_ats/cli_commands/market.py`), and two scheduler jobs `odds_tue_open_halves`/`odds_sat_halves` (`scripts/capture_scheduler.py`, riding `odds_tue_open`/`odds_sat`'s exact windows via `requires=(...)`, no `dedupe_dir` since the `-halves` suffix deliberately does not match the scheduler's snapshot-name pattern). No new `config/source_policies.json` entry (same `the_odds_api` source id, not a new provider) and no new `source_freshness_policy.py` row (nothing consumes this data for card-publication gating yet -- that is step 3). 16 new tests in `tests/test_odds_ingest_halves.py`, 3 more in `tests/test_capture_scheduler.py`; `ruff format/check`, `mypy src`, and the full named verification set (171 tests: this lane's own plus `test_capture_scheduler`/`test_cli_contract`/`test_source_policy`/`test_source_freshness_policy`/`test_script_contracts`/`test_market_data`/`test_odds_backfill`) all pass. **Live proof, 2026 Week 1 (measured):** `nfl-ats odds-ingest-halves --week-reference 2026-09-08T12:00:00Z` (today precedes Week 1's Tuesday-Monday cycle, so `--week-reference` selects it without fabricating the capture timestamp) captured all **16/16** events, **404** quote rows, **64** credits (`credits_per_event` 4, matching the plan), `requests_remaining` **99852** after. Per-market average books/event across the slate: `totals_h1` 4.25 (4-6), `spreads_h1` 4.12 (4-5), `spreads_h2` 2.12 (2-3), `totals_h2` 2.12 (2-3) -- confirms lane AO's single-event thinner-2H-coverage finding at full-slate scale. **Disclosed correction:** this lane spent **128 credits across two live runs, not the ~64-credit single run the fleet brief authorized** -- the first run reused one `now` value for both week-selection and the snapshot's `observed_at`, correctly picking Week 1 but fabricating a future `observed_at_utc` (`2026-09-08T12:00:00Z`, a real capture stamped as not-yet-happened); caught before reporting, the flawed snapshot was deleted, `capture_half_markets` was refactored to separate `observed_at` (always real wall-clock time) from `week_reference` (week-selection only), and the run was repeated correctly (`observed_at_utc` `2026-09-05T18:22:56Z`). Still 0.13% of the 99,980-credit balance measured before either run. Status stays **🚧** per this row's own predeclared bar: a season of paired weekly snapshots (step 3, confirming `half_line_script_2h_underdog` on production) has not run yet. Full numbers: scratchpad `reports/laneAP_half_market_capture.md`, `docs/half_game_markets.md` "Capture" section.  **2026-09-05 (CX7, measured rerun after 42d78f6):** corrected pregame 1H +5.2928 accuracy points, 95% [-2.5132, +12.6042], probability_positive=0.90925, n=48; 2H +16.6667 [0.0000, +25.0000], probability_positive=0.92352, n=4; unresolved close-graded leads, with both 2H-era estimates unavailable (`artifacts/vegasinsider/cx7_42d78f6_lead02/results.json`; full correction in `docs/lead02_half_line_script.md`). **2026-09-11 (lane; job runs verified by the coordinator via `--status`):** the 2026-09-05 `odds_sat_halves` FAIL was a real defect (the pre-fix current-calendar-week event filter resolved to an empty window before Week 1's cycle began), fixed the same day by `42d78f6` (`filter_events_to_next_week`) with real 16/16-event halves captures on 09-07/08/09 — the job itself had simply never been re-exercised for real. Wednesday and Thursday games had only the Tuesday half-line snapshot, and no Sunday/Monday halves job existed, so four paired jobs were added mirroring `odds_sat_halves` (`odds_wed_opener_halves` Wed 18:00, `odds_thu_tnf_halves` Thu 18:00, `odds_sun_close_halves` Sun 12:30, `odds_mon_mnf_halves` Mon 19:00, each requiring its parent bulk job) and each executed once for real: MANUAL-RUN OK x4, 14/14 remaining-week events, 56 credits each, quota 99,192 -> 98,968. All six half-market windows (Tue/Wed/Thu/Sat/Sun/Mon) now exist and have run. No prospective 2H challenger, ledger or recorder exists in code (grep: zero hits; 65 registered challengers, none half-line) — the only registry entries are the offline 2005-2016 `half_line_script` family. Building the paired challenger (overlay module, ledger, `challengers.json` entry, `crew_tilt_refresh_decisions`-pattern wiring) is the next lane once the first same-day full+half pairs land (Sun 2026-09-13 12:30 ET, Mon 2026-09-14 19:00 ET). **2026-09-11 (lane, build; registration and ledger verified by the coordinator):** the paired prospective challenger now exists. `src/nfl_ats/half_line_refresh_overlay.py` reads the freshest bulk and halves snapshots captured before each refresh pass, matches bookmakers quoting both the full-game and second-half spread per game, and backs the full-game underdog when the matched-book median favourite disagrees between the two legs (mirrors the played pick otherwise; pick'em never fires). Wired into `refresh-picks` (fail-open, `--record-decisions`-gated) and `nfl-ats settle` (new `half_line_refresh` ledger, arms `played` / `half_line_2h_underdog`); registered `half_line_2h_underdog_refresh_v1` ACTIVE_PROSPECTIVE ('Back the underdog when the second-half line disagrees'). Week 1 live: 14 of 14 eligible games had a matched halves quote (3 books each), 1 fired — GB at MIN, full game MIN -1.5 but second half GB -1.5, so the challenger backs GB against the served MIN — 14 rows recorded, graded parquet written by settle with no errors. Follow-up: add the ledger to `lockday_package.PENDING_REFRESH_LEDGERS`. **2026-09-12 (opencode lane, verified by the coordinator):** done in `scripts/lockday_verify.py` `PENDING_REFRESH_LEDGERS`; `lockday_verify.py --season 2026 --week 1` prints `ok half_line_2h_underdog_refresh_v1 126 rows`. |
| LEAD-64 | ⬜ | Friday final designations reach the card before Sunday | Measured 2026-09-12 07:30 ET: the public nflverse injuries release (the only official-report source the scheduler is allowed to read; NFL.com capture paused by MKT-09, Sportradar needs a key) was last modified Fri 12:12 UTC and carried 8 game-status designations for 2026 Week 1, all on the Wednesday/Thursday teams, while PFT headlines captured Fri 15:49-21:00 UTC named Kamara, Nabers, McMillan, Odunze, Stewart, Ossai/Ponds and Sanders with Friday designations. So the served card's injury features (rebuilt by the lineups_* refits from the player snapshot) do not see Friday's designations for the Sunday slate at all; the news is read only as the late-week-follow veto. Candidate: a headline-level designation parser over `data/raw/injury_news` (player, team, out/doubtful/questionable, filed time) feeding the same `report_status` column the feature builder reads, point-in-time by article `lastmod`, with the feed's own rows taking precedence when both exist; grade the marginal on the served card the way the other refresh overlays are. **Corrected 09:45 ET the same morning:** the release is regenerated upstream daily in the morning (last-modified Fri 12:12 UTC, Sat 11:35 UTC); the Sat 06:00 pull ran before it and the 07:33 re-pull missed it by two minutes. Re-pulled at 09:41 ET: 59 designations for Week 1 (27 Out, 26 Questionable, 6 Doubtful), including both ATL quarterbacks Out -- the reason PIT went from -3.5 to -6. So the feed is adequate; the timing was wrong. Fixed in the scheduler (nflverse_injuries_<day>_0900 + player_snapshot_<day>_0915, wed-sun) and the snapshot was rebuilt by hand at 09:42 ET. The headline parser stays queued as the same-day fallback for a morning the release is late. **Lineup panel defect found 12:30 ET the same day:** the This Week lineup panel (`scripts/build_week_lineups.py`) fetched the live nflverse release and stamped every 2026 row with the week_proxy fallback (kickoff minus 24 h), so on Saturday it saw 29 of 182 report rows and showed ATL QB1 Tua Tagovailoa as "no report / plays 99%" after he had been ruled Out. It now reads the player snapshot (rows stamped at the capture that first saw them, 182/182 visible) and carries each player's designation onto the panel (Tua: Out, 0%). Remaining limit: with both ATL quarterbacks Out the play-probability model gives QB3 Cooper Rush 19% to play / 4% to start, because it has no two-quarterbacks-out history to learn from; the panel is context only, the pick does not read it. **2026-09-12 evening (opencode lanes C, F; verified by the coordinator):** the headline parser exists as a data product nothing reads yet: `nfl-ats injury-headlines` (`src/nfl_ats/injury_headlines.py`, `docs/injury_headlines.md`) unions every `data/raw/injury_news` capture, keeps first-seen per URL, parses player, position, team and designation, resolves team-less names against the newest roster season of the player snapshot, splits two-player headlines, and writes `artifacts/injury_headline_designations/<ts>/` (parquet, summary, manifest, latest.json). Measured: 458 rows over 55 captures, 237 designations (76 out, 94 questionable, 34 doubtful, 26 ir, 7 active), 221 headlines without a designation keyword. Third pass (lane H) fixes opponent-as-team, forward scope of 'rule out', and team_source gaps. Next: compare against the official feed on a late morning, then the owner call on feeding `report_status`. |
| LEAD-65 | 🚧 | Protection-mismatch tilt: does the cross-offseason window hold up? | Owner question 2026-09-13: the served pass-protection mismatch tilt builds its four-game pressure window from the previous four regular-season games (playoffs excluded, `pbp08_protection_mismatch_tilt_overlay.py:78`), and the window rolls across the offseason, so weeks 1-4 flag on last season's line and pass rush even when both have turned over. Lane `docs/lanes/lead65-protection-window-split.md`: split the tilt's marginal on the played card into window-spans-offseason (weeks 1-4) versus later, paired bootstrap, `probability_positive` per cell, recorded under family `pbp08_protection_mismatch_window_split`. A resolved wrong sign in the early cell would argue for a season-aware window (or a roster-continuity gate); an unresolved read keeps the tilt as served. **Measured 2026-09-13 (leave-one-out marginal in the played card, 2020-2025, week-blocked bootstrap):** window-spans-offseason cell (weeks 1-4) +2.50 points of the whole card, 90% [+0.55, +4.41], probability_positive 0.977 on 21 flips; weeks 5-18 -0.70, 90% [-1.66, +0.26], probability_positive 0.099 on 58 flips, `unresolved_below_power`. The opposite of the hypothesis: the edge lives where the window reaches into last season. Next: week-gated paired challenger `pbp08_protection_mismatch_early_window_v1` with the mechanism stated (a line-versus-rush matchup only last season's tail shows is not yet priced in September). |

## Recommended execution order

**Next action, refreshed 2026-09-05 (late evening) after the 25-lane Codex day
(review findings closed, validated headline numbers, layout A, calibrated lineup
chances, 39 active challengers). Pushed through `3fdd73b`.**

1. **DO THIS FIRST (owner order, 2026-09-05): turn on the late-week line-move
   rule before the Thursday 2026-09-10 refresh so it is in the picks the owner
   submits for Week 1.** The rule: if the spread moves at least half a point
   against our Tuesday pick between Wednesday and the pick deadline, switch to
   the other side. Measured on top of the picks we actually play, 2023-2025
   (799 games): +1.752 accuracy points, week-blocked 95% [-0.868, +4.375],
   `probability_positive` 0.899, positive in every season (+1.13 / +1.13 /
   +3.00), 143 of 799 picks switched. It is wired as the paired challenger
   `late_week_move_follow_refresh_v1` (src/nfl_ats/late_week_move_follow_refresh_overlay.py,
   commit 64b39fc); promote that module's decision to the served refresh pick
   in `refresh-picks`, keep recording both sides, republish the card, and
   report the Week 1 games it switches. Do not re-open the decision: the
   owner's rule is that a 0.90 marginal on the played card is played. The
   other two EV-positive constructs stay as paired challengers for now:
   trade-deadline drag (+0.877, P+ 0.986) cannot fire before November, and
   expected lineup loss (+0.658, P+ 0.665) is smaller.
   **2026-09-06: promoted in code (measured).** The served `refresh-picks`
   pick now follows the module's 0.5-point equal-book Wednesday-to-deadline
   decision with precedence over the 1.0-point consensus rule
   (`LATE_WEEK_MOVE_FOLLOW_POLICY`, shared `late_week_follow_frame` so served
   and challenger agree by construction); every revision row keeps both arms'
   evidence plus the model-only counterfactual (pick-revision ledger 33 -> 38
   columns), the paired challenger recorder keeps running unchanged, and the
   board's Season-ops note now reads the half-point rule. Pinned by 6 new
   `test_pick_refresh.py` cases (served override, sub-threshold stand,
   precedence, served/challenger parity, summary+ledger+card, fail-open) with
   the full pre-existing refresh/tilt/contract suites still green. No Week 1
   game to report yet: the Tuesday ledger the rule switches from is first
   written at the Tuesday 2026-09-08 lock (2026-09-08 is a Tuesday; earlier
   text said Monday), so the first live fire is the NEW Wednesday
   2026-09-09 6:15 PM ET pass (`refresh_wed`, below), then Thursday's.
   **2026-09-07 (measured): the rule would not have fired at all as
   scheduled.** `data/scheduler_log.txt` shows every in-season refresh job
   (`refresh_sun`, `refresh_sun_inactives_early/late`, 2026-09-06) dying on
   `usage: nfl-ats refresh-picks [-h] --season SEASON --week WEEK` -- the
   parser required a pair the schedule never passed and nothing had ever
   parsed the jobs' argv. Fixed: `refresh-picks` now defaults `--season`/
   `--week` to the active model's linked forecast
   (`cli_common._add_active_forecast_season_week_args`,
   `active_model.active_forecast_season_week`), and
   `tests/test_capture_scheduler.py` parses every scheduled `nfl-ats` argv
   against the real parser. Second gap, same day: 2026 Week 1 opens on a
   WEDNESDAY (`2026_01_NE_SEA`, 2026-09-09 20:20 ET, schedules snapshot
   20260905T211016Z) and the schedule had no post-Tuesday odds capture and no
   refresh pass before that kickoff; added `odds_wed_opener` (Wed 18:00 ET)
   and `refresh_wed` (Wed 18:15 ET, closes 19:45), both backfill-guarded.
   The scheduler daemon was restarted on the new code (44 enabled jobs).
   **2026-09-09 (measured): the served arm is now the LEADING BOOKS' MEDIAN,
   not the equal-book mean.** Predeclared in `docs/sharp_weighted_follow.md`
   before any arm was computed and measured in
   `artifacts/sharp_weighted_follow/20260909T233606Z/`: on the frozen
   2026-09-05 baseline, 2023-2025, 799 opener-graded games, the leader-median
   arm (S1) scores +3.004 accuracy points [-0.993, +6.953] `probability_positive`
   0.930 against the equal-book arm's (S4) +1.752 [-0.870, +4.326] P+ 0.907;
   head to head S1 - S4 = +1.252 [-0.990, +3.522] P+ 0.864 week-blocked,
   [0.000, +2.256] P+ 0.982 season-blocked. Mechanism: S1's fire set almost
   contains S4's (325 both, same side on all 325; 4 equal-only) plus 198 games
   where the leaders cleared 0.5 and the twelve-book mean was diluted -- the
   Tuesday card is 47.94% right on those and following the leaders makes it
   52.06%; a fire-count-matched loose equal gate does not recover it (S1
   +1.377, P+ 0.961). Served as
   `LATE_WEEK_LEADER_MEDIAN_FOLLOW_POLICY` / `late_week_leader_median_follow_0_5`;
   `late_week_follow_frame` now returns BOTH arms so the equal-book rule keeps
   recording as the paired OFF challenger under its existing
   `late_week_move_follow_refresh_v1` id, with the served arm registered as
   `late_week_leader_median_follow_v1`. The three `late_week_*` pick-revision
   columns now carry the leader-median move, side and leading-book count.
   Proven against the real data root (16 games with exposure, 1 followed:
   `2026_01_MIA_LV`, leader median -0.5 on 3 leading books) and, on a scratch
   artifacts copy, `--record-decisions` wrote the revision row with
   `movement_policy=late_week_leader_median_follow_0_5`.
2. On Tuesday 2026-09-08 run the real lock as `weekly-run --record-decisions`;
   do not create the genuine Week 1 rows early. The chain now refits and
   activates a new model id, then runs `opener-evaluation` and
   `overlay-composition` (about 17 minutes together; skipped only when the
   model id is unchanged) and ends with `publish-board` (85d2e79). Read the
   per-recorder result JSON, run `scripts/lockday_verify.py` against the real
   rows, screenshot-check the board, push. **Injury sentence expectation,
   corrected 2026-09-07 (measured):** the newest player snapshot
   (`20260905T123614Z`) has zero 2026 injury rows because the league's first
   Week 1 report is published Wednesday, so the lock's card will read "No
   injury reports had been published yet when these picks were made; they
   lean on lineups and recent play." -- that is the true state, not a
   defect. The ENG-39 `injury_feature_presence` check aborted both the
   Sunday 2026-09-06 and Monday 2026-09-07 daily forecast refreshes at
   `weekly-run` step 5 on exactly this; it now passes only when
   `players.injury_reports_absent_reason` proves the week's rows do not
   exist in the newest snapshot (recorded verbatim as a warning), and still
   fails closed otherwise (`docs/injury_timestamp_fallback.md`, 2026-09-07
   section).
3. Standing per-session contract (AGENTS.md): one visible dashboard
   improvement, `publish-board`, push; Codex lanes (gpt-6-astra) instead of
   Claude agents until the owner says otherwise; never trigger GitHub Actions.
4. **Completed 2026-09-02:** the six Tuesday recorders are automatic,
   `crew_tilt_refresh_v1` is on the late-refresh path, and the static lock-day
   rehearsal (`scripts/lockday_rehearsal.py`, now 39 active paths, 0 errors)
   imports no model stack and touches no ledger.

The historical inactives screen and MOD-13 Stage 2 are now recorded, and neither
changes the played card. Further MOD-13 work requires a new predeclaration with
a candidate-sized positive control; the injury-value follow-up remains gated on
the Week 1 prospective observation. The four formerly queued opener
confirmations are now complete on the rotation-assigned 2020-2021 block (456
paired non-push games / 35 weeks each): Reddit home-comment ratio +0.439
accuracy points, `probability_positive=0.665`; pace mismatch -0.219, P+=0.286;
away-team illness -0.439, P+=0.312; and third-down red-zone reversion -0.658,
P+=0.111. All four are recorded through both registries as
`unresolved_below_power` / `unresolved`; none changes the played card or
supplies an admissible terminal ground.

**Historical graph pointer, carried from 2026-08-26 (Wave 11) and resolved
2026-08-31.** Test schedule-adjusted
sack rate on top of the model we actually play, not on top of a bare market
baseline. In the graph screen it scored **+2.949 accuracy points** against the
bare baseline (`graph_team_stat_off_sack_rate`, week-blocked P+ 0.987,
[+0.401, +5.707]) -- the best single cell of the 38 -- but two things stand
between that number and the card, and neither is a threshold:

1. It was measured as a marginal over a **zero-feature market baseline**. The
   project's own recorded lesson is that a component positive on its own can go
   negative once stacked on the played chain, so the marginal that decides is
   the one measured on top of production (`weak_stack` / `market_residual`,
   ridge alpha 10).
2. Roughly 40% of the +2.949 is the home-tilt measurement artifact documented
   in `docs/graph_ratings_v2_screen.md` section 6: that cell's own within-week
   permutation null centres at **+1.227**, not zero, and the observed value
   sits at the 92.5th percentile of it.

**Measured 2026-08-31 (this item is now resolved as a next-action):** the
on-production marginal was scored under new rotation family
`graph_off_sack_rate_on_production` (window [2014, 2016], 749 games, 51 weeks,
predeclared in `docs/graph_team_stat_on_production.md` before scoring).
Candidate = production `weak_stack` + the frozen graph sack-rate column;
baseline = production alone; full production chain fit on both arms. Paired
delta **-0.935 accuracy points**, week-blocked 95% [-2.625, +0.809],
`probability_positive` **0.122**; the cell's own within-week permutation null
centres at +0.416 and the observed value sits at its 6.0th percentile; a
positive control (column leaked to the realized margin) scores +49.0 pts,
P+ 1.000, so the harness is not blind. On EV grounds the number does not
favour adding the feature to the played chain; recorded
`unresolved_below_power` (registry 605 → 606), NOT closed — the interval's
upper bound is positive and no positive control bounded an effect of the
screen's +2.949 size. The bare-baseline screen reading did not survive
measurement on top of production — the recorded composition lesson, again.
Parent family `graph_ratings_v2_team_stat` was not touched and keeps its
**two eligible close windows** for its own bare-baseline question; the
harness (`scripts/graph_team_stat_screen.py`,
`scripts/graph_team_stat_record.py`) remains available. The hard deadline in
this file remains Week 1 locking **2026-09-08**, which is a separate item (see
item 6 below and `docs/prospective_evidence.md`).

**Historical pointer refreshed 2026-08-31** (read-only registry survey; full
detail in `docs/pool_edge_plan.md`'s "2026-08-31 registry state and next
shots" addendum). With `off_sack_rate` now resolved-as-next-action (above),
the family's own doc names two remaining predeclared on-production
candidates: `graph_team_stat_def_yards_per_play` (95.5th percentile of its
own permutation null, the least artifact-contaminated of the three named
cells) first, then `graph_team_stat_off_rush_epa_per_play` (highest
reliability in the family, 0.987, but only the 53.5th percentile of its own
null). Three more candidates are queued behind those in the addendum's
ranked agenda: a fresh opener-graded `fluview_home_market_elevated`
confirmation on an unspent window, the `injury_value_lost_gradient` /
`_narrowed` on-production test (blocked on the 2026 prospective look landing
first), and one clean confirmation of the Best Pick dispersion-filtered
ranker on a window that is not a fourth reuse of the same 107 opener weeks.

**Historical pointer refreshed 2026-09-01 (afternoon fleet session; every number below is measured that day from the named artifact or registry entry).** Every item the 2026-08-31 pointer named as pending has now run or been resolved:

1. **The graph-on-production line is three-for-three negative, and the reliability premise is what failed.** `graph_team_stat_off_rush_epa_per_play` measured on top of PRODUCTION `weak_stack` (close-graded, rotation-assigned [2014, 2016], 749 games, 51 weeks; `docs/graph_team_stat_off_rush_epa_on_production.md`, artifact `artifacts/graph_team_stat_off_rush_epa_per_play_on_production/20260901T184239Z/results.json`): paired delta **-0.935 accuracy points**, week-blocked 95% **[-1.998, +0.135]**, `probability_positive` **0.037**; candidate 50.07% vs production 51.00%; observed at the 0.5th percentile of its own within-week null. Recorded `unresolved_below_power` under `graph_off_rush_epa_on_production` and the window spent `unresolved`; no card change. With `off_sack_rate` (-0.935, P+ 0.122) and `def_yards_per_play` (-0.668, P+ 0.189) all three cells the 38-family screen carried forward now read negative on production. The family's highest split-half reliability (0.987), replicated in sign on a disjoint opener-graded window (+1.996 pts, P+ 0.828), did **not** predict survival on top of production: reliability measures consistency, not whether the played chain already prices the trait. All three graph on-production families report `remaining_eligible_windows: 0`.
2. **The CFB replication explains the shape of that result.** On the XLG-03 clean core (8,933 graded games, 199 weeks; `docs/graph_team_stat_cfb_replication.md`), the graph column BEATS the raw differential as a single feature on all three available cells (+0.369 / +0.291 / +0.694 accuracy points, week-blocked P+ 0.798 / 0.765 / 0.897, moving 17-26% of picks) -- the first cross-league corroboration of the `graph_input_screen` family -- but ADDED on top of a benchmark that already carries the raw statistic it is worth about nothing (-0.011 / +0.022 / -0.179 points, P+ 0.467 / 0.535 / 0.266, intervals of +/-0.29 to +/-0.69 points, roughly four times tighter than the NFL on-production reading's). Interpretive consequence: the NFL -0.935 / -0.668 readings sit inside the noise of a true effect near zero and should be described as consistent with a null, not as the graph damaging the chain. The graph lane, if pursued, is a REPLACEMENT for a raw team-state input, not another column (queued as WP24 on CFB first). `off_sack_rate` has no CFB counterpart (no sack/pressure column in the CFB table).
3. **`fluview_home_market_elevated` now has three independent negative-leaning reads against one close-graded positive.** The family's second opener window, (2022, 2023), read **-1.751 pts, 95% [-4.501, +0.986], P+ 0.094** (`docs/fluview_opener_look.md` sections 8-9; first window -0.439, P+ 0.341; a doc-only post-hoc union read 2020-2023 -1.134, P+ 0.113), and the CFB replication reads -0.388, P+ 0.200 on 5,671 games (XLG-08). Nothing closes (no interval wholly on one side of zero; trait reliability 0.98 in both leagues), but spending further NFL window on this cell is now a worse bet than it looked; the family has no default-size opener block left.
4. **Best Pick dispersion-filter confirmation remains window-blocked** (`docs/best_pick_followup.md`, "Determination: STOP"); **`injury_value_lost_gradient` remains gated** on the 2026 prospective look landing (Week 1 locks 2026-09-08).
5. **New leads opened by the fleet:** PER-13 Stage 1 met its EV gate (availability Brier +0.0075514, P+ 1.000, placebo-clean) so its Stage 2 on production is warranted; the totals tiebreaker regime ran (POL-12) and wave 2 (drive pace) is the EV favourite at P+ 0.8235; the inactives T-90 channel is designed and its capture is being built (`docs/inactives_channel.md`); the era-magnitude report (`docs/era_magnitude_report.md`) found `bye_overval_home_edge`'s split is already covered by `docs/bye_overvaluation_screen.md` and that the Sagarin family's era heterogeneity is most plausibly its own 2012 coverage hole (0 of 256 games usable), now being fixed.

**2026-09-02 completed-wave status.** The three 2026-09-01 fleet leads are now
handled: PER-13 has its opener confirmation (P+ 0.084; production is the EV
favourite, family unresolved), totals wave 2 is served behind strict identity
guards, and T-90 inactives feed a separate late-refresh challenger. The
inactives Section 5 historical proxy subsequently measured -1.3986 accuracy
points with `probability_positive=0.0418` and remains unresolved because its
large oracle control did not bound an effect of the candidate's size. MOD-13
Stage 2 was pick-identical to production on its assigned opener window (0.0000
points, `probability_positive=0.000`) and also remains unresolved: its
implementation-time target-leak control deviated from the literal predeclaration
and demonstrated only gross sensitivity. The hard operational priority is the
2026-09-08 Week 1 lock/prospective recording; it is a deadline, not a research
window to spend early.

**Graph lane, replacement question (WP24, 2026-09-01; `docs/graph_team_stat_cfb_replacement.md`).** On WP8's CFB window (8,933 games, 199 weeks), swapping a statistic's whole raw `home_`/`away_`/`diff_` triple for its graph katz differential reads **-0.257 / +0.056 / +0.022 accuracy points** (week-blocked P+ **0.145 / 0.573 / 0.528**) at the close and **-0.022 / +0.034 / -0.325** (P+ **0.462 / 0.543 / 0.221**) at the opener, moving 5-11% of picks; substitution beats addition on two of three cells (`off_success_rate` from P+ 0.266 to 0.528 on the identical window). The predeclared bar for opening an NFL replacement predeclaration (a majority above P+ 0.5 at the opener) is **not met (one of three)**, so the NFL spec is written in that doc's §10 and not run. Two by-products: an ablation arm shows each raw team-state triple is worth at most 0.21 points, so a one-metric swap moves into an almost-free slot; and with the raw columns gone the graph column earns its slot on both OFFENCE cells (`off_epa_per_play` **+0.213 pts at the opener, P+ 0.827**; `off_success_rate` +0.123 close, P+ 0.710) while losing it on DEFENCE (-0.190, P+ 0.064) -- the offence-only version is being predeclared and run as a sequential confirmation (WP35). Split-half reliability of the three CFB graph traits is now measured at Spearman-Brown **+0.9929 / +0.9925 / +0.9938** (P+ 1.000, above the raw statistic in every cell), so `no_split_half_reliability` is inadmissible for this family. All three cells `unresolved_below_power` (`graph_team_stat_cfb_replacement`); no card moves on CFB evidence.

The August 2026 build slice completed the fair-margin/market-residual outcome
workbench, empirical margin distributions, current-odds archive, cross-book
consensus, versioned 2009–2025 PBP ingestion, PBP efficiency ablation,
timestamped QB/depth foundation, straight-up pool cards, conservative Kelly
haircuts, conditional bankroll paths, a free versioned historical closing-line
cross-check, and nested rolling-origin model/feature selection. The first
full-history PBP result was negative and is retained. A later nested comparison
found only a small logistic probability improvement and no reliable ATS edge;
this is also retained. A free 2025 opener/nine-book closing sample validated
the line-movement contract, and the purchased 2020–2025 point-in-time archive
has since closed the multi-season quote-timing gap: the frozen close-prediction
pilot has taken its one look (direction of movement replicated out-of-sample;
magnitude no better than a no-movement baseline).
QB promotion is blocked by historical point-in-time coverage rather than by
model code. MOD-12 is complete for the current residual-player workbench; new
candidate families still require a newly frozen budget.

The temporal PageRank/HITS comparison is also complete. Graph candidates were
selected in zero of eight outer seasons; the simpler ridge/SRS schedule rating
was selected three times and the existing market/context model five times.
Season-blocked paired intervals found no reliable improvement, so both remain
research features rather than defaults. The PageRank/HITS screen's underlying
artifact does not survive on disk or in any commit; per `docs/closure_audit.md`
§3 its numbers are an unrecoverable prose record, registered as
`graph_schedule_rating_brier` (`registry/weak_signals.json`,
`unresolved_below_power`) rather than re-runnable evidence.

The first player-family ablation is also complete. The existing 52.05% player
profile was driven mainly by lineup continuity and QB state: injury-only reached
51.28%, continuity 51.95%, and QB plus continuity 52.34% on the same 2,075
games. A value-weighted injury extension using lagged offensive EPA and
defensive disruption per snap raised the full fixed profile to 52.14%, but its
increment over the prior full player profile was only 0.10 points and its
blocked interval crossed zero. Two-season nested profile selection reached
52.47% over 2020–2025 versus 50.88% for fixed base. Its paired accuracy interval
excluded zero under both week and season blocking, while Brier worsened and
2025 scored only 49.82%. Retain it as a refinement lead, not a promoted model;
the completed regularization/calibration gate below is the stronger decision
record, and the completed participation-based player-rating screen is documented below.

The frozen regularization/calibration gate is now complete. Four profiles ×
three Ridge strengths × four calibration policies produced 48 declared
candidates from 12 reused raw prediction streams. The prior-two-season selector
scored 50.70% over 1,582 outer games versus 50.88% for fixed base; its -0.19
point paired change had a week-blocked interval of [-2.48, +2.21] points. It is
not promoted. QB+continuity/alpha-1 led the pooled table at 52.63%, while
full-player/alpha-1/beta reached 52.34% with 0.24965 Brier; both are explicitly
post-grid leads. A separately fixed participation-rating signal was tried next
instead of expanding this grid.

The participation source is now fully ingested: 478,989 rows from 2016–2025.
The declared adjusted-plus/minus candidate used competitive valid 11-on-11
plays, a three-season lag, team effects, Ridge alpha 1,000, and a 500-play
reliability prior. Adding its two injury-value contrasts reduced matched ATS
classification from 52.14% to 51.71% over 2,075 games. The -0.43-point change
had a week-blocked interval of [-1.53, +0.63] points, and probability error
worsened. It is retained as a negative result and will not be retuned on these
seasons. The next player-availability experiment targeted the other half of the
original hypothesis: learn actual play probability from historical report,
practice, position, and next-game snap outcomes instead of assigning fixed
questionable/doubtful weights by hand. That replacement improved the direct
availability Brier score from 0.09500 to 0.09056 over 57,294 out-of-season
player-games. It moved matched ATS classification from 52.14% to 52.24%, only
two additional correct games; the week-blocked change interval was [-0.63,
+0.78] points and 2025 remained 49.08%. Retain it as a promising refinement,
not a promotion. Expected role delivery is next because any-snap participation
treats a one-play appearance as equivalent to a full workload.

Prediction integrity is permanently release-blocking: no modeling or feature
work is allowed to bypass FND-11/FND-12, even when the research code itself
appears to run successfully.

Evaluator performance is also guarded. The outcome bootstrap was rewritten
from per-draw pandas reconstruction to block-level sufficient statistics: the
standard 2,000-draw report fell from roughly 290 seconds to 0.38 seconds while
matching the old saved intervals to floating-point precision. See
`docs/performance.md` for budgets and regression rules.

The deterministic positive-control audit now establishes what those intervals
can and cannot tell us. It reproduced all 2,127 active residual predictions to
`6e-14`, reproduced every cover probability exactly, and recovered the exact
1,080/2,075 classification result. Across eight synthetic pregame signals, a
known 0.5-point-per-standard-deviation effect improved ATS accuracy by 0.78
points on average but cleared the week-blocked interval only three times; a
known 1-point effect improved accuracy by 1.42 points but cleared it only twice.
A 2-point effect improved accuracy by 3.96 points and cleared seven of eight
week and season intervals. Permuted controls produced no false interval clears.
Therefore an unresolved interval is evidence of uncertainty, not proof that a
small effect is absent. Conversely, this audit does not rescue candidates that
moved in the wrong direction or justify searching the same outcomes again.

The August 2026 replication program then spent the untouched pre-2018 windows
on the three strongest reopened leads, with every specification and decision
rule frozen before any run. ~~All three closed.~~ **Two of the three were
reclassified `unresolved` on 2026-08-18** (`registry/rotation_registry.json`,
`scripts/audit_terminal_verdicts.py`) — both negatives sat below the
instrument's own resolving power, not evidence of a null. (1) The raw-PBP
market-residual bundle, whose post-hoc 2018–2025 comparison showed +1.69
points, scored −0.08 points against base on 1,247 never-selected-on 2013–2017
games with margin error resolved worse; the +1.69 is recorded as within-window
noise plus build selection. **Re-read 2026-08-18:** f=229/1247=18.36%, split
114-115, paired SE 1.21 pts, **MDE80 3.40 pts** — the −0.08 is noise against a
3.40-point resolving power. The closure cited margin MAE, which the same
predeclaration listed as a secondary endpoint (direction only, no
gate-shopping); the one declared override was Brier, whose interval
[−0.00553, +0.00072] crosses zero and never fired. (2) QB-plus-continuity at
the declared alpha-1 scored exactly +0.00 points on 997 games in 2014–2017
(arms disagreed on 176 picks and split 88–88) with all probability diagnostics
worse; the 52.34/52.63 figures are recorded as selection artifacts. **Re-read
2026-08-18:** f=176/997=17.65%, paired SE 1.33 pts, **MDE80 3.73 pts** — the
family's own predeclaration called a null at this sample size "the modal
expected outcome, declared acceptable in advance," so a predicted null cannot
also be the evidence that closes the family. (3) Expected role delivery failed its intermediate-target
gate — the clipped delivery model lost to both parents in all 11 seasons
because injury-listed players who play at all deliver essentially their full
prior role — so no ATS rows were generated and the 2018–2025 outcome set was
not viewed again. A conservative ledger now counts roughly 130–150 candidate
streams scored against the 2018–2025 outcomes; the best pooled numbers there
(52.47–52.77%) are what selection on noise plus a possibly small real effect
would produce. The strategic conclusion is that further mining of 2018–2025
with variants of existing families is unlikely to yield trustworthy gains; new
information sources — point-in-time market quotes, CFB-replicated mechanisms,
and prospective 2026 outcomes — are the admissible paths forward. Both
replication windows (2013–2017 and 2014–2017) are declared spent for their
respective families.

