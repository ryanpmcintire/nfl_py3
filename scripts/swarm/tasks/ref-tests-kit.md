Execute reports/wave1/hyg-tests-audit.md recommendation 1 ONLY: extract the shared overlay/tilt test scaffold into tests/_overlay_test_kit.py and convert the 18 duplicated files to use it (~1,010 lines). CRITICAL: these are research-screen tests, NOT contract/safety/canary tests - but verify each converted file asserts the same things before/after (diff the test IDs). If any file is safety-adjacent, leave it and note why. Full gates.
