RE-TASK of rejected blog-rwb07-scorecards code (read reports/wave1/blog-rwb07-scorecards.md for what went wrong). The previous worker invented the clv.py API. REAL contract: build_pairing_table(root: Path, *, capture_kind, labels, seasons, schedule) builds from the market-capture ARCHIVE directory; close_reference_table(pairing: pd.DataFrame, schedule: pd.DataFrame); score_clv(picks, pairing, close_reference) needs picks with game_id, side (HOME/AWAY), decision_label columns. Read src/nfl_ats/clv.py signatures YOURSELF before writing anything. Thread an optional capture-root parameter through the season-scorecard path; when capture data is absent, return an explicit missing-data marker (never silently nan). Tests with fixtures; no real scoring looks. Full gates.
