Execute the top recommendations of reports/wave1/hyg-scripts-audit.md (read it first, plus the report on branch swarm/hyg-scripts-audit if file missing). Extract shared boilerplate (schedule loading via default_schedules, argparse scaffolding, artifact provenance writing, bootstrap helpers) into scripts/_common.py modules and convert AT LEAST the 15 highest-duplication screens to use them. Behavior-preserving only: every touched script's CLI interface and outputs stay identical. Full gates must pass. Target: >=800 net lines removed this task; do NOT archive/delete any script.
