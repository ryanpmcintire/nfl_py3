scripts/ holds ~190 python files, many one-shot research screens sharing 30-60 lines of boilerplate each (schedule loading, argparse scaffolding, artifact writing, bootstrap helpers). Quantify the boilerplate: cluster scripts by shared-block similarity, estimate lines recoverable by extracting shared helper modules, list which scripts are pure one-shot artifacts whose results are already recorded in registries (candidates for an archive/ directory - do NOT delete anything). Report to reports/wave1/hyg-scripts-audit.md. This is the single biggest bloat surface; be thorough.
