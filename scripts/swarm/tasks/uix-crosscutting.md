Cross-cutting audit across ALL four pages (index, track_record, models, findings): navigation consistency, IA (rubric dimension 4), accessibility (5), mobile robustness (9/10) - include generated-page templates in src/nfl_ats/public_board.py since much HTML is produced there. Also audit CURRENT_PREDICTIONS.md readability standalone. Same scoring/evidence rules; Score your assigned page against EVERY dimension of docs/uiux_rubric.md (read it first). Evidence rule: every score cites file+line or rendered behaviour; unexplained scores are invalid. You may run the site generator read-only to produce rendered HTML if the pipeline supports it without local data; otherwise score source HTML directly and mark inferred where rendering matters. Output reports/wave1/uix-crosscutting.md with a consolidated cross-page top-5 improvement backlog ranked by total weighted-point gain.
