# Dashboard preview

Open [This Week](ball-week.html). The connected top tabs open [The Model](ball-model.html), [History](ball-history.html), and [Findings](ball-findings.html).

The current preview preserves the board and inspector, named offensive and defensive players, human-readable lineup retrieval dates, and an always-visible tiebreaker. It adds field lighting and motion, a score scenario, expanded game room, locally saved watchlist, downloadable PNG cards, pending postgame receipts with separately labeled examples, and keyboard navigation (arrows, Enter, Escape).

This is a saved Week 1 preview, not a newly issued forecast. Score scenarios do not change the original picks. No recorded finals have been supplied.

## Probability correction

Measured in the saved forecast `artifacts/margin_predictions/2026-week-01-20260905T141453Z`: the Miami curve used the empirical calculation (57.3%), while the card and slider used the configured Gaussian calculation (54.4%). Read in `src/nfl_ats/margin.py`: the previous sweep always used empirical smoothing, regardless of the prediction setting. The previous explanation blaming refresh timing was unsupported.

The source fix passes the configured probability method into sweep generation. Rendering prefers the same verified Gaussian parameters used by the slider. Adjusted picks distinguish their decision score from the original model probability. The preview applies that same mapping to the saved curves.

Measured verification: 41/41 browser checks in `.tmp/ball-feature-results.json`; 138 focused Python tests passed for outcomes, margin, board content, and terminal rendering. Six unrelated full-suite failures were reproduced with the original HEAD source in `.tmp/ball-baseline-tests.txt`. Browser screenshots are in `.tmp/ball-*.png`.

The owner requested publication on 2026-09-06. Open the [live interactive preview](https://ryanpmcintire.github.io/nfl_py3/design/mockups/ball-week.html) or the [main dashboard](https://ryanpmcintire.github.io/nfl_py3/). The preview remains a saved Week 1 design study; the main dashboard is regenerated by `publish-board`.

## Final verification commands

Measured this session:

- `.\.tools\uv.exe run ruff format --check .`: passed.
- `.\.tools\uv.exe run ruff check .`: passed.
- `.\.tools\uv.exe run mypy src`: passed (245 source files).
- `.\.tools\uv.exe run pytest --basetemp=F:\Repos\nfl_py3\.tmp\pytest-ball-final -o cache_dir=F:\Repos\nfl_py3\.tmp\pytest-ball-cache-final -n 4 -q --tb=short`: 5,500 passed, six pre-existing failures (full output `.tmp/ball-final-pytest.txt`; baseline reproduction `.tmp/ball-baseline-tests.txt`).
- `node .tmp/verify_ball_features.cjs`: 41/41 passed, zero browser exceptions.
- `.\.tools\uv.exe run --no-sync nfl-ats publish-board --site-destination .tmp/ball-production-review`: all four pages generated. Rendered diff: index includes corrected curves, explanations, and slider marker lookup; the other three pages change generated metadata and time. Diff files: `.tmp/ball-production-*.html.diff`.
- `.\.tools\uv.exe run --no-sync nfl-ats handoff`: refreshed.

Remaining Git changes: five source files, two regression test files, HANDOFF and ROADMAP; mockup HTML, CSS, JavaScript and this index are untracked. No commit or push made.

## Publication verification - 2026-09-06

Measured: `ruff format --check .`, `ruff check .`, and `mypy src` passed using `.\.tools\uv.exe run` with a workspace-local `UV_CACHE_DIR`. Ruff reported an inaccessible pre-existing `.pytest-temp-lineup` directory.

Measured: `.\.tools\uv.exe run pytest --basetemp=F:\Repos\nfl_py3\.tmp\pytest-publish -o cache_dir=F:\Repos\nfl_py3\.tmp\pytest-publish-cache -n 4 -q --tb=short` completed with 5,500 passed and six failures (`.tmp/publish-pytest.txt`). The failing test names match the prior baseline report (`.tmp/ball-baseline-tests.txt`): two experiment-registry scanner contracts, drift step ordering, expectation copy, tiebreaker test arguments, and local totals-wave2 data alignment. Reported, unverified by a new baseline run: those failures predate this change.

Measured: `node .tmp/verify_ball_features.cjs` passed 41/41 with zero browser exceptions. `nfl-ats publish-board` regenerated `docs/index.html`, `model.html`, `history.html`, and `findings.html`. Rendered diff: corrected curves, adjusted-score explanations and slider marker lookup on This Week; generation metadata elsewhere. `git diff --check` passed. All saved mockup assets are included in the requested publication.
