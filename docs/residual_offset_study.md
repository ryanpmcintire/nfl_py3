# MOD-06 residual location study — predeclaration, 2026-09-07

Read (`src/nfl_ats/margin.py:1074-1149`): the weekly model sorts completed
regular-season training games by gameday/game_id, holds out floor(0.20*N),
fits a temporary ridge on the first 80%, and computes actual home ATS margin
minus temporary prediction on the trailing 20%. It removes nonfinite draws,
then refits the point estimator on all N games. The window expands with N;
it is not a fixed number of games. Read (`src/nfl_ats/calibration.py:294-295,326`):
Gaussian location is the unweighted arithmetic MEAN, not the empirical median.
Read (`src/nfl_ats/margin.py:496-500`, `src/nfl_ats/calibration.py:367-369`):
the residual is home-oriented; the Gaussian forced pick is home when the
predicted market residual plus this location is >= 0. Gaussian scale does
not affect that boundary. Read (`artifacts/active_ats_model.json:1`): the frozen
incumbent is 2ceaf63b56b7ce25, weak_stack/ridge/10/market_residual/gaussian,
with no probability calibration.

## Frozen family and estimators (written before candidate scoring)

Family: `mod06_residual_offset_opener_v1`, inheriting `mod08_smooth_cdf_mapping`.
All arms retain the exact incumbent point prediction, training features,
weekly cutoff and ridge configuration. Only the home-oriented offset changes:

| Arm | Definition |
|---|---|
| zero | offset 0; sign anchor, home at exact zero for consistent tie handling |
| production | mean of trailing production 20% residual sample |
| shrink_025 / shrink_050 / shrink_075 | production mean times 0.25 / 0.5 / 0.75 |
| ew_64 / ew_256 | weighted mean of that sample, weights 2^(-age/half-life), age in games, newest age zero |
| short_128 | mean of latest min(128, sample size) production draws |
| long_40pct | mean residual on trailing floor(0.40*N), generated by a temporary model fitted on the first 60%; final point model unchanged |
| median | empirical median of the production 20% draws |

The longer-window arm changes the temporary fit as required to keep every
residual out of time; its final prediction remains the incumbent's. No arm
uses any residual dated at or after the decision week's earliest archived
kickoff. The leakage test changes those outcomes and checks every offset.

## Population, governance, uncertainty and decision

Primary grade is OPENER, paired forced-pick accuracy points versus production
on `artifacts/opener_evaluation/20260907T122841Z/per_game.parquet` (the requested
1,537-row archive). Pushes are retained in predictions but excluded identically
from all accuracy denominators. Rebuilt production predictions must match the
archive before any scoring. Read (`src/nfl_ats/clv.py:2028-2030`): the established
opener evaluator replaces spread_line only; other inputs retain close-era
values. This approximation is inherited and applies equally to all arms.

Read (`docs/rotation_registry.md:22-53`, `src/nfl_ats/rotation.py:1608-1611`):
windows retire per family, 2018-2025 requires a mining acknowledgment, and
only one assigned window may be held. Declare through the CLI before scoring;
assign/score/record consecutive two-season blocks 2020-2021, 2022-2023,
2024-2025 in that order, without modifying any arm between blocks. The
full-archive combination is a declared descriptive union of these three looks,
not a fourth independent confirmation. The CLI spells `record-look` as
`rotation record` (measured: `nfl-ats rotation --help`).

Discount: this is reused, extensively mined 2020-2025 data, inheriting the
MOD-08 mapping lineage and active-model selection, plus ten correlated arm
comparisons. All estimates and probability_positive values are conditional
descriptive evidence, not fresh confirmation or ten independent pool votes.
No invented numerical discount factor is imposed. The empirical frozen-pick
family-maximum null will quantify selection optimism under this null.

Use 20,000 whole-week paired bootstrap draws, seed 20260817, percentile 95%
intervals and strict fraction(delta > 0) as probability_positive; this is the
repository week-blocked bootstrap expressed with block sums/counts for speed.
Within-week game correlation is ZERO by owner mandate: no estimated rho,
design-effect padding, effective-n reduction, or extra variance inflation.
Also report per-season deltas, disagreement counts, and zero-delta incumbent
ties explicitly (strict probability_positive=0, a tie rather than adverse EV).

Frozen-pick null: freeze all arm and incumbent sides and permute actual home
cover labels within each season/week, 2,000 draws with seed 20260817. Report
each arm's null mean, 95% interval and observed percentile, plus the maximum
delta across alternative arms in each shared null draw. No refitting under
the null and no inferred within-week correlation. The best expected-value
arm is the highest mean paired improvement (ties: incumbent, then table order);
probability_positive above 0.5 favours an arm versus incumbent. No promotion
threshold gates that decision. No active-model, profile or card changes.

## Closing-grounds taxonomy (verbatim from fleet brief)

An interval or CI that contains zero is NEVER grounds to reject, fail, or close an experiment. At this
evaluator's ~2-point resolution, "contains zero" is the EXPECTED outcome for a real small signal. Only two
grounds ever close a line of work: (1) refuted mechanism -- a RESOLVED wrong sign (whole interval on the
wrong side of zero) or zero split-half reliability; (2) bounded by a positive control proven able to
detect an effect that size. Everything else is `unresolved_below_power`: record it with
`nfl-ats weak-signals record`, report `probability_positive`, never the binary "contains zero". The
registry code hard-rejects inadmissible closures; if a record command errors, the verdict is wrong, not
the validator. Never use 95%, 0.90, or any threshold as a DECISION bar; decide on expected value
(`probability_positive` above 0.5 favours playing it), thresholds only govern what docs may CLAIM.
Grade at the OPENER (the pool's grade); a close-graded number may never veto a play. Within-week game
correlation is ZERO by owner mandate: never estimate or pad it. Never say something "needs N more
games": the data is fixed and the project is model-limited.

## Post-score leakage audit correction

Measured (schedule audit against the study per-game artifact, 2026-09-07):
2021 Week 2's archive starts Sunday although the full schedule starts Thursday.
The inherited opener evaluator therefore trains on that Thursday outcome.
The candidate offsets now stop before the full schedule week's first game;
the requested archived incumbent and common point predictions remain fixed.
Measured (pick-only repair check): this changes none of the ten arms' picks
or statistical summaries. The incumbent remains an explicitly inherited
timing approximation on this week; it is not a pristine Tuesday backtest.
The corrected per-game artifact records both cutoffs, both training maxima,
and the clean production mean, allowing the exception to be audited.
The predeclaration hash stored in artifacts covers the original text above,
excluding this explicitly post-score correction. The replay and repair modes
verify every numerical summary; repair additionally requires every side and
settlement to remain exactly unchanged. No estimator grid was retuned.

## Implementation and composed-card measurement

Lane D protocol, declared before composition scoring (2026-09-07): retain
family `mod06_residual_offset_opener_v1` and compare only `gaussian_median`
against the incumbent `gaussian`, with the already promoted union of coach
fade, division revenge, player arrests and spread-gap zone. Recompute each
arm's overlay triggers against its own baseline; freeze union membership.
Primary outcome is the paired difference in opener accuracy on the same
1,503 non-push archive games. Use 20,000 whole-week bootstrap samples,
seed 20260817, percentile 95% interval and strict probability_positive.
Within-week correlation is zero: no extra variance inflation. Compare the
Week 1 model picks separately from the composed picks. All generated output
is isolated under the fleet scratchpad's `laneD/artifacts` directory.

Read (`registry/rotation_registry.json`, family `mod06_residual_offset_opener_v1`):
all three 2020-2025 blocks were already spent by lane A. This is an overlapping
descriptive follow-up on the selected median arm, conditional on mapping,
model and overlay selection, not an independent confirmation. The discount
is this explicit selection/reuse qualification, not an invented numerical
shrinkage. The closing taxonomy above applies unchanged.

Measured (`scratchpad/laneD/comparison.json`, fleet scratchpad, 2026-09-07):
the frozen played union scores 833/1,503 (55.4225%) with the median versus
830/1,503 (55.2229%) with the mean: +0.1996 accuracy points, week-blocked
95% [-1.1281, +1.5013], probability_positive 0.59745, 107 weeks. Inferred
decision: prefer switching to `gaussian_median` on the forced-pick card;
this is an expected-value preference, not a demonstrated stable gain.

Measured (same artifact): model-only scores exactly reproduce lane A:
811 versus 802 correct, 53.9587% versus 53.3599%, +0.5988 points,
95% [-0.7338,+1.9398], probability_positive 0.79470. All median sides match
lane A, with 115 non-push disagreements (117 including pushes). Composed
sides disagree on 87 non-push games (88 including pushes). The incumbent
and candidate unions flip 432 and 420 non-push games respectively.
Measured (`scratchpad/laneD/week1_comparison.csv`): Week 1 model changes are
SEA to NE and TEN to NYJ; after the frozen union only SEA to NE remains.

Measured (`scratchpad/laneD/recording_commands.json`): recorded the composed
comparison as `mod06_residual_offset_opener_v1_gaussian_median_composed_2020_2025`
through `weak-signals record`, classification `unresolved_below_power`.
The `rotation record --replace` call preserves the latest spent window's
artifact, metrics and assignment dates, adding this overlapping full-archive
follow-up and its discount to the notes. No fresh window or independent
replication is claimed. Measured (`scratchpad/laneD/comparison.json`): composed
season deltas for 2020-2025 are -0.9091, -1.6949, +0.8065, +1.8797, -0.7519,
+1.4981 points; the effect is not uniformly positive by season.


## Production promotion implementation, 2026-09-07 (lane G)

Measured (fleet scratch `laneG/01-backtest.log` through `05-board.log`): the
actual CLI chain completed under isolated artifacts and registry roots:
median margin-backtest, Week 1 margin-predict with matching activation,
opener-evaluation, overlay-composition, and publish-board. Measured
(`laneG/06-verification.json`): all three number-provenance checks match the
scratch active model; the rendered index shows NE +3.5 and a 55.4% headline
from composition accuracy 0.5542248835662009. Measured
(`laneG/08-rendered-diff.json`): the only ticker pick change against the
current site is SEA -3.5 to NE +3.5. This reproduces the selected arm; it
is not an additional experiment or independent registry observation.

Read (`src/nfl_ats/weekly.py`, steps 4?5): both commands now explicitly name
`gaussian_median`, after the isolated proof passed. Read
(`src/nfl_ats/cli_commands/prediction.py`, margin-predict parser;
`src/nfl_ats/outcomes.py`, weekly scoring and sweep signatures): production
defaults match. Read (`src/nfl_ats/cli_commands/publishing.py`, recorder map):
`gaussian_mean_mapping_incumbent` is paired prospectively; the ECDF
challenger verifies the card's recorded mapping before reading ECDF.
Measured (`laneG/07-tests.log`): 234 tests passed. Measured
(`python scripts/lockday_rehearsal.py`): 40 active paths, 0 errors.
Read (`artifacts/active_ats_model.json`): the real manifest remains Gaussian
mean; the coordinator performs the authorized production refresh next.
See [promotion notes](gaussian_median_promotion.md).
