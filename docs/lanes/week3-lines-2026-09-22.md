# Week 3 Splash lines

## Goal

Capture the genuine 2026 Week 3 FTPL Splash Sports board without changing a pool entry, then run the established weekly-lock workflow.

## State

Blocked on acquiring the locked Week 3 board through an authorized read-only browser session. Reported from the root capability check: CDP port 9234 exposes only `about:blank`, and root-owned port 9236 is the isolated dashboard verifier; no known authenticated Splash page is visible through those endpoints. This is the known runtime limitation, not evidence that every possible browser profile or session was searched. Measured with `.\.tools\uv.exe run --no-sync python scripts\check_splash_board.py --season 2026 --week 3`: exit 1, and no `data/splash/2026_week03_*.json` capture exists. Historical Week 1 and Week 2 captures establish that an agent may read the authenticated contest board and pass its text to `capture_splash_lines.py`; owner transcription is not required. The missing official board is the upstream source for the line lock, forecast, grading ledger, card, and dashboard.

Current market acquisition is incomplete but is not a newly imposed publication prerequisite. Read in `src/nfl_ats/pick_probability.py:648` and `src/nfl_ats/pick_probability_fit.py:46`: missing or unusable movement becomes movement `0` with availability `0`, and both values are fitted features. No market-readiness gate was retained.

## Tried

- Read `docs/splash_lines.md:260-286,310-312`: the supported route is for a browser agent to open the authenticated FTPL board after it says the spreads are locked, copy the complete matchup blocks, and pass them to the capture CLI.
- Read `data/splash/2026_week01_20260908_noon.json:2-15` and `data/splash/2026_week02_20260915_1302.json:2-10,143-150`: both use `capture_method: browser_read_by_agent`. The stable locator is contest `contest_01KZKRMEGF4NNVRFG54TN5P5RH`, channel `BIG-DAN-2276`, name `FTPL - Your favorite pick 'em league!`, format `NFL Pick'Em, Winner (ATS)`, and 250 entries. Week 1 recorded slate `slate_01KZKRMEGC59N3YE61C68YP9H9`; Week 2 did not, so Week 3's slate id must be read from the page if exposed rather than reused.
- Read `scripts/scheduled_weekly_lock.py`: it validates the pool board, resolves the target from the verified schedule, and invokes the normal weekly workflow without replacing prior decisions.
- Measured the current private Odds Gap snapshot `data/market/raw/20260922T210214Z-odds-gap-private`: retrieval `2026-09-22T21:02:14Z`, one game, six quotes, three books, covering Atlanta at Green Bay only.
- Reported from the root scheduler run: the paid provider reached the service at `2026-09-22T20:50:02Z` but received HTTP 401.
- Reported from the Bovada worker: the endpoint returned HTTP 200 and `application/json`, but the body was exactly the two-byte object `{}`; no snapshot was created and no valid schema change was identified.
- Read `src/nfl_ats/tiebreaker.py:165` and `src/nfl_ats/tiebreaker.py:544`: the tiebreaker scans stored snapshots for the final game and can fall back to schedule spread and total values explicitly labeled possibly stale. That age risk is an input-health diagnostic; it does not establish a current-market capture requirement for publication.
- Removed the proposed market-readiness preflight from `scripts/scheduled_weekly_lock.py`; its tracked content has no diff.

## Next

If an already-running authorized browser/CDP session exposing Splash becomes visible, use it to open `splashsports.com`, locate the FTPL contest by the stable metadata above, select the Week 3 NFL Pick'Em slate, confirm that the page says the spreads are locked, and copy every matchup block. Do not inspect credentials or browser profiles and do not click entry or pick controls. Save the copied page text to a temporary path. Until such a session is visible, retain the bounded runtime capability blocker; do not assume owner transcription is required.

`.\.tools\uv.exe run --no-sync python scripts\capture_splash_lines.py --season 2026 --week 3 --text-file <temporary-board-text-path> --contest-id contest_01KZKRMEGF4NNVRFG54TN5P5RH --entries 250 --capture-method browser_read_by_agent --dry`

If the page exposes a Week 3 slate id, add `--slate-id <week-3-slate-id>` to both capture commands; never reuse the Week 1 slate id.

After validating the complete matchup list, repeat without `--dry` to save the capture.

`.\.tools\uv.exe run --no-sync python scripts\check_splash_board.py --season 2026 --week 3`

Once the genuine board passes validation, exercise the production scheduler entry:

`.\.tools\uv.exe run --no-sync python scripts\capture_scheduler.py --run-job weekly_lock`

Do not add `--replace`, `--replace-week`, or `--skip-ingest`. The scheduler owns the exact production argv and preserves completed Week 2 decisions.

## Open

- Current capability limitation: CDP port 9234 exposes only `about:blank`, and port 9236 is the isolated dashboard verifier. No authenticated Splash page is currently known through those endpoints; broader browser-profile or session access was not inspected.
- Operational diagnostics only: paid-provider authorization failed, Bovada returned an empty object, and Odds Gap covered one game. These do not replace the pool board and do not create a new serving gate.
- Define reader-facing market input-health semantics separately before implementing any freshness or coverage policy. Fitted movement missingness and potentially old tiebreaker inputs are distinct conditions.
