# UI-20 2026-09-24: coin-flip gap on This Week's Cover chance cell

## Goal
One visible reader-facing improvement on This Week, sourced from an existing
artifact-keyed percentage, not already shipped per the ROADMAP UI-20 tail.

## State — DONE, unpublished
Surveyed the UI-20 row tail: the flip line ("Flips at"), confidence words
(Slight/Lean/Strong with a legend), Books-now, Why this pick, tiebreaker,
season-record strip, history opener-vs-close, and the full 2026-09-05 queue
(a)-(h) are all already shipped. The default suggestion (flip line in plain
words) was already live at `board_content.py:2647`/`board_terminal.py:552-579`,
so it was skipped as duplicate.

New gap found: the This Week "Cover chance" cell shows only a raw percentage
(e.g. "56.2%") and a Slight/Lean/Strong word; nothing states in plain words how
far that sits above a coin flip. Added:
- `GameRow.coin_flip_gap_text` property, `src/nfl_ats/board_content.py:329-339`
  (right after `probability_text`): computes `(pick_probability - 0.5) * 100`,
  rounds to 1 decimal, renders "X points above a coin flip" (singular "point"
  handled) or "Even money, a true coin flip." Reuses the same
  `pick_probability` field already rendered as `probability_text` — no new
  artifact.
- Rendered as a hover tooltip on the existing "prob" table cell,
  `src/nfl_ats/board_terminal.py:912-914`, matching the established
  `title="..."` tooltip pattern already used on the Books-now cell (line
  908-910) and the flip-line cells (`_flip_line_html`).

## Tried / Verified
- `.tools\uv.exe run --no-sync ruff format` + `ruff check` on both touched
  files: clean.
- `mypy src`: `Success: no issues found in 240 source files`.
- `pytest -k "board"`: `302 passed` (no test edits needed).
- Rendered `nfl-ats publish-board --site-destination <scratch dir>` (NOT
  `docs/`) — confirmed every This Week row's Cover-chance cell now carries a
  `title="N.N points above a coin flip"` tooltip in the rendered
  `index.html` (verified via grep on the scratch output; sample: 10.5, 10.6,
  6.1, 1.1, ... points above a coin flip across the played card).
- Did not run `publish-board` against `docs/`, did not commit, did not push —
  root's job per AGENTS.md.

## Next
Root: review the rendered-page diff for `docs/index.html` (Cover chance cell
tooltip only), run real `publish-board`, push so Pages rebuilds.

## Open
None. No blockers, no code-comment additions, no test files added/edited.
